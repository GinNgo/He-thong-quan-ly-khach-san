# UI and Functional Gap Register

**Status**: Runtime/source revalidation updated on 2026-07-28; unresolved items remain explicit rather than being treated as complete.
**Categories**: `UI_UX`, `RESPONSIVE`, `ACCESSIBILITY`, `NAVIGATION`, `PERMISSION`, `API_CONTRACT`, `DATA`, `BUSINESS_RULE`, `TESTABILITY`
**Dispositions**: `FIX_NOW`, `DEFER_FEATURE`, `BLOCKED_PREREQUISITE`, `ACCEPTED_LIMITATION`

> Historical audit entries are not automatically current defects. `REVALIDATE` means source/runtime verification is still required.

| Gap ID | Severity | State | Category | Actor / Route | Problem / Hypothesis | Evidence | Disposition / Next step |
|---|---|---|---|---|---|---|---|
| GAP-001 | HIGH | REVALIDATE | API_CONTRACT, BUSINESS_RULE | Customer `/search` -> `/booking/:roomTypeId` | UI may allow selecting quantities across multiple room types while reservation contract accepts one `roomTypeId` and quantity, risking lost or inconsistent booking intent. | Historical `Feature-02` audit and `docs/audit/FAKE_OR_DISCONNECTED_FEATURES.md`; runtime/source DTO recheck pending T027 | Likely `DEFER_FEATURE`; verify current UI/DTO and create dedicated mixed-cart booking feature if still present |
| GAP-002 | MEDIUM | REVALIDATE | BUSINESS_RULE, UI_UX | Customer booking/profile | Customer add-on service selection is missing although staff-side reservation services may exist. | Historical fake/disconnected report; current public route inventory has no add-on route | `DEFER_FEATURE` unless current backend exposes a safe customer-owned contract; verify T027 |
| GAP-003 | MEDIUM | REVALIDATE | BUSINESS_RULE, DATA | Public property detail | Reviews appear in product intent/mockups but may have no backend entity/API/UI completion. | Historical fake/disconnected report; source verification pending | `DEFER_FEATURE` with separate domain/API specification if still absent |
| GAP-004 | HIGH | REVALIDATE | BUSINESS_RULE, PERMISSION | Property owner mutations | Expired subscription/quota may not consistently block mutations while preserving historical reads. | Constitution IV and historical backlog; `SubscriptionFeatureServiceTest` exists, so older claim may be stale | Verify backend tests and browser billing/inventory behavior in T016/T027 |
| GAP-005 | HIGH | REVALIDATE | API_CONTRACT, BUSINESS_RULE | Customer payment | Payment create/callback may lack full idempotency, allowing duplicate payment/invoice effects. | Historical backlog; payment tests exist and may have fixed it | Verify current service/database constraints and repeated browser action in T012/T027 |
| GAP-006 | HIGH | REVALIDATE | PERMISSION | Owner/management APIs | Forged property ID may access cross-tenant data if ownership checks are incomplete. | Historical backlog; tenant/security integration tests now exist | Verify `PropertyAccessService`, tests and property query manipulation in T016/T017/T027 |
| GAP-007 | HIGH | FIXED | NAVIGATION | Admin global search | Static quickLinks previously contained routes absent from app.routes.ts and could route users to admin 404. | Source alignment in frontend/src/app/layout/admin-layout/admin-layout.ts; browser aliases reached canonical routes | FIX_NOW completed; keep route aliases covered by regression |
| GAP-008 | HIGH | CONFIRMED_PARTIAL | PERMISSION, API_CONTRACT | Admin generic partner routes | Generic routes share one component; property-owners fails with an error and other routes expose raw API keys or no row actions. | Browser audit of all ten generic routes; see AUD-041 to AUD-050 | FIX_NOW for endpoint-specific schemas/error handling and explicit permission UX; do not weaken backend authorization |
| GAP-009 | MEDIUM | REVALIDATE | API_CONTRACT, TESTABILITY | Admin/management list pages | Several service boundaries still use `any` or raw arrays; server pagination/search may be absent or unverified. | Prior Feature-02 audit; current typed-contract/source audit pending | Type only source-confirmed DTOs; add backend query support only when a real scale/journey gap is proven |
| GAP-010 | MEDIUM | SOURCE_OBSERVED | UI_UX, NAVIGATION | Public header locale/currency button | “VI / VND” control appears interactive but no click behavior, menu or state change is connected in `ClientLayout`. | `frontend/src/app/layout/client-layout/client-layout.html` | Confirm keyboard/click behavior T011; either implement supported setting or render it as non-interactive status |
| GAP-011 | MEDIUM | FIXED | UI_UX | Admin shell | Footer used the Aurora identity instead of LuxeStay. | Browser admin shell shows LuxeStay Hospitality Group; source updated in frontend/src/app/layout/admin-layout/admin-layout.html | Closed; preserve shared branding regression |
| GAP-012 | MEDIUM | FIXED | UI_UX, DATA | Admin shell avatar | Missing avatar previously depended on external ui-avatars.com. | Source now uses local initials fallback in frontend/src/app/layout/admin-layout/admin-layout.ts; browser shows initials avatar | Closed; retain offline-safe fallback |
| GAP-013 | MEDIUM | REVALIDATE | NAVIGATION, PERMISSION | `/admin/properties` and generic partner routes | Several admin routes have auth only or no explicit frontend permission guard, so denied users may enter shell then fail opaquely. | `frontend/src/app/app.routes.ts` | Browser-test denied actor T017; add guard/denied state only as UX defense while backend remains authoritative |
| GAP-014 | MEDIUM | FIXED | TESTABILITY | Frontend automated tests | Older reports stated Vitest/Playwright crashes. | npm test -- --watch=false: 26 files/37 tests passed; npm run build: exit 0 | Closed as stale; retain jsdom canvas warning as non-blocking test-environment note |
| GAP-015 | HIGH | CONFIRMED_BROKEN | UI_UX, NAVIGATION | System admin /admin/properties | Thêm mới is rendered without a click handler, so the advertised property-create journey is inert. | Browser click produced no dialog/state change; frontend/src/app/features/admin/property-management/property-management.html has no click binding | FIX_NOW only after confirming intended property-create API/form contract; otherwise record create action as missing and defer domain work |
| GAP-016 | MEDIUM | CONFIRMED_PARTIAL | UI_UX, API_CONTRACT, TESTABILITY | Admin generic partner tables | Generic tables render raw API field names and lack route-specific actions, reducing clarity and reviewability. | Browser loaded real rows with headers such as NAME VI, HOTEL ID and IS DEMO; no row action buttons | FIX_NOW by introducing endpoint-specific column/action metadata and empty/error contracts |
| GAP-017 | MEDIUM | CONFIRMED_PARTIAL | TESTABILITY, UI_UX | Frontend build/performance | Production build passes but initial bundle exceeds configured budget and CommonJS/font-inline warnings remain. | npm run build: initial 2.53MB vs 2.00MB budget; @stomp/stompjs and sockjs-client CommonJS warnings | Follow-up performance task; do not block current functional handoff |
| GAP-018 | HIGH | CONFIRMED_BROKEN | NAVIGATION, UI_UX | Public `/hotel/:id` | Invalid property detail navigation can leave the shell with an empty main content area instead of a not-found or recoverable error state. | EVD-003: `/hotel/999999` loaded shell with empty main content | `FIX_NOW`; add typed not-found/error state and recovery CTA, then verify valid/invalid IDs |
| GAP-019 | HIGH | CONFIRMED_BROKEN | API_CONTRACT, UI_UX | Customer `/payment-simulator` and `/booking/:roomTypeId` | Missing reservation context is rendered as `#NaN`/`0 đ` and checkout completion remains disabled without a clear route recovery explanation. | EVD-011 and EVD-016: invalid/missing context observed through real UI | `FIX_NOW`; guard route context, render explicit invalid-session state and verify duplicate-submit/idempotency with a valid customer reservation |
| GAP-020 | HIGH | CONFIRMED_BROKEN | NAVIGATION, TESTABILITY | Admin notification and AI assistant routes | Advertised `/admin/notifications` and `/admin/ai-assistant` routes resolve to `/admin/404`, so the shell affordances cannot reach their intended surfaces. | EVD-031 and EVD-032: direct navigation ended at `/admin/404` | `FIX_NOW`; align menu/quick links with registered routes or implement the missing routes, then verify empty/error/retry states |
| GAP-021 | MEDIUM | CONFIRMED_BROKEN | UI_UX, NAVIGATION | Public/customer chat widget | Public home exposes no chat trigger or widget DOM, so the advertised open/send/offline journey has no entry point. | EVD-053: home DOM inspection found no chat affordance; admin chat empty state is separately covered by EVD-025 | `FIX_NOW` if chat is in scope; render a supported entry point and explicit offline state, otherwise remove the exposed requirement/menu copy |

## P1/P2 Reproduction Detail

Detailed reproduction, expected/actual outcome, evidence IDs, resolution and verification are added as each gap is confirmed. Fixed items above have source and browser evidence; remaining `REVALIDATE` rows still require the missing actor or branch before disposition changes. T046 browser evidence is recorded in `audit-matrix.md` under `EVD-001` through `EVD-055`.
