# UI and Functional Gap Register

**Status**: Runtime/source revalidation updated on 2026-07-28; unresolved items remain explicit rather than being treated as complete.
**Categories**: `UI_UX`, `RESPONSIVE`, `ACCESSIBILITY`, `NAVIGATION`, `PERMISSION`, `API_CONTRACT`, `DATA`, `BUSINESS_RULE`, `TESTABILITY`
**Dispositions**: `FIX_NOW`, `DEFER_FEATURE`, `BLOCKED_PREREQUISITE`, `ACCEPTED_LIMITATION`
**States**: `REVALIDATE`, `CONFIRMED_PARTIAL`, `CONFIRMED_BROKEN`, `FIXED`

> Historical audit entries are not automatically current defects. `REVALIDATE` means source/runtime verification is still required. Evidence references use comma-separated `EVD-###` IDs defined once in `audit-matrix.md`.

| Gap ID | Severity | State | Category | Actor / Route | Problem / Hypothesis | Evidence | Disposition / Next step |
|---|---|---|---|---|---|---|---|
| GAP-001 | HIGH | FIXED | API_CONTRACT, BUSINESS_RULE | Customer `/search` -> `/booking/:roomTypeId` | The historical audit assumed a mixed-room cart, but the current UI intentionally retains one `selectedRoomType` plus quantity and the reservation contract accepts the same single `roomTypeId` plus quantity. | EVD-067, EVD-071; `HotelDetailComponent`, `BookingCheckoutComponent` and `ReservationRequest` align on the single-room-type contract | Closed as a stale hypothesis. A mixed-room cart would be a separate `DEFER_FEATURE`, not a defect in the current advertised journey |
| GAP-002 | MEDIUM | CONFIRMED_PARTIAL | BUSINESS_RULE, UI_UX | Customer booking/profile | Staff-side reservation service items exist, but the routed customer checkout/profile journey has no customer-owned add-on selection contract or mutation. | EVD-071; `ReservationServiceItem`/`AddServiceRequest` are staff-side, while the routed customer booking DTO contains no service IDs | `DEFER_FEATURE`; specify ownership, pricing, cancellation/refund and invoice behavior before adding customer add-ons |
| GAP-003 | MEDIUM | CONFIRMED_PARTIAL | BUSINESS_RULE, DATA | Public property detail | Search/detail DTOs expose aggregate rating/review-count fields, but there is no review entity, controller or customer create/list journey. | EVD-071; source search found aggregate rating metadata only and no review domain/API | `DEFER_FEATURE`; create a separate review domain/API/moderation specification before exposing submission UI |
| GAP-004 | HIGH | CONFIRMED_PARTIAL | BUSINESS_RULE, PERMISSION | Property owner mutations | Effective-subscription lookup and quota checks reject missing/expired entitlement and reached limits while read-only management context/properties remain available. Full browser verification with an expired owner fixture is still missing. | EVD-071; `SubscriptionFeatureServiceTest` (6 tests), effective-date repository query and `ManagementPortalService.requireWithinLimit` source review | `BLOCKED_PREREQUISITE` for expired-owner browser data; retain read access and verify property/room/staff mutations without changing quota policy |
| GAP-005 | HIGH | CONFIRMED_PARTIAL | API_CONTRACT, BUSINESS_RULE | Customer payment | Payment create/callback must remain idempotent across repeated callbacks and concurrent retries. The payment service, unique transaction index and ownership-aware create-url path are covered, but the demo callback remains callable without an authenticated owner/session. | EVD-062, EVD-064, EVD-065; `PaymentServiceImplTest`, `V10__payment_idempotency_constraint.sql` | `BLOCKED_PREREQUISITE` for callback contract decision; retain service/index coverage and resolve GAP-022 before browser mutation testing |
| GAP-006 | HIGH | CONFIRMED_PARTIAL | PERMISSION | Owner/management APIs | Customer reservation/payment ownership and tenant A/B mutation boundaries deny tested foreign resources; wrong-role entry to the management shell is now blocked, but the full forged property-context/property-switch browser matrix remains pending. | EVD-064, EVD-065, EVD-074, EVD-075; `TenantIsolationIntegrationTest` (10 tests) plus management role-guard browser regression | `BLOCKED_PREREQUISITE` for an assigned multi-property browser fixture; do not weaken backend checks |
| GAP-007 | HIGH | FIXED | NAVIGATION | Admin global search | Static quickLinks previously contained routes absent from app.routes.ts and could route users to admin 404. | EVD-044, EVD-049, EVD-051; source alignment in `frontend/src/app/layout/admin-layout/admin-layout.ts` | `FIX_NOW` completed; keep route aliases covered by regression |
| GAP-008 | HIGH | CONFIRMED_PARTIAL | PERMISSION, API_CONTRACT | Admin generic partner routes | Endpoint-specific UI schemas, loading/empty/error contracts, read-only hints and supported actions are now implemented. The property-owners backend query still fails in the current dataset; denied-actor and pending-approval mutation branches still need representative fixtures. | EVD-057 and focused `PartnerOverviewComponent` tests; browser audit of all ten generic routes | FIX_NOW for the remaining backend query/data contract and denied/pending runtime fixtures; do not weaken backend authorization |
| GAP-009 | MEDIUM | REVALIDATE | API_CONTRACT, TESTABILITY | Admin/management list pages | Several service boundaries still use `any` or raw arrays; server pagination/search may be absent or unverified. | Prior Feature-02 audit; current typed-contract/source audit pending | Type only source-confirmed DTOs; add backend query support only when a real scale/journey gap is proven |
| GAP-010 | MEDIUM | FIXED | UI_UX, NAVIGATION, ACCESSIBILITY | Public header locale/currency status | The application currently supports only Vietnamese/VND, so the no-op button was replaced with a labeled non-interactive status. Header interactive targets are now at least 44px and the mobile sample has no horizontal overflow. | EVD-052, EVD-054, EVD-077; focused `ClientLayout` test plus desktop/375px browser verification | Closed for the misleading-control defect. Introduce a real button/menu only with locale state, translation coverage and currency-formatting contracts |
| GAP-011 | MEDIUM | FIXED | UI_UX | Admin shell | Footer used the Aurora identity instead of LuxeStay. | EVD-051; source updated in `frontend/src/app/layout/admin-layout/admin-layout.html` | Closed; preserve shared branding regression |
| GAP-012 | MEDIUM | FIXED | UI_UX, DATA | Admin shell avatar | Missing avatar previously depended on external ui-avatars.com. | EVD-051; source now uses local initials fallback in `frontend/src/app/layout/admin-layout/admin-layout.ts` | Closed; retain offline-safe fallback |
| GAP-013 | MEDIUM | CONFIRMED_PARTIAL | NAVIGATION, PERMISSION | Management shell plus `/admin/properties` and generic partner routes | A customer actor could enter `ManagementLayout` because the shell used only `authGuard`; the management route now applies the same supported role set as the backend and redirects denied actors to `/403`. Some admin routes still rely on backend denial and need representative denied fixtures. | EVD-074; `role-guard.spec.ts` and `frontend/src/app/app.routes.ts` | `FIX_NOW` completed for management wrong-role entry; retain backend authorization and browser-test the remaining admin-only routes before adding more UI guards |
| GAP-014 | MEDIUM | FIXED | TESTABILITY | Frontend automated tests | Older reports stated Vitest/Playwright crashes. | EVD-066: `npm test -- --watch=false` passed 29 files/47 tests; production build exited 0 | Closed as stale; retain jsdom canvas warning as non-blocking test-environment note |
| GAP-015 | HIGH | FIXED | UI_UX, NAVIGATION | System admin /admin/properties | The advertised property-create journey was inert because `Thêm mới` had no handler. | EVD-056: create dialog, accessible fields, footer actions and required-field validation now work; source wires the form to `POST /api/v1/hotels`. | Closed for the inert-control defect. Keep the end-to-end draft submission blocked until the current browser context receives province/ward data. |
| GAP-016 | MEDIUM | FIXED | UI_UX, API_CONTRACT, TESTABILITY | Admin generic partner tables | Generic tables no longer expose raw API field names. Each endpoint now has curated columns, localized enum/date/currency formatting, route-specific empty copy and supported actions or an explicit read-only explanation. | EVD-057; `partner-overview.component.spec.ts` covers schema, 403 state and approval action contract | Closed for the frontend presentation defect; retain regression coverage when endpoint fields evolve |
| GAP-017 | MEDIUM | FIXED | TESTABILITY, UI_UX | Frontend build/performance | Eager route imports and duplicated global styles previously pushed the initial bundle over budget; component font imports also generated inline-font budget warnings. | EVD-058, EVD-066, EVD-068: initial bundle reduced from 2.54MB to 1.08MB; 47 tests and representative browser routes pass | `FIX_NOW` completed for route/style/font scope. `ACCEPTED_LIMITATION`: upstream `@stomp/stompjs` and `sockjs-client` remain documented CommonJS warnings isolated behind the lazy admin/realtime boundary. |
| GAP-018 | HIGH | FIXED | NAVIGATION, UI_UX | Public `/hotel/:id` | Invalid property detail navigation previously left the shell with an empty main content area instead of a not-found or recoverable error state. | EVD-003, EVD-059; `HotelDetailComponent` recovery state and tests | `FIX_NOW` completed; retain invalid/valid detail regression and keep populated approved inventory as the remaining prerequisite |
| GAP-019 | HIGH | CONFIRMED_PARTIAL | API_CONTRACT, UI_UX | Customer `/payment-simulator` and `/booking/:roomTypeId` | Missing reservation context was rendered as `#NaN`/`0 đ` and checkout completion remained disabled without a clear route recovery explanation. UI context guards and duplicate-submit protection are now implemented; payment callback policy is still unresolved. | EVD-011, EVD-016, EVD-060, EVD-061, EVD-062, EVD-065 | `FIX_NOW` completed for UI; defer callback mutation verification to GAP-022 approval and a valid approved/public booking fixture |
| GAP-020 | MEDIUM | CONFIRMED_PARTIAL | UI_UX, ACCESSIBILITY, TESTABILITY | Admin notification panel and AI assistant widget in `/admin` | The actual shell controls are now browser-tested. Notification open/error/retry/close works, and AI open/close/loading plus accessible names work; AI now has bounded timeout, explicit retry, mobile sizing and Escape focus recovery. Notification data/mark-read/realtime and a real AI completion remain blocked because the current notification persistence/API path hangs. | EVD-072, EVD-073, EVD-076; seven focused AI tests | `FIX_NOW` completed for AI accessibility/recovery and real control coverage; resolve GAP-023/runtime notification health before mark-read/realtime can be completed |
| GAP-021 | MEDIUM | CONFIRMED_PARTIAL | UI_UX, NAVIGATION, ACCESSIBILITY | Public/customer chat widget | The widget is mounted and its 56px trigger opens the real support panel. T056 now provides named trigger/close controls, dialog semantics, a real `/login` router link and explicit loading/offline/send-error recovery. Authenticated history/send/reconnect remain unverified because the current browser session has no chat token. | EVD-053, EVD-078, EVD-079; `ChatWidgetComponent` browser/source audit and focused tests | `FIX_NOW` completed for accessible/recoverable UI. Keep the authenticated customer browser journey blocked until a real token/data fixture is available; do not add optimistic success |
| GAP-022 | HIGH | CONFIRMED_BROKEN | PERMISSION, BUSINESS_RULE, API_CONTRACT | Non-production `/api/payments/callback` and `/payment-simulator` | The demo callback accepts `reservationId`, `method` and caller-provided `transactionId` without authentication or a server-issued session binding, then can confirm the reservation and create a payment. This is a high-impact payment/authorization policy change, not a UI-only fix. | Source: `backend/src/main/java/com/hotel/controllers/MockPaymentController.java:22-34`; EVD-062 (callback intentionally not invoked) | `DEFER_FEATURE`; user approval required for the contract (authenticated owner callback vs signed server-issued demo transaction) before implementing authorization, replay and ownership tests |
| GAP-023 | HIGH | CONFIRMED_BROKEN | PERMISSION, API_CONTRACT | Admin notifications REST/WebSocket | `/api/notifications/**` is explicitly `permitAll`, both admin list/mark-read methods have no authorization annotation, `/ws` accepts all origins and clients subscribe to the global `/topic/notifications`. The architecture test currently whitelists this as known public debt even though the UI presents admin/system messages. | EVD-072, EVD-073; `SecurityConfig`, `NotificationController`, `WebSocketConfig`, `EndpointSecurityArchitectureTest` | `DEFER_FEATURE`; requires an explicit admin/per-user notification contract, authenticated WebSocket handshake/subscription rules and regression tests. Do not tighten this authorization implicitly inside the UI audit |
| GAP-024 | CRITICAL | CONFIRMED_PARTIAL | PERMISSION, API_CONTRACT, BUSINESS_RULE | Customer/support chat REST and WebSocket | The original caller-controlled identity/routing defect is corrected: the selected contract is central `SYSTEM.AI_CHAT`, sender IDs come from JWT principals, customer history is principal-scoped, support reads/replies require `AI_CHAT:VIEW/CREATE`, `/ws-chat` requires JWT on STOMP `CONNECT`, origins and destinations are constrained, and delivery uses `/user/queue/messages`. Full authenticated browser send/reconnect is still not observed with the current session. | EVD-078, EVD-079; `ChatController`, `WebSocketConfig`, `ChatHandshakeInterceptor`, `ChatChannelInterceptor`, `ChatAuthorizationService`, `ChatService`, `ChatServiceTest`, `ChatChannelInterceptorTest`, `ChatControllerIntegrationTest`; Constitution 3 | `FIX_NOW` completed for source/security contract and automated spoof/cross-account/offline/accessibility regression. `BLOCKED_PREREQUISITE` remains only for authenticated UI send/reconnect evidence requiring a real customer token and support actor; do not mark that branch complete without those fixtures |

## P1/P2 Reproduction Detail

Detailed reproduction, expected/actual outcome, evidence IDs, resolution and verification are added as each gap is confirmed. Fixed items above have source and browser evidence; remaining `REVALIDATE` rows still require the missing actor or branch before disposition changes. Current evidence is recorded in `audit-matrix.md` as the contiguous, unique catalog `EVD-001` to `EVD-079`. GAP-022 and GAP-023 remain intentionally stopped for user review because they change payment or notification authorization contracts; GAP-024 is converged at source/test level and only its authenticated browser branch remains blocked by missing fixtures.
