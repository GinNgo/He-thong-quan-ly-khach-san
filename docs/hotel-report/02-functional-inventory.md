# Danh mục chức năng hệ thống

Ngày rà soát: 2026-08-05

## Cập nhật P0-02J (2026-08-06)

- `PUB-014` (context `PUB-006`, `CROSS-042`): contract giá tìm kiếm là giá trị số VND, không yêu cầu JSON giữ phần thập phân `.0` hay scale cố định.
- `PropertySearchControllerIntegrationTest` chuyển từ assertion phụ thuộc representation `Double 300000.0` sang so sánh `BigDecimal` theo giá trị; vẫn giữ đúng JSON path và số tiền nghiệp vụ.
- Focused property search PASS 5/5; search/pricing regression PASS 18/18; P0-02A đến P0-02I guards đều PASS.
- Full backend: 836 total, 827 PASS, 5 FAIL, 4 ERROR; giảm đúng 1 FAIL, không phát sinh FAIL/ERROR mới. Package PASS.
- Chỉ test source thay đổi; production artifact, pricing flow, API và frontend contract không bị tác động. Evidence: `docs/hotel-report/fix-progress/P0-02J.md`.

## Cập nhật P0-02G (2026-08-06)

- `PROP-SUB-021`: read-only `/api/v1/hotels/my-hotels` được xác nhận không bị subscription gate; growth mutation vẫn fail-closed bằng `MAX_PROPERTIES`.
- `AUTH-019`/`CROSS-042`: P0-02H xác nhận 7 endpoint architecture báo đều là false positive của rule cũ; public/authenticated request-matcher contract được khai báo hẹp trong test, full backend còn 825 PASS / 7 FAIL / 4 ERROR.
- `CROSS-031`: P0-02I xác nhận generic `IllegalArgumentException` phải giữ 400/`INVALID_REQUEST` và message public an toàn, không trả raw exception text; auth/error focused PASS 7/7, auth negative regression PASS 33/33, full backend còn 826 PASS / 6 FAIL / 4 ERROR.
- FeatureGateIntegrationTest PASS 5/5: unauthenticated 401, read-only/no-feature 200, mutation/no-feature 403, mutation/feature 200, wrong-role 403.
- Feature/package/subscription policy PASS 38/38; hotel regression PASS 11/11.
- P0-02A–F guards PASS 18/18, 24/24, 40/40, 11/11, 16/16 và 21/21.
- Full backend: 836 total, 824 PASS, 8 FAIL, 4 ERROR; giảm đúng 1 FAIL và không có lỗi mới.
- Evidence: `docs/hotel-report/fix-progress/P0-02G.md`.

## Cập nhật P0-02F (2026-08-06)

- `AUTH-013`: AvatarUploadIntegrationTest PASS 3/3 ổn định hai lần; avatar/upload regression PASS 10/10.
- `AUTH-005`: auth-refresh/profile/avatar group PASS 21/21 theo mặc định, alphabetical và reverse-alphabetical.
- Fixture cleanup nay xóa `auth_refresh_tokens` trước user; production FK `auth_refresh_tokens.user_id -> users.id` giữ nguyên.
- P0-02A/B/C/D/E guards PASS 18/18, 24/24, 40/40, 11/11 và 16/16.
- Full backend: 834 total, 821 PASS, 9 FAIL, 4 ERROR; giảm đúng 1 ERROR avatar, không phát sinh lỗi mới.
- Evidence: `docs/hotel-report/fix-progress/P0-02F.md`.

## Cập nhật P0-02E (2026-08-06)

- `PUB-030`: packaged location import integration PASS 1/1; location/import/inventory regression PASS 16/16.
- `CROSS-040`: Unicode/inventory integration PASS 2/2, xác nhận bảo toàn chuỗi tiếng Việt và tìm kiếm không dấu trong H2. Trạng thái tổng thể vẫn `FAIL` vì các finding mojibake/terminology ngoài test này chưa được xử lý.
- `CROSS-042`: hai suite pin đúng `BackendApplication`; combined và reverse-order đều PASS 3/3; P0-02A/B/C/D guards PASS 18/18, 24/24, 40/40 và 11/11.
- Full backend: 834 total, 820 PASS, 9 FAIL, 5 ERROR, 0 SKIPPED; giảm đúng 2 ERROR `multiple @SpringBootConfiguration`, không phát sinh lỗi mới.
- Evidence: `docs/hotel-report/fix-progress/P0-02E.md`.

Tài liệu này chuyển đổi 179 capability có trạng thái trong `docs/audit/system/MASTER_FUNCTION_INVENTORY.md` sang taxonomy báo cáo yêu cầu. Trạng thái được ánh xạ: `COMPLETE_VERIFIED -> PASS`, `PARTIAL -> PARTIAL`, `BROKEN/PLACEHOLDER/MISSING -> FAIL`, `BLOCKED_EXTERNAL -> BLOCKED`, `NOT_APPLICABLE -> OUT_OF_SCOPE`. Các trạng thái phản ánh bằng chứng hiện có cộng với lần build/test/browser ngày 2026-08-05; PASS không được suy ra chỉ từ việc có source.

## Thống kê

| Trạng thái | Số lượng |
|---|---:|
| BLOCKED | 7 |
| FAIL | 56 |
| PARTIAL | 55 |
| PASS | 61 |
| **Tổng** | **179** |

## Ma trận chức năng

| Function ID | Module | Tên chức năng / mục đích | Actor | Route giao diện | Component / page | API / controller | Service / repository | Bảng dữ liệu / external | Trạng thái | Ưu tiên | Ảnh hưởng báo cáo | Screenshot | Sơ đồ | Ghi chú bằng chứng và việc còn thiếu |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AUTH-001 | Authentication and Account | Credential registration | Khách vãng lai / Khách hàng / Quản trị | `/register`; `RegisterComponent` | `AuthService.register()` normalizes identifiers and maps conflict field errors | `POST /api/auth/register`; `AuthService.register()` uses normalized pre-checks plus database-race fallback | `POST /api/auth/register`; `AuthService.register()` uses normalized pre-checks plus database-race fallback | `users.version`, normalized identity indexes, role-code and user-role uniqueness in V48 | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Public. Bằng chứng: T214 backend registration 5/5 plus service regression 6/6 and Angular register 2/2; P0-02A registration focused 5/5, order guards 18/18 theo cả hai chiều và full backend suite 5/5. Evidence: `docs/hotel-report/fix-progress/P0-02A.md`. Cần làm: Preserve normalized identity and stable 409 contract; email verification and throttling remain separate auth tasks.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-002 | Authentication and Account | Username/password login | Khách vãng lai / Khách hàng / Quản trị | `/login`, `/admin/login` | `AuthService.login()`, `setSession()`, client logout | `POST /api/auth/login`; `AuthenticationManager`, `CustomUserDetailsService`, `JwtTokenProvider` | `POST /api/auth/login`; `AuthenticationManager`, `CustomUserDetailsService`, `JwtTokenProvider` | Reads `users`, roles and permission mappings | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Public. Bằng chứng: T215 backend 10/10 and Chromium login/logout 1/1 cover normalized username/email, wrong password, unknown/disabled accounts and browser cleanup. Cần làm: Preserve enumeration-safe 401 responses and the canonical username token subject; session renewal/revocation stay in AUTH-004 to AUTH-006.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-003 | Authentication and Account | Suspended/disabled account enforcement | Khách vãng lai / Khách hàng / Quản trị | All login and authenticated routes | Login/admin-login show the bilingual suspension reason; the global 401 interceptor clears token, user and permission state and redirects protected routes with `reason=ACCOUNT_DISABLED` | `AccountStatusPolicy` rejects every status except normalized `ACTIVE`; credential/social issuance, HTTP JWT reload and new chat/notification WebSocket authentication all pass through the gate; auth handlers preserve the stable code through wrapped Spring exceptions | `AccountStatusPolicy` rejects every status except normalized `ACTIVE`; credential/social issuance, HTTP JWT reload and new chat/notification WebSocket authentication all pass through the gate; auth handlers preserve the stable code through wrapped Spring exceptions | Reads `users.status` on authentication reload | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Public login; every protected request and realtime reconnect requires an active account. Bằng chứng: Backend policy/service/filter/auth HTTP tests and Angular login/interceptor tests pass; evidence: `docs/testing/evidence/007/remediation/T193-account-status-enforcement.md`. Cần làm: Preserve the stable code and reload gate; durable session revocation and proactive termination of already-open sockets remain separate AUTH-006/CROSS-002 work.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-004 | Authentication and Account | Access-token session and renewal handling | Khách vãng lai / Khách hàng / Quản trị | All authenticated routes | `AccessTokenSessionStore` keeps access tokens in tab-scoped `sessionStorage`, migrates valid legacy tokens, rejects malformed/expired JWTs, and drives interceptor plus `AuthService` expiry cleanup | Stateless signed JWT with configured expiry; malformed and expired bearer tokens fail with stable 401 responses | Stateless signed JWT with configured expiry; malformed and expired bearer tokens fail with stable 401 responses | No server session record; access token is browser-tab scoped and user metadata is non-authoritative | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Authenticated bearer token. Bằng chứng: T216 Angular 8/8, backend HTTP 16/16 and Chromium 2/2 cover migration, expiry, malformed tokens, header denial, stale protected-route recovery and logout. Cần làm: Preserve the documented tab-scoped invariant; rotating HttpOnly refresh cookies are complete in AUTH-005/T217, while server revocation remains AUTH-006/T218.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-005 | Authentication and Account | Refresh token / silent renewal | Khách vãng lai / Khách hàng / Quản trị | Protected API retry path; no standalone UI | `AuthService.refreshAccessToken()` uses credentialed refresh and `authRefreshInterceptor` shares one in-flight request, stores the renewed access token and retries the failed request | `POST /api/auth/refresh`; `RefreshTokenService` issues hash-only rotating families, locks the candidate row, detects replay and expires/revokes sessions; successful login issues the cookie | `POST /api/auth/refresh`; `RefreshTokenService` issues hash-only rotating families, locks the candidate row, detects replay and expires/revokes sessions; successful login issues the cookie | `auth_refresh_tokens` created by additive V49; raw token only in HttpOnly cookie, SHA-256 hash and family/status lifecycle persisted | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Authenticated session plus same-origin `X-Refresh-Request` marker; backend remains authoritative. Bằng chứng: T217 backend 23/23, Angular 10/10 and production build passed; evidence: `docs/testing/evidence/007/remediation/T217-refresh-token-rotation.md`. Cần làm: Preserve hash-only storage, family replay revocation and single-flight retry; explicit logout/session revocation remains AUTH-006/T218.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-006 | Authentication and Account | Logout and token revocation | Khách vãng lai / Khách hàng / Quản trị | Profile/layout logout actions | `AuthService.logout()` clears tab-scoped artifacts, calls logout and broadcasts realtime disconnect | `POST /api/auth/logout`; refresh-family revocation plus `users.auth_revoked_at` access-token cutoff enforced by `JwtAuthFilter` | `POST /api/auth/logout`; refresh-family revocation plus `users.auth_revoked_at` access-token cutoff enforced by `JwtAuthFilter` | V50 `users.auth_revoked_at`; active `auth_refresh_tokens` rows become `REVOKED/LOGOUT` | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Same-origin marker plus server-owned bearer/cookie identity. Bằng chứng: T218 backend logout/access-token integration, Angular 5/5 and production build; evidence: `docs/testing/evidence/007/remediation/T218-logout-revocation.md`. Cần làm: Preserve durable family revocation, stable `SESSION_REVOKED` and immediate chat/notification disconnect.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-007 | Authentication and Account | Remember-me | Khách vãng lai / Khách hàng / Quản trị | Login forms | Unsupported remember-me checkbox removed; login submits only username/password under the fixed session policy | `LoginRequest` and `AuthService.login()` accept only username/password; no alternate session lifetime is exposed | `LoginRequest` and `AuthService.login()` accept only username/password; no alternate session lifetime is exposed | None | PASS | P2 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: `remember-me-removal.spec.ts` verifies both public/admin forms omit the checkbox and the request field. Cần làm: Keep the session lifetime fixed until an explicitly approved short/long policy is designed and tested.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-008 | Authentication and Account | Forgot/reset password | Khách vãng lai / Khách hàng / Quản trị | `/forgot-password`, `/reset-password?token=...`; recovery links from public/admin login | `AuthService.requestPasswordReset()` and `resetPassword()` | `POST /api/auth/forgot-password` generic 202; `POST /api/auth/reset-password` validates locked hashed one-time expiry/replay token, changes password and revokes sessions | `POST /api/auth/forgot-password` generic 202; `POST /api/auth/reset-password` validates locked hashed one-time expiry/replay token, changes password and revokes sessions | V51 `password_reset_tokens`; only token hashes and email fingerprints persisted | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Public request; server-authoritative token completion. Bằng chứng: T220 service 6/6, HTTP 1/1, Angular recovery 3/3 and production build; evidence: `docs/testing/evidence/007/remediation/T220-password-reset.md`. Cần làm: Preserve enumeration-safe response, rate limits, one-time expiry/replay and secret-managed mail settings.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-009 | Authentication and Account | Authenticated password change | Khách vãng lai / Khách hàng / Quản trị | `/settings`; admin profile password dialog | Typed `UserService.changePassword()`; shared 8-256 policy across registration/reset/settings/admin; successful change clears client auth and returns to login | `PUT /api/users/me/password` validates the DTO, returns `CURRENT_PASSWORD_INVALID`, changes the hash and revokes sessions | `PUT /api/users/me/password` validates the DTO, returns `CURRENT_PASSWORD_INVALID`, changes the hash and revokes sessions | `users.password_hash`, `users.auth_revoked_at`; active refresh rows revoked with `PASSWORD_CHANGE` | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Authenticated current principal. Bằng chứng: T221 service 3/3, HTTP 3/3, Angular 6/6 and production build; evidence: `docs/testing/evidence/007/remediation/T221-password-change.md`. Cần làm: Preserve the shared policy, stable error and mandatory reauthentication.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-010 | Authentication and Account | Email verification and email-change verification | Khách vãng lai / Khách hàng / Quản trị | `/register`, `/verify-email`, `/profile` | Typed confirm/resend/change client; verified and pending-email profile states | Hashed expiring one-time tokens; authenticated resend/change; direct profile email mutation ignored; verified changes update email/login identity and revoke sessions | Hashed expiring one-time tokens; authenticated resend/change; direct profile email mutation ignored; verified changes update email/login identity and revoke sessions | V52 `users.email_verified_at`, unique `users.pending_email`, `email_verification_tokens` | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Public token confirmation; current authenticated principal for resend/change. Bằng chứng: T222 backend 26/26, Angular 5/5 and build; P0-02A guard mở rộng PASS 26/26, không còn schema teardown làm mất `email_verification_tokens`. Evidence: `docs/hotel-report/fix-progress/P0-02A.md`. Cần làm: Preserve token/rate/uniqueness/session rules; unverified users keep browse/booking access pending an approved restrictive policy.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-011 | Authentication and Account | Current profile read | Khách vãng lai / Khách hàng / Quản trị | `/profile`, `/settings`, `/admin/profile` | Typed current-profile client with loading, error/retry and empty presentation | `GET /api/users/me`; read-only `UserService.getUserWithSaaSContext()` assembles role/property/SaaS context | `GET /api/users/me`; read-only `UserService.getUserWithSaaSContext()` assembles role/property/SaaS context | `users`, roles, assigned properties, active subscription/limits/usage and booking/chat counts | PASS | P2 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Authenticated principal id only; no caller-selected profile id. Bằng chứng: T223 controller/service 4/4, Angular profile state/regression 5/5 and build; evidence: `docs/testing/evidence/007/remediation/T223-current-profile-read.md`. Cần làm: Preserve principal-owned lookup and recoverable client states.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-012 | Authentication and Account | Profile update | Khách vãng lai / Khách hàng / Quản trị | `/profile`, `/admin/profile` | Typed profile-update payload, phone validation, verified email-change client and auth-state synchronization | Validated `PUT /api/users/me` normalizes name/phone and permits only managed upload paths or HTTPS avatars; direct email mutation is ignored and verified changes update email plus email-based username | Validated `PUT /api/users/me` normalizes name/phone and permits only managed upload paths or HTTPS avatars; direct email mutation is ignored and verified changes update email plus email-based username | `users.full_name`, `users.phone`, `users.avatar_url`; `users.email` and `users.username` change together only after verification | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Authenticated current principal only; duplicate active/pending/username identities fail closed. Bằng chứng: T224 backend 24/24, Angular 7/7 and build; evidence: `docs/testing/evidence/007/remediation/T224-profile-update.md`. Cần làm: Preserve the dedicated DTO, safe avatar allowlist, verification gate, duplicate-identity conflict and session revocation.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-013 | Authentication and Account | Avatar upload and display | Khách vãng lai / Khách hàng / Quản trị | Client/admin profile file controls and layout avatars | `UserService.uploadAvatar()` then profile update | `POST /api/uploads/image`; `FileUploadService`; public `GET /api/public/uploads/{filename}` | `POST /api/uploads/image`; `FileUploadService`; public `GET /api/public/uploads/{filename}` | Filesystem `uploads/` plus `users.avatar_url`; no upload metadata/ownership record | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Upload requires `isAuthenticated()`; read is public. Bằng chứng: P0-02F focused avatar PASS 3/3 hai lần, avatar/upload PASS 10/10 và auth/avatar order guards PASS 21/21; kiểm tra anonymous denial, signature an toàn, current-user update, replacement cleanup và public serving. Evidence: `docs/hotel-report/fix-progress/P0-02F.md`. Cần làm: Add browser evidence, persistent/object storage ownership metadata, orphan lifecycle and production storage documentation.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-014 | Authentication and Account | Registration welcome-email template and safe failure response | Khách vãng lai / Khách hàng / Quản trị | Register success message distinguishes delivered/not delivered | Typed `RegistrationResponse.welcomeEmailSent` | `EmailService.sendRegistrationSuccess()` escapes template values and fails registration email safely | `EmailService.sendRegistrationSuccess()` escapes template values and fails registration email safely | No durable outbox/retry record | PASS | P2 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Triggered after public registration. Bằng chứng: `EmailServiceTest` 2/2 and register messaging test passed. Cần làm: Keep the truthful response contract; durable retry/observability belongs to the SMTP delivery row.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-015 | Authentication and Account | Actual SMTP delivery, retry and observability | Khách vãng lai / Khách hàng / Quản trị | No mail administration UI | No status/history client | Gmail SMTP adapter configured by application properties; delivery exception is logged and returns false | Gmail SMTP adapter configured by application properties; delivery exception is logged and returns false | No outbox, delivery attempt or bounce record | BLOCKED | P2 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: External mail credential. Bằng chứng: Internal adapter/template tests passed; no sandbox inbox evidence or credentials. Cần làm: Add environment-driven host/port/security settings, a non-production mailbox contract, retry/outbox and delivery audit; configure only sandbox/test credentials via secrets.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-016 | Authentication and Account | Google social login | Khách vãng lai / Khách hàng / Quản trị | Google button is disabled unless frontend client id exists | `AuthService.googleLogin()` via `angularx-social-login` | `POST /api/auth/google`; verifies audience and verified email | `POST /api/auth/google`; verifies audience and verified email | Links/provisions by email only; no provider identity table | BLOCKED | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Public OAuth callback adapter. Bằng chứng: Fail-closed unit and disabled-UI test passed; no provider sandbox credential. Cần làm: Configure the same sandbox client id in frontend and `GOOGLE_CLIENT_ID`, register localhost origins, use test accounts, add adapter contract tests and never commit credentials.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-017 | Authentication and Account | Facebook social login | Khách vãng lai / Khách hàng / Quản trị | Facebook button disabled unless app id exists | `AuthService.facebookLogin()` via `angularx-social-login` | `POST /api/auth/facebook`; debug-token app-id check and `/me` profile lookup | `POST /api/auth/facebook`; debug-token app-id check and `/me` profile lookup | Links/provisions by email only; no provider identity table | BLOCKED | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Public OAuth callback adapter. Bằng chứng: Fail-closed unit and disabled-UI test passed; no Facebook test app/user credentials. Cần làm: Use a Facebook test app/test users, set frontend app id plus backend `FACEBOOK_APP_ID/SECRET`, verify app domains and add mocked contract/error tests; keep the secret backend-only.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-018 | Authentication and Account | Social-account provisioning/linking | Khách vãng lai / Khách hàng / Quản trị | Social login provisions only for an unowned email; existing email requires explicit authenticated linking | `AuthService.loginWithGoogle()/loginWithFacebook()` plus account-settings linked-account panel | `POST /api/auth/google`, `POST /api/auth/facebook`; `GET/POST/DELETE /api/auth/social-identities/**`; verified provider subject and email policy | `POST /api/auth/google`, `POST /api/auth/facebook`; `GET/POST/DELETE /api/auth/social-identities/**`; verified provider subject and email policy | `social_identities` with unique `(provider, provider_subject)` and `(user_id, provider)`; immutable subject columns; `users` and customer role for new accounts | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Provider verification plus authenticated JWT for link/list/unlink. Bằng chứng: T226 service tests 10/10 and Angular typed API tests 3/3; evidence: `docs/testing/evidence/007/remediation/T226-social-identity-linking.md`. P0-01C đồng bộ parent account-settings fixture với contract Observable `listSocialIdentities()`: focused account settings PASS 3/3, không còn unhandled error; full frontend suite giữ 314/317 test PASS và giảm unhandled error từ 1 xuống 0. Cần làm: Provider sandbox credentials and real OAuth browser evidence remain AUTH-016/AUTH-017 external gates.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-019 | Authentication and Account | Roles, permission masks and auth menu context | Khách vãng lai / Khách hàng / Quản trị | Route guards/layout menus consume roles/permissions | Session stores roles and permissions | Credential login returns role and permission masks; `GET /api/auth/my-menu` builds menu from masks | Credential login returns role and permission masks; `GET /api/auth/my-menu` builds menu from masks | Roles, functions and role-permission mappings | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Bearer token for meaningful results; route is under the broad public auth matcher. Bằng chứng: Credential unit does not assert masks; social login returns an empty permission list; no menu auth test. P0-01B isolated the `PartnerOverviewComponent` role fixture through the `getRoles()` contract; focused 3/3 and the same file passed in the full frontend suite. Cần làm: Align credential/social responses, explicitly annotate menu authentication, test permission aggregation and ensure server authorization never depends on client-stored masks.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-020 | Authentication and Account | Login throttling, lockout and auth audit | Khách vãng lai / Khách hàng / Quản trị | No UI state | None | No rate limiter, failed-attempt lockout, login audit or suspicious-activity event found | No rate limiter, failed-attempt lockout, login audit or suspicious-activity event found | No login-attempt/audit persistence | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Missing. Bằng chứng: No source or test found. Cần làm: Add IP/account-aware throttling, safe lockout/unlock policy, structured audit events without secrets, monitoring and deterministic time-based tests.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-021 | Authentication and Account | Auth legal/support destinations | Khách vãng lai / Khách hàng / Quản trị | Login/register footer and terms/support text | Static anchors/notices | No supporting route/API | No supporting route/API | None | FAIL | P2 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Public. Bằng chứng: Terms, privacy, cookie, contact and support links use `href="#"` or unsupported text. Cần làm: Link real localized policy/support routes, prevent page-jump placeholders and include keyboard/screen-reader/browser checks.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| AUTH-022 | Authentication and Account | Auth HTTP integration test harness | Khách vãng lai / Khách hàng / Quản trị | Not user-facing | Angular focused suite executes | `AuthControllerIntegrationTest` pins `BackendApplication` and supplies only a test encryption key | `AuthControllerIntegrationTest` pins `BackendApplication` and supplies only a test encryption key | H2 `create-drop`; Flyway disabled; context có database isolation rõ ràng | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Test-only. Bằng chứng: P0-02A chứng minh shared `testdb` từng bị `ChatControllerIntegrationTest` teardown; sau khi cô lập chat DB, auth/registration order guards PASS 18/18 theo cả hai chiều, guard mở rộng PASS 26/26 và full suite giảm 5 ERROR. Evidence: `docs/hotel-report/fix-progress/P0-02A.md`. Cần làm: Preserve pinned contexts, deterministic fixtures và database name isolation cho mọi class có `@DirtiesContext`.. Nguồn: [auth-account.md](inventory/auth-account.md) |
| PROP-SUB-001 | Property and Subscription | Public partner registration | Khách hàng / Chủ cơ sở / Quản trị | `/partner/register`; `PartnerRegisterComponent` | Direct `HttpClient` POST | `POST /api/partner/register`; `PropertyRegistrationService.registerPropertyOwner()` | `POST /api/partner/register`; `PropertyRegistrationService.registerPropertyOwner()` | Creates `users`, `hotels`, `user_properties`; `Hotel.city` and `Hotel.country` are non-null but this flow never sets them | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Public. Bằng chứng: No focused service/controller/browser test. Cần làm: Replace the nested unvalidated request with a validated DTO, collect canonical province/ward/address fields, set all required hotel columns and add transactional positive/duplicate/rollback tests.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-002 | Property and Subscription | Existing-customer partner conversion | Khách hàng / Chủ cơ sở / Quản trị | Same public form; an authenticated bearer may also reach it | Form does not distinguish signed-in conversion from new account creation | Service reuses an existing email only when the authenticated username/email matches | Service reuses an existing email only when the authenticated username/email matches | Updates existing user roles and creates property/mapping | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: Public endpoint with optional principal. Bằng chứng: Source-only. Cần làm: Split anonymous registration from authenticated conversion, prohibit password fields for signed-in conversion, normalize email identity and test cross-account takeover attempts.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-003 | Property and Subscription | Pending/draft property access gate | Khách hàng / Chủ cơ sở / Quản trị | `/management/**` | Management context keeps assigned setup/billing properties visible and hides operational links until the selected property is operational | `PropertyAccessService.assignedHotelIds()` defines draft-only ownership scope; `requireManagedHotel()` and `requireAccessibleOrNotFound()` require `approval_status=APPROVED` and `operation_status=ACTIVE`, returning `PROPERTY_NOT_OPERATIONAL` otherwise | `PropertyAccessService.assignedHotelIds()` defines draft-only ownership scope; `requireManagedHotel()` and `requireAccessibleOrNotFound()` require `approval_status=APPROVED` and `operation_status=ACTIVE`, returning `PROPERTY_NOT_OPERATIONAL` otherwise | Active mappings remain available for setup/billing; operational APIs use the approved+active gate | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Owner/management roles plus server-side property state. Bằng chứng: `PropertyAccessServiceTest` 6/6, `ManagementPortalServiceTest` 1/1, `PropertyOperationalErrorContractTest` 1/1, Angular management/dashboard tests 6/6; evidence: `docs/testing/evidence/007/remediation/T194-property-operational-access.md`. Cần làm: Preserve the split assigned/operational scopes; pending-owner lifecycle and approval transition consistency remain PROP-SUB-005/006/008 scope.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-004 | Property and Subscription | Partner registration status | Khách hàng / Chủ cơ sở / Quản trị | `/partner/registration-status` | Direct `GET /partner/registration-status` | `PropertyRegistrationService.registrationStatus()` | `PropertyRegistrationService.registrationStatus()` | Reads all user-property mappings | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Authenticated current account. Bằng chứng: No component/API test. Cần làm: Return per-property state and rejection reason; do not classify every non-empty non-pending mapping as approved; support rejected, suspended, cancelled and mixed multi-property states.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-005 | Property and Subscription | Owner submits a draft property for review | Khách hàng / Chủ cơ sở / Quản trị | Admin property form can call `PropertyService.submitProperty()` | `POST /api/v1/hotels/{id}/submit` | `HotelController.submitHotel()` changes only `status` to `PENDING` | `HotelController.submitHotel()` changes only `status` to `PENDING` | Leaves `approval_status` as `DRAFT` | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Có | Quyền: `PROPERTY_OWNER` or `SUPER_ADMIN` plus property access. Bằng chứng: No focused test; admin action visibility excludes `DRAFT`. Cần làm: Add an explicit transition service that atomically changes approval/operation state, records actor/time, rejects invalid transitions and makes the submitted row actionable in the approval queue.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-006 | Property and Subscription | Admin property approval/rejection | Khách hàng / Chủ cơ sở / Quản trị | `/admin/property-approvals`; `PartnerOverviewComponent` | `PropertyService.approveProperty()` / `rejectProperty()` | `POST /api/v1/hotels/{id}/approve\&#124;reject`; controller changes only legacy `status` | `POST /api/v1/hotels/{id}/approve\&#124;reject`; controller changes only legacy `status` | `approval_status`, `operation_status`, owner mapping and audit fields remain unchanged | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Backend requires `SUPER_ADMIN`; UI route itself has no permission guard. Bằng chứng: P0-01B restored the frontend approval regression with a contract-level role mock; focused `PartnerOverviewComponent` passed 3/3 and the file passed in the full frontend suite, including the real `/api/v1/hotels/{id}/approve` client endpoint assertion. No successful HTTP/domain transition test exists. Cần làm: Implement one state machine for submit/approve/reject, update all three status fields consistently, persist reviewer/reason/timestamps, notify owner and test public visibility plus management access.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-007 | Property and Subscription | Property suspension/reactivation/closure | Khách hàng / Chủ cơ sở / Quản trị | No dedicated UI or action | None | No suspend/reactivate/close endpoint or service found | No suspend/reactivate/close endpoint or service found | Hotel and user-property enums mention inactive/suspended values but no lifecycle evidence exists | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Missing. Bằng chứng: No source/test. Cần làm: Add permissioned reasoned transitions, suspend operational access/public inventory, preserve historical data and bookings, notify affected actors and test reactivation/idempotency.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-008 | Property and Subscription | Owner role and mapping activation timing | Khách hàng / Chủ cơ sở / Quản trị | Registration and management shell | Pending applicants do not receive `PROPERTY_OWNER`; the role is granted only when approval activates ownership | Registration and claim services create `PENDING` owner mappings; approval changes the mapping to `ACTIVE`, while reject/cancel changes it to `INACTIVE` with an end timestamp | Registration and claim services create `PENDING` owner mappings; approval changes the mapping to `ACTIVE`, while reject/cancel changes it to `INACTIVE` with an end timestamp | `app_user_role`, `user_properties` | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Có | Quyền: Operational owner authority starts at approval; pending applicants remain non-operational. Bằng chứng: P0-02D cập nhật stale WebMvc fixture cho dependency disclosure mới; `HotelControllerIntegrationTest` PASS 2/2, Hotel/registration/disclosure regression PASS 9/9 và full backend giảm đúng 2 ERROR. Các evidence ownership trước đó giữ nguyên; evidence: `docs/hotel-report/fix-progress/P0-02D.md`, `docs/testing/evidence/007/remediation/T195-owner-mapping-activation.md`. Cần làm: Preserve complete controller-slice dependencies, the activation boundary and add transfer/history policy in later ownership tasks.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-009 | Property and Subscription | Registration review history and notifications | Khách hàng / Chủ cơ sở / Quản trị | Status page has only current coarse state | No inbox/history model | No review-event/history/notification service in registration flow | No review-event/history/notification service in registration flow | No immutable onboarding transition history or reason model | FAIL | P2 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Missing. Bằng chứng: No source/test. Cần làm: Persist transition events and reviewer notes, expose safe owner history, send in-app/email notifications and add retry/audit evidence.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-010 | Property and Subscription | Imported-property claim request | Khách hàng / Chủ cơ sở / Quản trị | Hotel detail claim modal; `/admin/property-claims` | `ClientApiService.submitPropertyClaim()`; direct admin HTTP | `POST /api/properties/{id}/claim`; `PropertyClaimService.requestClaim()` | `POST /api/properties/{id}/claim`; `PropertyClaimService.requestClaim()` | `property_claim_requests` | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Authenticated requester; admin view/approve permission. Bằng chứng: Claim service quota test passed; controller tests did not start. Cần làm: Validate method/data/note length and allowed methods, add evidence-file handling if approved, return DTOs, rate-limit requests and execute requester/admin HTTP tests.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-011 | Property and Subscription | Claim approval and owner activation | Khách hàng / Chủ cơ sở / Quản trị | Admin claim table approve action | Direct admin HTTP | `PropertyClaimService.approveClaim()` creates owner mapping but does not grant `PROPERTY_OWNER`; sets `approval_status` to undocumented `ACTIVE` and leaves operation state inconsistent | `PropertyClaimService.approveClaim()` creates owner mapping but does not grant `PROPERTY_OWNER`; sets `approval_status` to undocumented `ACTIVE` and leaves operation state inconsistent | Claim, hotel and user-property mutation in one transaction | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: `PROPERTY_CLAIM_APPROVE` or `SUPER_ADMIN`. Bằng chứng: Quota rollback unit passed; no successful lifecycle/integration test. Cần làm: Apply the canonical approval state machine, grant the approved owner role, deactivate conflicting ownership where policy requires, record audit evidence and test login/navigation after approval.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-012 | Property and Subscription | Claim rejection/cancellation | Khách hàng / Chủ cơ sở / Quản trị | Admin uses browser `prompt`; requester cannot cancel | Direct admin HTTP | `rejectClaim()` does not require `PENDING`; an approved claim can be relabeled rejected while ownership remains | `rejectClaim()` does not require `PENDING`; an approved claim can be relabeled rejected while ownership remains | Claim status/reason only | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Claim approve permission. Bằng chứng: No negative state test. Cần làm: Restrict reject to pending claims, require validated reason, add requester cancellation before review and guarantee no stale ownership/mapping effect.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-013 | Property and Subscription | Claim response privacy and serialization | Khách hàng / Chủ cơ sở / Quản trị | Claim/admin tables consume explicit property/requester summaries | Typed `PropertyClaimResponse` contract | Controller returns `PropertyClaimResponseDTO`; service maps inside the transaction, `User.passwordHash` is write-only and claim JPA relationships are ignored defensively | Controller returns `PropertyClaimResponseDTO`; service maps inside the transaction, `User.passwordHash` is write-only and claim JPA relationships are ignored defensively | Only approved property/user summary fields are serialized; password hashes, roles, subscriptions and tenant relationship graphs are excluded | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Authenticated requester/admin claim permissions. Bằng chứng: Backend claim/service/privacy/controller tests 16/16 and Angular safe-contract test 1/1 passed; evidence: `docs/testing/evidence/007/remediation/T196-claim-response-privacy.md`. Cần làm: Preserve DTO-only responses and add fields only through explicit privacy review.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-014 | Property and Subscription | Claim uniqueness and concurrency | Khách hàng / Chủ cơ sở / Quản trị | No visible duplicate-state recovery | None | Only same-user pending existence is checked; approval has no row lock or unique owner constraint | Only same-user pending existence is checked; approval has no row lock or unique owner constraint | No uniqueness constraint prevents duplicate active owner mappings | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Authenticated/admin. Bằng chứng: One rollback unit; no concurrent request/approve test. Cần làm: Add database uniqueness/invariant constraints, pessimistic or optimistic locking and concurrent duplicate request/approval tests.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-015 | Property and Subscription | Ownership transfer, co-owner, relinquish and removal | Khách hàng / Chủ cơ sở / Quản trị | No route/UI | None | No transfer/invite/accept/remove lifecycle found | No transfer/invite/accept/remove lifecycle found | `user_properties` can represent owners but has no governed transfer history | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Missing. Bằng chứng: No source/test. Cần làm: Define primary/co-owner policy, invitation acceptance, last-owner protection, transfer audit and subscription responsibility before implementation.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-016 | Property and Subscription | Authoritative platform plan catalog and order snapshot | Khách hàng / Chủ cơ sở / Quản trị | `/management/billing` | `PlatformBillingService.getCatalog()` | `/api/platform/subscription-plans`; `SubscriptionOrderService` snapshots plan version/price/duration/features | `/api/platform/subscription-plans`; `SubscriptionOrderService` snapshots plan version/price/duration/features | `subscription_plans`, platform orders and snapshots | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Authenticated owner/representative with property access. Bằng chứng: Platform evidence plus fresh platform service/UI tests. Cần làm: Preserve server-owned snapshots and keep catalog output free of merchant secrets.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-017 | Property and Subscription | Platform purchase, renewal and approved upgrade | Khách hàng / Chủ cơ sở / Quản trị | `/management/billing` and payment panel | Platform billing typed client | Platform order/attempt/callback/application, renewal and upgrade services | Platform order/attempt/callback/application, renewal and upgrade services | Platform order, transaction, contract, entitlement and history tables | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Property owner and context-specific permissions. Bằng chứng: Existing T093-T110 browser/evidence plus fresh platform tests passed. Cần làm: Keep regression coverage when the management entitlement bridge is replaced; completion here is bounded-context-only.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-018 | Property and Subscription | Downgrade and proration policy | Khách hàng / Chủ cơ sở / Quản trị | Billing UI displays truthful policy blocker | `getPolicyAvailability()` | `SubscriptionPolicyService` returns `POLICY_NOT_CONFIGURED` without mutation | `SubscriptionPolicyService` returns `POLICY_NOT_CONFIGURED` without mutation | No downgrade/credit mutation | BLOCKED | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Authenticated owner with property access. Bằng chứng: Policy service 4/4 and prior negative journey evidence. P0-01D xác nhận production vẫn fail-closed bằng `downgradeConfigured=false`, `prorationConfigured=false` và `POLICY_NOT_CONFIGURED`; chỉ đồng bộ hai expectation tiếng Anh đã cũ với UI tiếng Việt. Focused billing PASS 4/4; full frontend non-payment regression PASS, chỉ còn hai payment wording test được defer. Capability vẫn BLOCKED cho đến khi có chính sách downgrade/proration được phê duyệt. Cần làm: Obtain approved versioned downgrade/proration/accounting rules before implementing; keep simulator/error contract and zero-mutation tests.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-019 | Property and Subscription | Legacy current subscription/catalog/features/usage APIs | Khách hàng / Chủ cơ sở / Quản trị | `/admin/plans`, management context and billing current-plan card | `SubscriptionService` | `/api/subscriptions/plans\&#124;me\&#124;me/features\&#124;me/usage`; catalog/feature services | `/api/subscriptions/plans\&#124;me\&#124;me/features\&#124;me/usage`; catalog/feature services | Legacy `account_subscriptions`, plan features and user-wide usage | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Plans public; current state authenticated. Bằng chứng: P0-02B bổ sung mock test-slice cho `OperationalMetrics`; `SubscriptionControllerIntegrationTest` PASS 6/6, catalog/feature/entitlement regression PASS 18/18, full backend tăng lên 812 PASS và giảm từ 18 xuống 12 ERROR. Production API/configuration không đổi. P0-01D focused `SubscriptionBillingComponent` PASS 4/4. Cần làm: Decide migration/compatibility ownership and stop presenting legacy records as authoritative after platform payment.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-020 | Property and Subscription | Entitlement source-of-truth bridge | Khách hàng / Chủ cơ sở / Quản trị | Selected-property billing card and `/api/platform/subscriptions/{targetHotelId}/entitlement` | `PropertySubscriptionEntitlementService` reads platform entitlement first and maintains an idempotent, audited legacy projection for unambiguous legacy scope | Platform entitlement plus unique `legacy_subscription_entitlement_projections` compatibility model | Platform entitlement plus unique `legacy_subscription_entitlement_projections` compatibility model | Owner/property access and platform billing view | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Backend bridge/controller tests 7/7 and Angular billing 4/4; evidence: `docs/testing/evidence/007/remediation/T197-entitlement-source-bridge.md`. Bằng chứng: Chưa có bằng chứng thực thi riêng. Cần làm: Preserve platform-first precedence; mutation gate scope is tracked by PROP-SUB-021.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-021 | Property and Subscription | Per-property plan limits and feature gates | Khách hàng / Chủ cơ sở / Quản trị | Management dashboard, room and room-type mutations | `SubscriptionFeatureService` resolves the selected property's locked platform entitlement and applies target-property counts for room, room-type, bulk-room and image mutations | Platform entitlement/projection plus property-owned operational rows | Platform entitlement/projection plus property-owned operational rows | Authenticated owner/property role | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Backend property/gate suite 22/22 and Angular dashboard 2/2; evidence: `docs/testing/evidence/007/remediation/T198-property-entitlement-gates.md`. Bằng chứng: Chưa có bằng chứng thực thi riêng. Cần làm: Extend the same property-scoped quota contract to staff mutations in PROP-OPS-005.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-022 | Property and Subscription | Subscription expiry and explicit revoke | Khách hàng / Chủ cơ sở / Quản trị | UI can display dates/status but has no lifecycle action | No revoke call | Legacy scheduler scans all rows and is not tested; no platform contract/entitlement expiry or revoke service/endpoint was found | Legacy scheduler scans all rows and is not tested; no platform contract/entitlement expiry or revoke service/endpoint was found | Legacy and platform status fields can remain stale | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Missing/admin policy. Bằng chứng: No scheduler/revoke evidence. Cần làm: Add clock-driven expiration and explicit audited revoke transitions for authoritative platform contract/entitlement/history; retain historical read/export access and test boundary times.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-023 | Property and Subscription | Subscription-refund entitlement effect | Khách hàng / Chủ cơ sở / Quản trị | Platform refund UI/service exists | Platform refund client | Refund service blocks without an approved `PlatformRefundEntitlementPolicy` | Refund service blocks without an approved `PlatformRefundEntitlementPolicy` | Platform refund, contract, entitlement and history | BLOCKED | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Platform refund permissions. Bằng chứng: Existing refund tests prove safe policy blocking. Cần làm: Approve full/partial refund entitlement policy, implement a versioned policy adapter and test exact contract/entitlement/history effects without touching Property Commerce.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-024 | Property and Subscription | Plan and feature administration/versioning | Khách hàng / Chủ cơ sở / Quản trị | `/admin/plans` is read-only and falls back to a `mailto:` upgrade link | Legacy catalog read only | No admin plan/feature create/update/activate/version API found; seed initializers mutate catalog | No admin plan/feature create/update/activate/version API found; seed initializers mutate catalog | `subscription_plans`, `subscription_features`, `plan_features` | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Missing plan-admin mutation permission. Bằng chứng: No CRUD/version test. Cần làm: Add governed versioned plan administration, prevent edits from changing existing order snapshots/contracts, validate limits/prices and audit activation/deactivation.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-SUB-025 | Property and Subscription | Claim/subscription HTTP test harness | Khách hàng / Chủ cơ sở / Quản trị | Not user-facing | Angular focused suite executes | Both MVC slices pin `BackendApplication`; the JPA tenant interceptor is isolated while real security filters and controller advice execute | Both MVC slices pin `BackendApplication`; the JPA tenant interceptor is isolated while real security filters and controller advice execute | MVC slice plus focused Mockito service lifecycle tests | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Test-only. Bằng chứng: Fresh Maven runs passed 13/13 HTTP and 3/3 service assertions; evidence: `docs/testing/evidence/007/remediation/T185-claim-subscription-http.md`. Cần làm: Preserve the pinned context and tenant-interceptor isolation; claim privacy/state-machine gaps remain tracked by PROP-SUB-010 through PROP-SUB-013, while entitlement ownership remains PROP-SUB-019 through PROP-SUB-021.. Nguồn: [property-subscription.md](inventory/property-subscription.md) |
| PROP-OPS-001 | Property Operations | Staff list and tenant-scoped read | Chủ cơ sở / Quản lý / Nhân viên | `/admin/users`; `UserManagement` | `UserService.getUsers()` | `GET /api/users`; `UserService.getAllUsers()` uses accessible property ids for non-system users | `GET /api/users`; `UserService.getAllUsers()` uses accessible property ids for non-system users | `users`, `user_properties`, roles | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Có | Quyền: `USER:VIEW`. Bằng chứng: Staff service scope plus HTTP super-admin allow and ordinary-user forbid assertions passed; multi-property staff-field privacy remains untested. Cần làm: Test multi-property and cross-property reads, return only staff fields required by the screen and stop loading property options from the public hotel search.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-002 | Property Operations | Staff creation and property assignment | Chủ cơ sở / Quản lý / Nhân viên | Staff create dialog | `UserService.createUser()` | `POST /api/users`; validates accessible property, blocks privileged roles and checks `MAX_STAFF` | `POST /api/users`; validates accessible property, blocks privileged roles and checks `MAX_STAFF` | Creates `users`, roles and active `user_properties` staff mapping | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: `USER:CREATE`. Bằng chứng: Four staff creation/quota/scope unit cases passed. Cần làm: Add validated DTOs, explicit password policy/invitation flow, allowed staff-role allowlist, duplicate normalization, transactional HTTP tests and remove the hard-coded fallback password.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-003 | Property Operations | Staff update, role change and property move | Chủ cơ sở / Quản lý / Nhân viên | Staff edit dialog | `UserService.updateUser()` | `PUT /api/users/{id}`; blocks self/privileged/cross-property mutation and moves active staff mapping | `PUT /api/users/{id}`; blocks self/privileged/cross-property mutation and moves active staff mapping | Updates user and staff mappings | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: `USER:UPDATE`. Bằng chứng: Property-move unit case passed; no HTTP/concurrency test. Cần làm: Validate state/roles/fields, lock concurrent moves, preserve assignment history, use property-scoped entitlement and add positive/IDOR/rollback tests.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-004 | Property Operations | Staff deactivate/remove lifecycle | Chủ cơ sở / Quản lý / Nhân viên | Staff management table with deactivate/rehire reason dialog | `UserService.deactivateStaff()` / `UserService.reactivateStaff()` | Property-scoped assignment lifecycle records reason, actor and timestamps; account becomes inactive when no active assignment remains; JWT reload rejects it | Property-scoped assignment lifecycle records reason, actor and timestamps; account becomes inactive when no active assignment remains; JWT reload rejects it | `user_properties` rows remain historical; additive lifecycle metadata and actor reference | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: `USER:DELETE` / `USER:UPDATE`. Bằng chứng: Backend lifecycle/security suite passed; Angular lifecycle suite 2/2; evidence: `docs/testing/evidence/007/remediation/T199-staff-lifecycle.md`. Cần làm: Extend property-scoped `MAX_STAFF` count-plus-insert enforcement in PROP-OPS-005.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-005 | Property Operations | Staff quota and entitlement scope | Chủ cơ sở / Quản lý / Nhân viên | Limit shown in management context/dashboard | Management API | `UserService` locks the target property's entitlement, counts active `STAFF` assignments for that property, then applies `MAX_STAFF` before insert/rehire; selected-property staff usage is returned to the dashboard | `UserService` locks the target property's entitlement, counts active `STAFF` assignments for that property, then applies `MAX_STAFF` before insert/rehire; selected-property staff usage is returned to the dashboard | Property-scoped platform entitlement and active `user_properties` rows | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: Owner/manager mutation. Bằng chứng: `UserServiceTest` quota isolation/order cases, `ManagementPortalServiceTest` selected-property usage assertion, frontend dashboard suite 2/2; evidence: `docs/testing/evidence/007/remediation/T200-staff-quota-scope.md`. Cần làm: Preserve entitlement lock ordering and add database-backed concurrent HTTP evidence when the production test harness is extended.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-006 | Property Operations | Custom role catalog CRUD | Chủ cơ sở / Quản lý / Nhân viên | `/admin/roles` | `RoleService` | `/api/roles`; normalized unique codes and soft-deactivation for custom roles | `/api/roles`; normalized unique codes and soft-deactivation for custom roles | Global `roles` plus user counts | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Có | Quyền: `ROLE` action masks. Bằng chứng: Source inspection; no focused backend or Angular role test. Cần làm: Add validated requests, HTTP/UI tests, assignment/deactivation invariants and an explicit decision whether roles are system-global or property-owned templates.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-007 | Property Operations | System-role integrity | Chủ cơ sở / Quản lý / Nhân viên | Role UI hides delete for `systemRole` but still permits edit | `RoleService.updateRole()` | Delete protects known system codes; update protects only code changes and can still alter name, description and status | Delete protects known system codes; update protects only code changes and can still alter name, description and status | Global system-role rows | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: `ROLE:UPDATE` / `ROLE:DELETE`. Bằng chứng: Source-only. Cần làm: Make immutable fields and active status explicit for every system role, enforce server-side regardless of stale flags and test seeded-role tampering.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-008 | Property Operations | Permission tree and role permission mutation | Chủ cơ sở / Quản lý / Nhân viên | `/admin/role-permissions` | `RolePermissionService` | Governed system roles are immutable; payloads reject null/duplicate/unknown entries and unsupported action bits; role version is checked optimistically and every accepted mutation writes an append-only audit snapshot | Governed system roles are immutable; payloads reject null/duplicate/unknown entries and unsupported action bits; role version is checked optimistically and every accepted mutation writes an append-only audit snapshot | `app_role`, `app_role_permission`, `app_role_permission_audit`, functions and modules | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Có | Quyền: `ROLE_PERMISSION:VIEW/UPDATE`. Bằng chứng: Service governance 5/5, Spring/H2 mapping 3/3, frontend build passed; evidence: `docs/testing/evidence/007/remediation/T201-role-permission-governance.md`. Cần làm: Preserve the expected-version contract and add HTTP 409/concurrent database evidence in the next security harness pass.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-009 | Property Operations | Role and staff mutation audit/concurrency | Chủ cơ sở / Quản lý / Nhân viên | No audit/history UI | None | No immutable role, permission or staff-assignment mutation audit/version contract found | No immutable role, permission or staff-assignment mutation audit/version contract found | Only generic created/updated metadata | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Missing. Bằng chứng: No source/test. Cần làm: Add actor/reason/before-after audit events, version checks and concurrent role/assignment mutation tests with a safe admin history view.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-010 | Property Operations | Property administration CRUD | Chủ cơ sở / Quản lý / Nhân viên | `/admin/properties`; owner management dashboard | `PropertyService`; `ManagementApiService` | Admin create accepts an entity directly; update copies only a subset; delete is physical; owner portal can create but has no property edit endpoint | Admin create accepts an entity directly; update copies only a subset; delete is physical; owner portal can create but has no property edit endpoint | `hotels` and cascading property images/assignments | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Admin endpoints require `SUPER_ADMIN`; route lacks a frontend guard. Bằng chứng: Property draft payload tests 2/2 passed; no backend lifecycle CRUD test. Cần làm: Use validated DTOs and a state-aware service, add owner-scoped edit for allowed draft/active fields, replace hard delete with closure/retention and add audit/rollback tests.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-011 | Property Operations | Property profile field completeness | Chủ cơ sở / Quản lý / Nhân viên | Admin create form and `/management/properties` create | Property and management clients | Management create persists bilingual text, contact, coordinates and check-in/out; admin update drops many of these fields | Management create persists bilingual text, contact, coordinates and check-in/out; admin update drops many of these fields | `hotels` has mixed legacy/current fields | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Property access or `SUPER_ADMIN` depending endpoint. Bằng chứng: Draft payload test passed; source-only persistence trace. Cần làm: Define one canonical property profile DTO, validate contact/URL/time/coordinates, support edit/read round trips and remove inconsistent legacy field handling.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-012 | Property Operations | Property gallery management | Chủ cơ sở / Quản lý / Nhân viên | Admin form accepts one `mainImage` URL; no gallery screen | None | No property-image create/reorder/primary/delete API found | No property-image create/reorder/primary/delete API found | `property_images` exists with primary, bilingual alt text and order | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Missing. Bằng chứng: Seed/search reads only. Cần làm: Add tenant-safe gallery upload/link/reorder/primary/delete endpoints and UI, enforce image quota atomically and test orphan cleanup and cross-property IDOR.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-013 | Property Operations | Image validation, ownership and storage lifecycle | Chủ cơ sở / Quản lý / Nhân viên | Room-type URL textarea; room create DTO URLs; generic authenticated upload | Inventory clients and avatar upload client | Room/room-type services accept arbitrary URL strings; generic upload has no property asset ownership; replaced/deleted assets are not reclaimed | Room/room-type services accept arbitrary URL strings; generic upload has no property asset ownership; replaced/deleted assets are not reclaimed | Filesystem plus property/room-type/room image rows | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Mixed authenticated and inventory permissions. Bằng chứng: Quota denial units passed; no scheme/signature/ownership tests. Cần làm: Introduce property-owned media records, allow approved URL/storage schemes only, verify file signatures/dimensions, require alt text/order rules and garbage-collect replaced assets safely.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-014 | Property Operations | Amenities catalog and property/room amenity assignment | Chủ cơ sở / Quản lý / Nhân viên | Search card can render amenities; customer room-search uses hard-coded filters | Public model has `amenities?: string[]` | No amenity entity/controller/service found; search response always sets an empty list | No amenity entity/controller/service found; search response always sets an empty list | Missing | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Missing. Bằng chứng: Source trace. Cần làm: Add localized amenity catalog, property and room-type assignments, admin/owner management, indexed search filtering and end-to-end display/filter tests.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-015 | Property Operations | Operational policies and house rules | Chủ cơ sở / Quản lý / Nhân viên | No management screen | None | Hotel stores only free-form check-in/check-out strings; no cancellation, child, pet, smoking, deposit-at-property or house-rule model/API | Hotel stores only free-form check-in/check-out strings; no cancellation, child, pet, smoking, deposit-at-property or house-rule model/API | No versioned property-policy tables | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Missing. Bằng chứng: Source trace. Cần làm: Define versioned localized policy models with effective dates, validation and booking snapshots; add owner UI and customer-visible details before policy-dependent booking changes.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-016 | Property Operations | Room-type CRUD and soft disable | Chủ cơ sở / Quản lý / Nhân viên | `/admin/room-types`, `/management/room-types` | Admin inventory and management clients | Tenant checks, property-local code uniqueness, numeric validation, quota checks and `INACTIVE` soft delete | Tenant checks, property-local code uniqueness, numeric validation, quota checks and `INACTIVE` soft delete | `room_types` | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Direct API uses `ROOM_TYPE` actions; management API uses role-only gate. Bằng chứng: Two quota/image unit cases passed; Angular component creation only. Cần làm: Add complete CRUD/UI behavior tests, database uniqueness, active-booking disable rules, per-property entitlement and permission parity for management routes.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-017 | Property Operations | Room-type image replacement | Chủ cơ sở / Quản lý / Nhân viên | Admin room-type editor accepts newline-separated URLs | Admin inventory client | Update replaces all room-type images and checks aggregate image quota | Update replaces all room-type images and checks aggregate image quota | `room_type_images` | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: `ROOM_TYPE:CREATE/UPDATE` on direct API. Bằng chứng: Image-quota rejection unit passed. Cần làm: Integrate owned uploads, validate URLs/alt text/order/primary image, make replacement concurrency-safe and prove failed replacement leaves the old gallery intact.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-018 | Property Operations | Physical room CRUD and bulk creation | Chủ cơ sở / Quản lý / Nhân viên | `/admin/rooms`, `/management/rooms` | Admin inventory and management clients | Property/type ownership checks, property-local room number check, bulk cap and soft out-of-service delete | Property/type ownership checks, property-local room number check, bulk cap and soft out-of-service delete | `rooms` | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Direct API uses `ROOM` actions; management API uses role-only gate. Bằng chứng: Three quota/scope units and shallow Angular creation test passed. Cần làm: Add DTO validation, database unique `(hotel_id, room_number)`, transactional bulk semantics, booking-conflict rules and full CRUD/IDOR/concurrency tests.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-019 | Property Operations | Physical-room image lifecycle | Chủ cơ sở / Quản lý / Nhân viên | No room image editor | DTO may carry images only on create | Create writes images; update explicitly does not update them and no separate endpoint exists | Create writes images; update explicitly does not update them and no separate endpoint exists | `room_images` | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: `ROOM:CREATE` only in reachable flow. Bằng chứng: Source-only. Cần làm: Add tenant-safe room image add/reorder/primary/delete or remove unsupported DTO fields; enforce owned media/quota and add update/rollback tests.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-020 | Property Operations | Authoritative room-state transitions | Chủ cơ sở / Quản lý / Nhân viên | Admin and management room inventory with dedicated maintenance commands | Admin inventory and management inventory clients | `RoomStatePolicy` is the authoritative transition boundary; locked reservation/check-in/checkout/housekeeping flows and metadata-only generic updates prevent arbitrary state combinations | `RoomStatePolicy` is the authoritative transition boundary; locked reservation/check-in/checkout/housekeeping flows and metadata-only generic updates prevent arbitrary state combinations | `rooms.version`, canonical room/housekeeping/maintenance columns, V37 checks | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: `ROOM:UPDATE` plus server-side property access. Bằng chứng: 35 backend focused tests, frontend build and focused UI tests, SQL Server lifecycle 2/2, and V37 migration validation; [T202 evidence](../../testing/evidence/007/remediation/T202-authoritative-room-state.md). Cần làm: T021 remains the maintenance work-order workflow; T207 remains broader assignment uniqueness/concurrency.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-021 | Property Operations | Maintenance workflow | Chủ cơ sở / Quản lý / Nhân viên | Admin room table toggles maintenance on/off through generic room update | Admin inventory client | No maintenance work order, reason, assignee, schedule, completion evidence or conflict policy | No maintenance work order, reason, assignee, schedule, completion evidence or conflict policy | Only `rooms.maintenance_status` and note | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: `ROOM:UPDATE`. Bằng chứng: No behavior test. Cần làm: Replace the toggle with reasoned maintenance work orders, booking-impact checks, assignment/history and explicit start/complete/reopen transitions.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-022 | Property Operations | Checkout creates dirty room and one housekeeping task | Chủ cơ sở / Quản lý / Nhân viên | Checkout flow | Property checkout client | `CheckoutOperationsService` applies dirty-room/task effects with a tenant-scoped idempotency key | `CheckoutOperationsService` applies dirty-room/task effects with a tenant-scoped idempotency key | `rooms`, `housekeeping_tasks`; unique checkout effect migration | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Checkout permissions. Bằng chứng: Fresh `CheckoutOperationsServiceTest` 6/6 passed plus prior T080 evidence. Cần làm: Preserve exactly-once and rollback coverage while implementing the missing work-queue lifecycle.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-023 | Property Operations | Housekeeping queue, assignment and start | Chủ cơ sở / Quản lý / Nhân viên | Responsive `/management/housekeeping` queue with loading/error/empty/stale states | `HousekeepingService` list/assignee/claim/assign/start client | Property-scoped APIs enforce ownership, self-assignment rules, stale takeover and optimistic version conflicts | Property-scoped APIs enforce ownership, self-assignment rules, stale takeover and optimistic version conflicts | V54 assignment/start/version evidence | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Dedicated `HOUSEKEEPING` role/function; management context admits the role. Bằng chứng: Backend 6/6 and Angular 4/4; [T311 evidence](../../testing/evidence/007/remediation/T311-housekeeping-queue.md). Cần làm: Preserve tenant/stale/version policy when extending completion/release.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-024 | Property Operations | Housekeeping completion and room release | Chủ cơ sở / Quản lý / Nhân viên | `/management/housekeeping` exposes assignee/permission-aware completion and release result | `HousekeepingService.complete()` sends `expectedVersion` | Canonical completion locks task/room, requires assigned active housekeeping operator, rejects stale commands, replays terminal results and preserves maintenance blocks | Canonical completion locks task/room, requires assigned active housekeeping operator, rejects stale commands, replays terminal results and preserves maintenance blocks | V56 `HOUSEKEEPING:APPROVE`; task version/completion and room clean/availability state | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Dedicated approve, tenant ownership and assignee-only execution. Bằng chứng: Backend 17/17 and Angular 7/7; [T312 evidence](../../testing/evidence/007/remediation/T312-housekeeping-completion.md). Cần làm: Preserve expected-version, terminal replay and maintenance ownership.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-025 | Property Operations | Property service catalog tenant isolation | Chủ cơ sở / Quản lý / Nhân viên | `/admin/services` property selector | Property-aware `HotelServiceService` and admin service catalog | Server-authorized property resolution, tenant-owned rows, explicit scoped queries, cross-property not-found behavior and immutable system templates | Server-authorized property resolution, tenant-owned rows, explicit scoped queries, cross-property not-found behavior and immutable system templates | V39 ownership check plus filtered tenant/system code uniqueness | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: `HOTEL` actions remain until T264. Bằng chứng: Backend 6/6, frontend 3/3 and disposable SQL Server validation pass; [T203 evidence](../../testing/evidence/007/remediation/T203-property-service-tenant-isolation.md). Cần làm: T264 permission parity, T265 CRUD UI and T266 validation/history remain separate.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-026 | Property Operations | Service route/API authorization parity | Chủ cơ sở / Quản lý / Nhân viên | `/admin/services` route requires `HOTEL_SERVICE` | Hotel service client | Every `/api/services` endpoint checks `FunctionCode.HOTEL` instead of `HOTEL_SERVICE` | Every `/api/services` endpoint checks `FunctionCode.HOTEL` instead of `HOTEL_SERVICE` | Role permission mappings use the migrated `HOTEL_SERVICE` code | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: UI and API disagree. Bằng chứng: Source trace; no HTTP test. Cần làm: Align on `HOTEL_SERVICE`, add action-specific UI controls and endpoint tests proving matching allow/deny decisions.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-027 | Property Operations | Service create/edit/deactivate UI | Chủ cơ sở / Quản lý / Nhân viên | Service table displays add/edit/delete buttons | Client has CRUD methods | Buttons have no click handlers, dialog or form; only list/reload works | Buttons have no click handlers, dialog or form; only list/reload works | Service rows readable | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: No UI action checks. Bằng chứng: Angular test only creates the component. Cần làm: Build validated localized CRUD UI with permission-aware controls, loading/error states and behavior tests after the tenant-safe backend is fixed.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-028 | Property Operations | Service validation, pricing status and history | Chủ cơ sở / Quản lý / Nhân viên | Table renders price/status | CRUD client | Backend accepts unvalidated code/name/price/status and physically deletes rows used by charge snapshots | Backend accepts unvalidated code/name/price/status and physically deletes rows used by charge snapshots | `services`; charge lines retain snapshots but catalog history is absent | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: `HOTEL` actions currently. Bằng chứng: Global CRUD unit tests pass. Cần làm: Validate positive integer VND and enums, use soft deactivation/versioning, block unsafe deletion and test historical folio/invoice stability.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-029 | Property Operations | Management portal action-level authorization | Chủ cơ sở / Quản lý / Nhân viên | `/management/properties`, `/management/room-types`, `/management/rooms` | `ManagementApiService` | Controller uses one broad role check and no `@Permission` per read/create/update/housekeeping action | Controller uses one broad role check and no `@Permission` per read/create/update/housekeeping action | Property access is checked inside services, but action masks are bypassed | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Any owner/admin/manager role admitted to all routes. Bằng chứng: Security architecture, feature-gate and tenant/IDOR suites pass, but the tests confirm only broad role/feature protection rather than action-mask parity. Cần làm: Apply the same action permissions as direct APIs, add frontend guards/control visibility and execute a role/action/tenant matrix for every endpoint.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-030 | Property Operations | Operations HTTP, tenant and feature-gate test harness | Chủ cơ sở / Quản lý / Nhân viên | Not user-facing | Angular focused suite executes | Full integration suites pin `BackendApplication`; a non-secret property-payment key is test-scoped and the production refund orchestrator constructor is injectable | Full integration suites pin `BackendApplication`; a non-secret property-payment key is test-scoped and the production refund orchestrator constructor is injectable | H2 `create-drop`; Flyway disabled | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Test-only. Bằng chứng: Fresh Maven run passed 17/17 permission, IDOR, tenant-filter, feature-gate and architecture assertions; evidence: `docs/testing/evidence/007/remediation/T186-operations-http-tenant-feature-gate.md`. Cần làm: Preserve the pinned context and test-only key; capability defects found by the suite remain tracked in PROP-OPS-001 through PROP-OPS-029.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PROP-OPS-031 | Property Operations | Property operations dashboard | Chủ cơ sở / Quản lý / Nhân viên | `/management/dashboard` | `ManagementApiService.context()` | Returns per-selected-property room/housekeeping counts but combines them with user-wide legacy plan/usage data | Returns per-selected-property room/housekeeping counts but combines them with user-wide legacy plan/usage data | Room/task counts by property; subscription/usage across accessible properties | PARTIAL | P2 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Broad management role. Bằng chứng: Dashboard render test passed. Cần làm: Use authoritative per-property entitlement/usage, expose timestamp/error semantics and add count reconciliation plus property-switch isolation tests.. Nguồn: [property-operations.md](inventory/property-operations.md) |
| PUB-001 | Public Booking | Home destination/date/guest/property-type search | Khách vãng lai / Khách hàng | `/`; hero and sticky search | `HomeSearchStateService`, `ClientApiService` | Search state serializes to `/api/public/properties/search` | Search state serializes to `/api/public/properties/search` | None before submit | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Có | Quyền: Public. Bằng chứng: Date dialog did not hang; local-date/state tests passed; live API unavailable. Cần làm: Add a successful API-backed browser journey, retain entered state through login/detail/back navigation, and prove validation messages, keyboard focus and mobile behavior.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-002 | Public Booking | Day-use search and booking | Khách vãng lai / Khách hàng | Home displays day use as coming soon, while state code can still serialize `stayType=DAY_USE` without checkout | `HomeSearchStateService` | `PropertySearchRequestDTO.stayType` is ignored; search rejects a check-in without checkout; reservation requires an overnight range | `PropertySearchRequestDTO.stayType` is ignored; search rejects a check-in without checkout; reservation requires an overnight range | Reservation dates have no day-use contract | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Có | Quyền: Public. Bằng chứng: State unit tests pass only preparatory serialization; no backend path. Cần làm: Either remove unreachable day-use request generation or define day-use hours, inventory overlap, capacity, price, checkout and booking contracts before enabling it.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-003 | Public Booking | Grouped autocomplete for province, ward, property and landmark | Khách vãng lai / Khách hàng | Home location dialog | `getSearchSuggestions()` with debounce, cancellation, retry and keyboard navigation | `GET /api/public/search/suggestions`; `PublicSearchSuggestionService` | `GET /api/public/search/suggestions`; `PublicSearchSuggestionService` | `locations`, `hotels`, `property_images` | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: Public. Bằng chứng: Live error state rendered; HTTP tests did not start. Cần làm: Restore the HTTP harness/API runtime, add ordering/deduplication/limit tests, verify keyboard selection and execute accent/no-accent, property and landmark browser journeys.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-004 | Public Booking | Current 34-province, ward and landmark compatibility | Khách vãng lai / Khách hàng | Autocomplete and landmark search URL | Home search state | `LocationController`, `ProvinceCompatibilityService`, landmark validation/radius | `LocationController`, `ProvinceCompatibilityService`, landmark validation/radius | `locations`, province compatibility data and landmark imports | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: Public. Bằng chứng: URL restoration units passed; province/landmark HTTP tests did not start. Cần làm: Execute imported-data integrity and HTTP/browser tests for all current provinces, legacy aliases, duplicate landmark names, invalid coordinates and radius/province mismatch.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-005 | Public Booking | Popular destinations and featured properties | Khách vãng lai / Khách hàng | Home discovery sections | `getPopularDestinations()` and featured-property search | Public discovery and property search APIs | Public discovery and property search APIs | Current locations and approved properties | PARTIAL | P2 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: Public. Bằng chứng: Angular empty/error states passed; live sections showed connection errors. Cần làm: Add a successful seeded browser run, deterministic ranking, image provenance/fallback checks and cache invalidation or freshness rules.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-006 | Public Booking | Primary public property search and eligibility | Khách vãng lai / Khách hàng | `/search`; `GET /api/public/properties/search` | `searchHotels()` | `PropertySearchServiceImpl` filters approved and operationally active properties and production demo data | `PropertySearchServiceImpl` filters approved and operationally active properties and production demo data | `hotels`, `room_types`, `rooms`, reservations and locations | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Có | Quyền: Public. Bằng chứng: Source trace; HTTP test did not start; live API unavailable. Cần làm: Repair the HTTP/runtime path and test approval, suspension, demo visibility, current-province aliases, invalid dates and every accepted query parameter.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-007 | Public Booking | Legacy public hotel search compatibility | Khách vãng lai / Khách hàng | Reachable `GET /api/v1/hotels/public/search`; no current UI consumer | Legacy-compatible client surface | `HotelController.searchHotels()` checks only legacy `status=ACTIVE`, filters some fields in memory and returns `Hotel` entities | `HotelController.searchHotels()` checks only legacy `status=ACTIVE`, filters some fields in memory and returns `Hotel` entities | `hotels`, room types | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Public. Bằng chứng: Source trace. Cần làm: Retire or delegate this endpoint to the canonical search contract; never return entities or expose unapproved/suspended inventory through divergent legacy status rules.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-008 | Public Booking | Property type, star and review filters | Khách vãng lai / Khách hàng | Desktop sidebar and mobile drawer | Search URL state and `SearchFilterSidebarComponent` | Native search applies type, star and minimum review score | Native search applies type, star and minimum review score | Hotel classification/rating fields | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: Public. Bằng chứng: Filter component tests passed; HOTEL URL chip verified in browser; HTTP not executed. Cần làm: Add API result assertions, zero/null rating rules, mobile drawer focus handling and filter-count/chip/pagination regression coverage.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-009 | Public Booking | Amenity, cancellation, pay-at-property and breakfast filters/badges | Khách vãng lai / Khách hàng | No reachable controls or meaningful badges | `amenityIds` is always empty in the sidebar | Request DTO exposes fields, but search ignores them and responses hard-code empty amenities/badges and all policy flags false | Request DTO exposes fields, but search ignores them and responses hard-code empty amenities/badges and all policy flags false | Amenity and versioned booking-policy models are absent | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Có | Quyền: Public. Bằng chứng: Source trace. Cần làm: Implement canonical amenity/policy data and assignments, indexed query predicates, property management, localized display and filter-to-booking snapshot tests.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-010 | Public Booking | Minimum/maximum price filtering | Khách vãng lai / Khách hàng | Price range slider | Search query params | Separate `EXISTS` clauses allow different room types to satisfy min and max, while the displayed cheapest room can fall outside the requested range | Separate `EXISTS` clauses allow different room types to satisfy min and max, while the displayed cheapest room can fall outside the requested range | Active room-type base prices | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: Slider emission unit passed; source confirms SQL defect. Cần làm: Apply both bounds to the same eligible room type used for availability and displayed pricing; add boundary, null-price, multi-room-type and sort consistency tests.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-011 | Public Booking | Sort and one-based pagination | Khách vãng lai / Khách hàng | Sort select and paginator | URL-backed state | `POPULAR`, `PRICE_ASC`, `PRICE_DESC`, `RATING`, `NEAREST`; page is clamped to 1+, size to 1..100 | `POPULAR`, `PRICE_ASC`, `PRICE_DESC`, `RATING`, `NEAREST`; page is clamped to 1+, size to 1..100 | Native query/count query | PARTIAL | P2 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: `sortBy=PRICE_ASC` verified in browser; landmark sort unit passed; no data-backed pagination execution. Cần làm: Add stable tie-breakers to every order, out-of-range page behavior, count parity and browser coverage across filter/sort/page changes.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-012 | Public Booking | Search/detail availability consistency | Khách vãng lai / Khách hàng | Search cards and detail room list | Search and room-type clients | Search SQL excludes only maintenance/out-of-service-style states, while `RoomAvailabilityService` also excludes dirty, cleaning and occupied rooms | Search SQL excludes only maintenance/out-of-service-style states, while `RoomAvailabilityService` also excludes dirty, cleaning and occupied rooms | Physical rooms, reservation details and assignments | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: Source trace; availability HTTP/concurrency suites blocked. Cần làm: Use one authoritative bookable-room predicate/projection for search, detail and reservation creation, then test every room/housekeeping/maintenance state and overlapping reservation status.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-013 | Public Booking | Adult, child and total capacity | Khách vãng lai / Khách hàng | Guest selector, search results and detail room choice | Search params and detail capacity guard | Search and booking validate separate adult/child/total capacity; public room-type detail receives only total guests | Search and booking validate separate adult/child/total capacity; public room-type detail receives only total guests | Room-type capacity fields | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Public/authenticated booking. Bằng chứng: State/checkout units passed; no API capacity matrix. Cần làm: Pass adults, children and quantity to detail availability, normalize null/zero legacy capacities and execute boundary plus mixed-age capacity tests end to end.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-014 | Public Booking | Search and detail price quote | Khách vãng lai / Khách hàng | Search cards, room cards and booking summary | Client displays API pricing but also carries URL estimates | Search chooses the lowest available room type and applies a fixed 15% multiplier; detail returns room totals from the same helper | Search chooses the lowest available room type and applies a fixed 15% multiplier; detail returns room totals from the same helper | Room-type base price only; no versioned public tax/fee/discount policy | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: Source trace and checkout units; no quote-consistency integration test. Cần làm: Define versioned tax/fee/discount/occupancy pricing, return quote identity/expiry, and prove search-detail-checkout totals match or refresh truthfully after price/catalog changes.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-015 | Public Booking | Public property detail eligibility | Khách vãng lai / Khách hàng | `/hotel/:id`; `GET /api/v1/hotels/public/{id}` | `getHotelById()` | `HotelManagementService.getHotelById()` returns any ID without approval/operation/demo eligibility checks | `HotelManagementService.getHotelById()` returns any ID without approval/operation/demo eligibility checks | `hotels`, property images | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Public. Bằng chứng: Invalid/not-found Angular tests passed; source confirms eligibility bypass. Cần làm: Resolve public detail through the canonical eligibility policy and return 404 for draft, rejected, suspended, closed or disallowed demo properties.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-016 | Public Booking | Public room-type listing and sellable status | Khách vãng lai / Khách hàng | Detail room list; `GET /api/public/properties/{hotelId}/room-types` and legacy route | `getRoomTypesByHotel()` | Shared public eligibility policy requires an approved/active property, filters inactive room types, and the locked booking transaction re-checks sellability after `findByIdForUpdate` | Shared public eligibility policy requires an approved/active property, filters inactive room types, and the locked booking transaction re-checks sellability after `findByIdForUpdate` | `room_types`, rooms, reservations | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Public read; authenticated booking. Bằng chứng: HTTP catalog and stale-property tests 7/7, policy/service/reservation tests, deterministic deactivation race; [T204 evidence](../../testing/evidence/007/remediation/T204-public-room-type-sellability.md). Cần làm: Preserve the shared policy; PUB-015/T283 still owns public property detail eligibility and PUB-023/T205 owns booking-request idempotency.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-017 | Public Booking | Complete public property content | Khách vãng lai / Khách hàng | Detail page shows hero, one image, description and rooms | Detail DTO/client | DTO has gallery URLs, but UI lacks gallery, amenities, policies, map, verified review detail and payment/cancellation facts | DTO has gallery URLs, but UI lacks gallery, amenities, policies, map, verified review detail and payment/cancellation facts | Several required models/assignments absent | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Public. Bằng chứng: Source/UI trace. Cần làm: Build a localized accessible gallery and sections for amenities, map/location, policies, verified reviews and booking/payment facts backed by canonical data.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-018 | Public Booking | Detail loading, invalid route, not-found and recovery states | Khách vãng lai / Khách hàng | `/hotel/:id` | Detail component | Public detail and room-type endpoints | Public detail and room-type endpoints | None | PARTIAL | P2 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Public. Bằng chứng: 2/2 detail recovery tests passed; live successful detail unavailable. Cần làm: Add cancellation-safe subscriptions, retry for room errors, stale-query refresh behavior and successful seeded desktop/mobile browser evidence.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-019 | Public Booking | Authentication gate and return to checkout | Khách vãng lai / Khách hàng | `/booking/:roomTypeId` guarded by `clientAuthGuard` | Auth/router services | `POST /api/reservations/book` requires `CUSTOMER` | `POST /api/reservations/book` requires `CUSTOMER` | JWT and user records | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Customer. Bằng chứng: Browser rendered login for an unauthenticated booking route; no completed return journey. Cần làm: Verify encoded return URL, login/register recovery, expired session handling and preservation/revalidation of dates, quantity and room type after authentication.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-020 | Public Booking | Checkout context and client-side tamper resistance | Khách vãng lai / Khách hàng | Booking form | `BookingCheckoutComponent` | Backend ignores URL price/name/hotel authority and reloads the locked room type, capacity and server price | Backend ignores URL price/name/hotel authority and reloads the locked room type, capacity and server price | Room type, hotel, reservation | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Customer. Bằng chứng: Six checkout tests passed, including duplicate-submit suppression and route context. Cần làm: Replace required display authority in URL with a backend quote/context fetch, reject stale/tampered context visibly and add reload/back-navigation browser tests.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-021 | Public Booking | Property-configured payment methods and pay-at-property truth | Khách vãng lai / Khách hàng | Checkout hard-codes pay at hotel, MoMo, VNPay and ZaloPay | Property payment client after reservation creation | Reservation stores caller method; payment attempt validates later; search flags always say pay-at-property false; booking requires enabled property payment configuration even for pay-at-hotel | Reservation stores caller method; payment attempt validates later; search flags always say pay-at-property false; booking requires enabled property payment configuration even for pay-at-hotel | Property payment configuration, reservation and attempts | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Customer. Bằng chứng: Payment-attempt component units pass; source contracts disagree. Cần làm: Fetch allowed/readiness methods before booking, hide disabled methods, define pay-at-property semantics and do not block an approved pay-at-property booking on unrelated online-provider readiness.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-022 | Public Booking | Server-owned booking capacity, price and deposit snapshot | Khách vãng lai / Khách hàng | Checkout submit | `bookRoom()` | `ReservationService` locks room type, validates property/capacity/availability, calculates price and captures deposit policy | `ReservationService` locks room type, validates property/capacity/availability, calculates price and captures deposit policy | Reservation, detail, property configuration and hold | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Customer. Bằng chứng: Reservation service 13/13 passed, including deposit policy; HTTP path not executed. Cần làm: Add validated request DTOs, room-type status validation, API/security/database integration tests and quote/version evidence in the booking response.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-023 | Public Booking | Booking-request idempotency and ambiguous client retry | Khách vãng lai / Khách hàng | Checkout submit and `Idempotency-Key` header | `ReservationController` + `MutationIdempotencyService` + booking checkout | Financial ledger persists a canonical request hash and response; reservation rows retain the scoped booking key so a committed booking can be recovered after a lost response; missing keys are rejected | Financial ledger persists a canonical request hash and response; reservation rows retain the scoped booking key so a committed booking can be recovered after a lost response; missing keys are rejected | Reservations, details and holds | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Customer/public booking. Bằng chứng: Backend persistence/replay/conflict/concurrency suites, Angular shared-tab retry test and Playwright timeout/reload journey; [T205 evidence](../../testing/evidence/007/remediation/T205-booking-idempotency.md). Cần làm: Preserve the mandatory key contract and recovery lookup; T206 still owns inventory hold expiry and overbooking races.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-024 | Public Booking | Overbooking protection and reservation-hold expiry | Khách vãng lai / Khách hàng | Booking and pending-payment inventory | Reservation and payment clients | Room-type locks serialize capacity checks; persisted holds expire through reservation-first locking, payment consumes the same hold, and SQL Server enforces one active hold per reservation | Room-type locks serialize capacity checks; persisted holds expire through reservation-first locking, payment consumes the same hold, and SQL Server enforces one active hold per reservation | Room types, reservation details, holds and payments | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Customer/system scheduler. Bằng chứng: Hold service/scheduler 5/5, H2 booking/expiry/payment-race integration 5/5 and isolated SQL Server locking 2/2 passed; [T206 evidence](../../testing/evidence/007/remediation/T206-overbooking-hold-expiry.md). Cần làm: Preserve room-type then reservation/hold lock ordering, the filtered active-hold uniqueness index and late-payment reconciliation behavior.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-025 | Public Booking | Multiple rooms of one room type | Khách vãng lai / Khách hàng | Detail quantity selector and one reservation detail | Quantity in booking request | Capacity, availability, price and hold quantity support one room type with quantity N | Capacity, availability, price and hold quantity support one room type with quantity N | One reservation detail and one hold quantity | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Customer. Bằng chứng: Source trace; checkout quantity tests are component-level. Cần làm: Add successful and sold-out quantity API/browser tests, assignment completeness checks and atomic rollback when fewer than N physical rooms are allocatable.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-026 | Public Booking | Mixed room-type multi-room cart | Khách vãng lai / Khách hàng | No cart or multi-line booking UI | None | Request accepts one `roomTypeId`; service creates one detail and one hold | Request accepts one `roomTypeId`; service creates one detail and one hold | Model can store multiple details, but creation cannot | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Customer. Bằng chứng: No implementation/test. Cần làm: Define a multi-line quote/cart, atomically lock room types in deterministic order, validate combined guests/prices/holds and support partial-unavailable recovery without partial booking.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-027 | Public Booking | Anonymous public booking identity and ownership | Khách vãng lai / Khách hàng | Reachable `POST /api/reservations/public/book`, but no current UI uses it | None | Creates a new `guest_<millisecond>` user with fake local email and `NOPASSWORD`; no lookup token, ownership credential or duplicate-guest policy | Creates a new `guest_<millisecond>` user with fake local email and `NOPASSWORD`; no lookup token, ownership credential or duplicate-guest policy | `users`, reservations and PII | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Public. Bằng chứng: Source trace. Cần làm: Disable the endpoint or implement a verified guest-booking identity/retrieval model, collision-safe identity, consent/retention rules, rate limits and owner-bound confirmation/access.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-028 | Public Booking | Booking success and payment-attempt retry | Khách vãng lai / Khách hàng | Checkout success panel | Booking component and property payment panel | Reservation is created once in component state; payment attempt uses idempotency and can retry without recreating that in-memory reservation | Reservation is created once in component state; payment attempt uses idempotency and can retry without recreating that in-memory reservation | Reservation and payment attempts | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Customer. Bằng chứng: Checkout/payment-panel units passed; no live provider/API journey. Cần làm: Persist resumable checkout state, recover after reload or redirect, distinguish reservation success from payment success and execute pending/success/failure/expiry browser journeys.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-029 | Public Booking | Public search query performance and result consistency | Khách vãng lai / Khách hàng | Search results | One request from UI | Each page row triggers additional room-type, per-room-type availability and image queries after a native query with multiple correlated subqueries | Each page row triggers additional room-type, per-room-type availability and image queries after a native query with multiple correlated subqueries | Hotels, room types, rooms, images and reservations | PARTIAL | P2 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: Source trace; no performance measurement. Cần làm: Replace N+1 enrichment with bounded projections/batches, index the canonical predicates and measure p95 for representative nationwide data and maximum page size.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| PUB-030 | Public Booking | Public-booking HTTP, concurrency and runnable API evidence | Khách vãng lai / Khách hàng | Not user-facing; supports public discovery/search/booking verification | Angular public flows remain covered separately | Four integration suites pin `BackendApplication` and use deterministic public, booking and simulator-payment fixtures | Four integration suites pin `BackendApplication` and use deterministic public, booking and simulator-payment fixtures | H2 isolated contexts with persisted rooms, reservations, payment policy and holds | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Test/runtime only. Bằng chứng: Fresh Maven run passed 14/14; evidence: `docs/testing/evidence/007/remediation/T187-public-booking-http-concurrency.md`. Cần làm: Preserve the pinned contexts and deterministic fixtures; live browser/API product journeys and SQL Server races remain tracked by their capability rows.. Nguồn: [public-booking.md](inventory/public-booking.md) |
| STAY-001 | Stay Lifecycle | Reservation list, detail and operational status visibility | Khách hàng / Lễ tân / Quản lý | `/admin/reservations`, customer `/profile?tab=bookings`, reservation timeline component | `ReservationService`, `ClientApiService` | `GET /api/reservations`, `/{id}`, `/my-bookings`; DTO includes payment/refund summaries | `GET /api/reservations`, `/{id}`, `/my-bookings`; DTO includes payment/refund summaries | Reservations, details, assignments, payments and refunds | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Có | Quyền: Customer ownership or reservation view/broad operational roles. Bằng chứng: Admin/profile component tests passed; no successful role-based live API journey. Cần làm: Add real seeded customer/receptionist/manager list-detail journeys, stable pagination/filtering, event history and explicit loading/error/empty evidence.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-002 | Stay Lifecycle | Change dates, guests, room quantity or room type after booking | Khách hàng / Lễ tân / Quản lý | No reachable edit/reschedule UI | None | No modification, re-quote or reschedule command/API found | No modification, re-quote or reschedule command/API found | No change request, price-delta or policy snapshot | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Missing. Bằng chứng: No source/test. Cần làm: Define allowed states, availability re-locking, re-quote/price-delta/refund rules and immutable change history before adding customer/staff UI.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-003 | Stay Lifecycle | Customer-owned booking cancellation | Khách hàng / Lễ tân / Quản lý | Customer booking card cancel action | `cancelMyReservation()` | `POST /api/reservations/{id}/cancel`; locks booking, checks owner/state, requests refunds, releases rooms and holds | `POST /api/reservations/{id}/cancel`; locks booking, checks owner/state, requests refunds, releases rooms and holds | Reservation, assignments, holds and refund requests | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: `CUSTOMER` plus resource ownership. Bằng chứng: Four reservation cancellation unit cases and profile UI tests passed. Cần làm: Add reason/confirmation, HTTP/IDOR/concurrency tests, provider-failure recovery and a real browser cancellation-to-refund journey.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-004 | Stay Lifecycle | Cancellation window, fee and snapshotted policy | Khách hàng / Lễ tân / Quản lý | Customer cancellation has no terms/fee preview | None | No versioned cancellation policy, deadline, fee or policy snapshot | No versioned cancellation policy, deadline, fee or policy snapshot | Missing | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Missing. Bằng chứng: Source trace. Cần làm: Introduce versioned property cancellation policy and booking snapshot, calculate refundable/fee amounts server-side and display the decision before confirmation.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-005 | Stay Lifecycle | No-show transition and consequences | Khách hàng / Lễ tân / Quản lý | No no-show action in the reachable admin screen | Generic status client could call `NO_SHOW` directly | Lifecycle allows `CONFIRMED -> NO_SHOW`; generic status endpoint has no arrival-time validation, reason, actor evidence or refund/room-release policy | Lifecycle allows `CONFIRMED -> NO_SHOW`; generic status endpoint has no arrival-time validation, reason, actor evidence or refund/room-release policy | Reservation status and hold release only | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Có | Quyền: Broad operational roles. Bằng chứng: Policy unit test covers the state edge only. Cần làm: Add a reasoned no-show command available only after the approved arrival threshold, define financial/inventory effects, dedicated permission, audit and positive/early/replay tests.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-006 | Stay Lifecycle | Available physical-room lookup for a booking | Khách hàng / Lễ tân / Quản lý | No current room-picker screen | No typed assignment client | `GET /api/reservations/{id}/available-rooms` filters type, date, property and non-bookable room states | `GET /api/reservations/{id}/available-rooms` filters type, date, property and non-bookable room states | Rooms, types, reservations and assignments | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Có | Quyền: Broad operational roles. Bằng chứng: Source trace; no HTTP/UI result matrix. Cần làm: Build the room-picker UI/client and test all room, housekeeping, maintenance and overlapping-assignment states with multi-room quantities.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-007 | Stay Lifecycle | Concurrent-safe physical-room assignment | Khách hàng / Lễ tân / Quản lý | No reachable assignment flow | `ReservationService.assignRooms()` | Reservation, active assignment rows and sorted candidate rooms are pessimistically locked before validation; identical replay returns the existing assignment without a second write | Reservation, active assignment rows and sorted candidate rooms are pessimistically locked before validation; identical replay returns the existing assignment without a second write | `reservation_rooms` stores stay dates; V41 adds room/date lookup and a filtered unique active-assignment guard | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Có | Quyền: Broad operational roles. Bằng chứng: P0-02C cập nhật fixture refund-summary dependency và khôi phục `ReservationLifecycleLockingTest` PASS 4/4; reservation/refund regression PASS 36/36, full backend giảm đúng 3 ERROR. H2 concurrent assignment 1/1 và SQL Server lifecycle 2/2 giữ nguyên. Evidence: `docs/hotel-report/fix-progress/P0-02C.md`, `docs/testing/evidence/007/remediation/T207-physical-room-assignment.md`. Cần làm: Preserve lock ordering, complete constructor fixtures and the active-room/date guard; assignment/reassignment UI remains STAY-008/T301 scope.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-008 | Stay Lifecycle | Assignment, reassignment and release UI | Khách hàng / Lễ tân / Quản lý | Reservation table has no assign/reassign control | None | Duplicate PUT/POST assignment APIs exist; no explicit reassignment/release command | Duplicate PUT/POST assignment APIs exist; no explicit reassignment/release command | Assignment status supports `ASSIGNED`/`RELEASED` | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Có | Quyền: Missing UI; broad backend role gate. Bằng chứng: No UI/test. Cần làm: Add permission-aware assignment UI with availability refresh, conflict recovery and explicit release/reassign history after STAY-007 is fixed.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-009 | Stay Lifecycle | Check-in readiness and execution | Khách hàng / Lễ tân / Quản lý | Admin reservation row exposes check-in directly | Generic `updateReservationStatus()` | `POST /check-in` and generic status update require all physical rooms assigned, then mark them occupied | `POST /check-in` and generic status update require all physical rooms assigned, then mark them occupied | Reservation and assigned rooms | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Có | Quyền: Broad roles rather than a dedicated check-in action. Bằng chứng: P0-02C khôi phục deterministic check-in locking assertion trong focused suite PASS 4/4 và related lifecycle/refund PASS 36/36; capability vẫn FAIL vì normal UI chưa có assignment workflow. Cần làm: Connect check-in to the assignment workflow, show missing-room/readiness errors, validate arrival policy and add dedicated permission plus real API/browser tests.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-010 | Stay Lifecycle | Staff-created/manual reservation | Khách hàng / Lễ tân / Quản lý | `/admin/reservations/create` | `createReservation()` | UI sends `userId` and `details[].roomId`, while `ReservationRequest` requires `roomTypeId`, quantity and authenticated username ownership | UI sends `userId` and `details[].roomId`, while `ReservationRequest` requires `roomTypeId`, quantity and authenticated username ownership | Reservation creation uses room type and current principal | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: `RESERVATION:CREATE`. Bằng chứng: Source contract mismatch; no component/API test. Cần làm: Replace the legacy form with a staff-booking contract that selects customer, quote and room type, uses server price/availability and assigns rooms only through the locked assignment workflow.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-011 | Stay Lifecycle | Action-level lifecycle authorization | Khách hàng / Lễ tân / Quản lý | Reservation actions render only for exact stored masks | Dedicated client commands for check-in, operational cancellation and no-show | Controller uses separate assignment/check-in/cancel/no-show permission functions, generic status rejects sensitive transitions and legacy checkout uses `CHECKOUT:CREATE` | Controller uses separate assignment/check-in/cancel/no-show permission functions, generic status rejects sensitive transitions and legacy checkout uses `CHECKOUT:CREATE` | Property access is checked before lifecycle writes; V42 seeds functions/default masks | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Exact function/action mask plus property not-found semantics. Bằng chứng: Backend action 5/5, IDOR 4/4, regression 15/15 and Angular 3/3; evidence: `docs/testing/evidence/007/remediation/T208-lifecycle-action-authorization.md`. Cần làm: Preserve command-specific masks; assignment UI/history and no-show timing/consequence remain separate tasks.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-012 | Stay Lifecycle | Legacy in-stay service charge | Khách hàng / Lễ tân / Quản lý | Admin reservation list opens the authoritative checkout workspace; no legacy add-service dialog | `PropertyCheckoutService.addServiceCharge()` | `POST /api/management/reservations/{id}/charges/services` creates append-only evidence; legacy `POST /api/reservations/{id}/services` returns `410 Gone` | `POST /api/management/reservations/{id}/charges/services` creates append-only evidence; legacy `POST /api/reservations/{id}/services` returns `410 Gone` | `reservation_charge_lines.legacy_service_item_id` links V43-backfilled rows to `reservation_services`; filtered unique guard prevents duplicate reconciliation | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: `RESERVATION_SERVICE:CREATE` plus property access. Bằng chứng: Backend 30/30, Angular 10/10, SQL Server V43 repeat/backfill 1 row and 150,000 VND; evidence: `docs/testing/evidence/007/remediation/T209-legacy-service-charge.md`. Cần làm: Preserve the append-only legacy link and no-double-count rule; STAY-013 continues tracking HTTP/idempotency and expanded minibar policy coverage.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-013 | Stay Lifecycle | Authoritative service and minibar charges | Khách hàng / Lễ tân / Quản lý | Checkout workspace loads the reservation property's catalog after preview and exposes explicit `SERVICE`/`MINIBAR` usage actions with retry state | `HotelServiceService.getServicesForHotel()` and `PropertyCheckoutService.addServiceCharge()` | `POST /api/management/reservations/{id}/charges/services` locks the reservation as tenant source, requires `Idempotency-Key`, snapshots server price/usage and returns persisted replay metadata | `POST /api/management/reservations/{id}/charges/services` locks the reservation as tenant source, requires `Idempotency-Key`, snapshots server price/usage and returns persisted replay metadata | `reservation_charge_lines`, `financial_idempotency_records`, property/system service catalog snapshots | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: `RESERVATION_SERVICE:CREATE` plus property access; cross-property catalog IDs fail closed. Bằng chứng: Backend 12/12 and Angular 15/15 focused tests passed; evidence: [T304-service-minibar.md](../../testing/evidence/007/remediation/T304-service-minibar.md). Cần làm: Preserve reservation-owned tenant resolution, persisted replay identity and the retired global/legacy catalog path; extend only through reconciled folio consumers.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-014 | Stay Lifecycle | Surcharge and negative adjustment | Khách hàng / Lễ tân / Quản lý | Checkout workspace surcharge form | `addSurcharge()` / `addNegativeAdjustment()` | Typed surcharge and permissioned negative adjustment services | Typed surcharge and permissioned negative adjustment services | Append-only charge lines, persisted idempotency/replay and financial audit events | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: `RESERVATION_SURCHARGE:CREATE`; negative adjustment mask checked in service. Bằng chứng: Backend focused 13/13 and Angular focused 15/15 passed; tenant/concurrent duplicate, localized alias and visible history evidence: [T305-adjustments.md](../../testing/evidence/007/remediation/T305-adjustments.md). Cần làm: Preserve persisted replay identity, permissioned negative adjustments and append-only history when extending folio adjustments.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-015 | Stay Lifecycle | Legacy direct payment mutation | Khách hàng / Lễ tân / Quản lý | Admin reservation management opens the authoritative folio workspace | Property payment attempts and manual confirmation; `PaymentService.processPayment()` removed | `POST /api/payments` returns `410 Gone`; authoritative attempts derive amount/method from server-owned folio and require provider callback or permissioned manual confirmation | `POST /api/payments` returns `410 Gone`; authoritative attempts derive amount/method from server-owned folio and require provider callback or permissioned manual confirmation | Legacy `payments` remain read-only evidence; V44 preserves source reference/status and marks unverified rows for reconciliation | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: `FINANCE:CREATE` is retained only to receive the retirement response; settlement permissions live on property payment commands. Bằng chứng: Backend 44/44, Angular 11/11, SQL Server V44 fixture and retirement matrix; evidence: [T210-legacy-direct-payment.md](../../testing/evidence/007/remediation/T210-legacy-direct-payment.md). Cần làm: Keep legacy rows read-only and resolve quarantined rows through an approved manual reconciliation workflow.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-016 | Stay Lifecycle | Booking financial summary during the stay | Khách hàng / Lễ tân / Quản lý | Checkout/payment/customer booking summaries | Property payment and checkout clients | `BookingFinancialSummaryService` derives charges, payments, refunds, balance and financial state | `BookingFinancialSummaryService` derives charges, payments, refunds, balance and financial state | Authoritative ledger/charges plus legacy compatibility reads | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Resource owner or authorized property role. Bằng chứng: Folio/checkout units pass; no complete API reconciliation against database fixtures. Cần làm: Add one-VND database reconciliation across deposits, multiple payments, refunds, services, surcharges and legacy compatibility rows.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-017 | Stay Lifecycle | Authoritative checkout preview | Khách hàng / Lễ tân / Quản lý | Checkout workspace | `PropertyCheckoutService.preview()` | Recomputes server folio and returns settled/outstanding/overpaid gate | Recomputes server folio and returns settled/outstanding/overpaid gate | Charge lines, ledger, refunds and reservation snapshots | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: `CHECKOUT:VIEW`. Bằng chứng: Preview 5/5, balance 3/3 and Angular request/gate tests passed. Cần làm: Add HTTP/tenant tests and SQL-backed reconciliation with concurrent payment/refund/charge changes between preview and checkout.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-018 | Stay Lifecycle | Debt override and overpayment handling | Khách hàng / Lễ tân / Quản lý | Checkout workspace supports reasoned debt override; overpayment stays blocked | `authorizeDebtOverride()` | Locks/rechecks folio, requires separate permission and records immutable override/audit; no approved overpayment resolution | Locks/rechecks folio, requires separate permission and records immutable override/audit; no approved overpayment resolution | Checkout overrides and audit events | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: `RESERVATION_DEBT_OVERRIDE:APPROVE`. Bằng chứng: Override 7/7 and balance tests 3/3 passed. Cần làm: Add approval policy/limits, HTTP/tenant/concurrency tests and implement an approved overpayment refund/credit policy before enabling that case.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-019 | Stay Lifecycle | Atomic checkout and rollback | Khách hàng / Lễ tân / Quản lý | Checkout workspace | `checkout()` sends only server override id | Reservation service locks booking, finalizes invoice, releases assignments, marks rooms dirty and creates housekeeping tasks in one transaction | Reservation service locks booking, finalizes invoice, releases assignments, marks rooms dirty and creates housekeeping tasks in one transaction | Reservation, invoice/lines/allocations, rooms, assignments and tasks | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: `CHECKOUT:CREATE`. Bằng chứng: Transaction units 5/5, fail-closed ordering 3/3 and SQL Server aggregate rollback/concurrent replay 2/2 passed; evidence: `docs/testing/evidence/007/remediation/T211-atomic-checkout-sqlserver.md`. Cần làm: Preserve the SQL Server constraint-failure and replay suite when checkout persistence or uniqueness contracts change.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-020 | Stay Lifecycle | Final invoice view, PDF and email | Khách hàng / Lễ tân / Quản lý | Customer `/my-invoices` and admin finalized-invoice screens share itemized view, PDF download and print states | Canonical `InvoiceService` client; legacy reservation invoice client is read-only compatibility only | Finalized list/detail/PDF/email endpoints render immutable property/customer snapshots, itemized room/service/minibar/tax/discount/payment/refund totals and deterministic checksum headers | Finalized list/detail/PDF/email endpoints render immutable property/customer snapshots, itemized room/service/minibar/tax/discount/payment/refund totals and deterministic checksum headers | Immutable invoices, lines, allocations and credit notes; legacy generation no longer supplies print data | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Customer owner or property access; email/adjustment remain separately authorized. Bằng chứng: Quiet rerun passed backend 20/20 and Angular 12/12; deterministic PDF and legacy-adapter tests included; evidence: [T309-itemized-invoice.md](../../testing/evidence/007/remediation/T309-itemized-invoice.md). Cần làm: Capture sandbox email/outbox evidence, localization and complete seeded customer/admin browser journeys before promoting T309.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-021 | Stay Lifecycle | Post-finalization credit notes | Khách hàng / Lễ tân / Quản lý | Customer invoice detail displays credit notes; management client can create them | Property checkout/invoice clients | Permissioned credit-note service appends corrections without rewriting invoice | Permissioned credit-note service appends corrections without rewriting invoice | Credit notes and lines | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: `INVOICE_ADJUST:APPROVE`. Bằng chứng: Model/service tests exist; no fresh HTTP/UI creation journey. Cần làm: Add approval/reason limits, tenant/IDOR/concurrency tests and a management creation/history UI with refreshed invoice reconciliation.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-022 | Stay Lifecycle | Checkout creates one dirty-room housekeeping effect | Khách hàng / Lễ tân / Quản lý | Checkout result and management dashboard counts | Checkout client | `CheckoutOperationsService` uses a tenant-scoped checkout effect key, releases assignments, marks rooms dirty and creates at most one task per room | `CheckoutOperationsService` uses a tenant-scoped checkout effect key, releases assignments, marks rooms dirty and creates at most one task per room | Rooms, assignments, housekeeping tasks and unique effect index | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Checkout permission. Bằng chứng: Fresh `CheckoutOperationsServiceTest` 6/6 and intercepted Playwright dirty-room assertion passed. Cần làm: Preserve the exactly-once and rollback assertions while completing the staff queue lifecycle.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-023 | Stay Lifecycle | Housekeeping queue, assignment and start | Khách hàng / Lễ tân / Quản lý | Responsive `/management/housekeeping` queue with loading/error/empty/stale states | `HousekeepingService` list/assignee/claim/assign/start client | Property-scoped APIs enforce ownership, self-assignment rules, stale takeover and optimistic version conflicts | Property-scoped APIs enforce ownership, self-assignment rules, stale takeover and optimistic version conflicts | V54 assignment/start/version evidence | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Dedicated `HOUSEKEEPING` role/function; management context admits the role. Bằng chứng: Backend 6/6 and Angular 4/4; [T311 evidence](../../testing/evidence/007/remediation/T311-housekeeping-queue.md). Cần làm: Preserve tenant/stale/version policy when extending completion and room release.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-024 | Stay Lifecycle | Housekeeping completion and room release | Khách hàng / Lễ tân / Quản lý | `/management/housekeeping` exposes assignee/permission-aware completion and release result | `HousekeepingService.complete()` sends `expectedVersion` | Canonical completion locks task/room, requires assigned active housekeeping operator, rejects stale commands, replays terminal results and preserves maintenance blocks | Canonical completion locks task/room, requires assigned active housekeeping operator, rejects stale commands, replays terminal results and preserves maintenance blocks | V56 `HOUSEKEEPING:APPROVE`; task version/completion and room clean/availability state | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Dedicated approve, tenant ownership and assignee-only execution. Bằng chứng: Backend 17/17 and Angular 7/7; [T312 evidence](../../testing/evidence/007/remediation/T312-housekeeping-completion.md). Cần làm: Preserve expected-version, terminal replay and maintenance ownership.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-025 | Stay Lifecycle | Verified post-stay reviews and rating aggregation | Khách hàng / Lễ tân / Quản lý | Search/detail show imported/static rating values only | None | No review entity, create/moderate/respond API or verified-stay check | No review entity, create/moderate/respond API or verified-stay check | Hotel average/count fields only | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Missing. Bằng chứng: Source trace. Cần làm: Add one-review-per-completed-stay rules, moderation/response, aggregate consistency, abuse controls and localized customer/property UI.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-026 | Stay Lifecycle | Favorites/wishlist | Khách hàng / Lễ tân / Quản lý | Heart controls on public cards/detail plus authenticated `/favorites` loading/error/empty/populated states | `FavoriteService` coalesces list loads, idempotent add/remove and logout reset | `GET/POST/DELETE /api/favorites`; authenticated principal supplies owner ID and non-public properties fail closed | `GET/POST/DELETE /api/favorites`; authenticated principal supplies owner ID and non-public properties fail closed | V53 `customer_favorites` with customer/hotel foreign keys, unique owner-target constraint and lookup indexes | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: `CUSTOMER` authority plus owner-scoped repository queries. Bằng chứng: Backend 7/7, frontend 7/7 and Angular development build passed; evidence: [T314-favorites.md](../../testing/evidence/007/remediation/T314-favorites.md). Cần làm: Preserve owner-scoped queries, customer-only authorization and additive V53 recovery; add full browser/SQL Server evidence only as a broader release gate.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-027 | Stay Lifecycle | Voucher/promotion application and redemption | Khách hàng / Lễ tân / Quản lý | Home has promotional/member cards and a static `PromotionsComponent` that can copy supplied codes | No quote/booking voucher client | No promotion catalog, eligibility, stacking, validation, reservation snapshot or redemption service | No promotion catalog, eligibility, stacking, validation, reservation snapshot or redemption service | Missing | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Missing. Bằng chứng: Display/copy source only; no financial effect. Cần làm: Implement approved promotion/membership/sponsored-placement policies, backend quote consistency, stacking/redemption limits and admin/customer lifecycle UI after product rules are approved.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-028 | Stay Lifecycle | End-to-end stay journey through the UI | Khách hàng / Lễ tân / Quản lý | Admin reservations/checkout plus customer invoices | Reservation, payment-attempt, checkout and invoice clients | All expected APIs can be represented by the browser fixture | All expected APIs can be represented by the browser fixture | Browser state only in this test | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Mocked role/session. Bằng chứng: Playwright 1/1 passed with all `/api/**` calls intercepted. Cần làm: Run the same journey against the real backend and deterministic SQL Server fixture, including assignment, negative permission, timeout, retry and rollback cases.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| STAY-029 | Stay Lifecycle | Stay lifecycle concurrency and executable database evidence | Khách hàng / Lễ tân / Quản lý | Not user-facing | Unit/component suites execute | Assignment locks the reservation, active assignment rows and candidate rooms in deterministic order; check-in locks assigned rows and rooms; checkout keeps locked replay-safe housekeeping effects | Assignment locks the reservation, active assignment rows and candidate rooms in deterministic order; check-in locks assigned rows and rooms; checkout keeps locked replay-safe housekeeping effects | `reservation_rooms`, `rooms`, `housekeeping_tasks` with real SQL Server transaction boundaries | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Test-only. Bằng chứng: P0-02C chứng minh production locking đúng, fixture Mockito cũ thiếu `PropertyRefundRequestRepository`; sau sửa locking unit PASS 4/4, related reservation/refund PASS 36/36, P0 guards PASS và full backend đạt 815 PASS/9 ERROR. H2 persistence rollback/replay 2/2 và isolated SQL Server lifecycle 2/2 giữ nguyên; evidence: `docs/hotel-report/fix-progress/P0-02C.md`, `docs/testing/evidence/007/remediation/T192-stay-lifecycle-sqlserver.md`. Cần làm: Preserve complete `ReservationService` fixtures and the SQL Server lifecycle harness; full invoice-and-operations rollback remains covered separately by T211/STAY-019.. Nguồn: [stay-lifecycle.md](inventory/stay-lifecycle.md) |
| CROSS-001 | Cross-Cutting | Admin notification history and mark-read | Quản trị / Hệ thống / Tác vụ nền | Admin header history supports load-more, dedupe, retained unread count and recoverable mark errors | Typed paged notification client | Deterministic retained owner/system paging, clamped size, stable missing/expired `NOT_FOUND`, foreign `ACCESS_DENIED` without mutation | Deterministic retained owner/system paging, clamped size, stable missing/expired `NOT_FOUND`, foreign `ACCESS_DENIED` without mutation | Existing notifications plus configurable visibility window | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: `REPORT:VIEW`; admin/staff ownership. Bằng chứng: Backend 14/14 and Angular 9/9; [T317 evidence](../../testing/evidence/007/remediation/T317-notification-history.md). Cần làm: Preserve pagination/retention/ownership; realtime reconnect remains CROSS-002.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-002 | Cross-Cutting | Realtime notification WebSocket security | Quản trị / Hệ thống / Tác vụ nền | Admin header connects to `/ws` | STOMP client subscribes to admin topic and personal queue with bearer header | Notification handshake/channel interceptor authenticates JWT, restricts destinations and blocks client publish | Notification handshake/channel interceptor authenticates JWT, restricts destinations and blocks client publish | STOMP/SockJS | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Personal queue or `REPORT:VIEW` for admin topic. Bằng chứng: Channel interceptor 6/6 and embedded-server admin subscription 1/1 passed. Cần làm: Add expired-token and reconnect/replay integration cases plus visible reconnect/offline recovery in the admin shell.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-003 | Cross-Cutting | Customer personal notification inbox | Quản trị / Hệ thống / Tác vụ nền | No customer notification center or badge | Client layout never connects `NotificationService` | Refund service persists and pushes personal notifications, but REST read requires `REPORT:VIEW` and only admin layout subscribes | Refund service persists and pushes personal notifications, but REST read requires `REPORT:VIEW` and only admin layout subscribes | Personal notification rows can remain unseen | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Customer access path is absent. Bằng chứng: Source trace. Cần làm: Add authenticated own-notification REST/UI/WS consumption, unread count, deep links and tests proving no cross-user access.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-004 | Cross-Cutting | Notification idempotency and delivery reliability | Quản trị / Hệ thống / Tác vụ nền | No delivery status UI | None | `sendUserNotificationOnce()` uses a unique `event_key`, but concurrent insert recovery, durable publish/outbox and reconnect replay are absent | `sendUserNotificationOnce()` uses a unique `event_key`, but concurrent insert recovery, durable publish/outbox and reconnect replay are absent | Unique filtered notification key; transient WebSocket delivery | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Internal event producers. Bằng chứng: Unit test covers equivalent reuse only. Cần làm: Handle uniqueness races idempotently, add transactional outbox/delivery attempts and reconcile persisted unread notifications after reconnect.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-005 | Cross-Cutting | Notification preferences, archive and lifecycle | Quản trị / Hệ thống / Tác vụ nền | No settings or notification page | None | No preference, channel opt-in, archive/delete or retention API | No preference, channel opt-in, archive/delete or retention API | Missing | FAIL | P2 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Missing. Bằng chứng: No source/test. Cần làm: Define mandatory versus optional event classes, per-channel preferences, archive/retention and accessible settings/history UI.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-006 | Cross-Cutting | Registration welcome-email template and safe failure | Quản trị / Hệ thống / Tác vụ nền | Registration success messaging | Registration response exposes whether mail was sent | Escaped HTML template; registration remains successful when mail is disabled/fails | Escaped HTML template; registration remains successful when mail is disabled/fails | JavaMail adapter | PASS | P2 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Public registration trigger. Bằng chứng: Email tests 2/2 and prior registration component evidence passed. Cần làm: Preserve truthful delivery messaging while adding the shared outbox/sandbox evidence in CROSS-009/CROSS-010.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-007 | Cross-Cutting | Booking-confirmation email | Quản trị / Hệ thống / Tác vụ nền | Confirmation stays implicit; delivery state is visible through the operator outbox | Booking transition schedules outbox after commit | Localized HTML/text `v2.vi`/`v2.en` template queues only after commit in a new transaction; rollback queues nothing; failure/retry does not reverse booking | Localized HTML/text `v2.vi`/`v2.en` template queues only after commit in a new transaction; rollback queues nothing; failure/retry does not reverse booking | T324 outbox and append-only attempts | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Operational transition plus outbox audit permissions. Bằng chứng: Backend 11/11; [T322 evidence](../../testing/evidence/007/remediation/T322-booking-confirmation-email.md). Cần làm: Preserve after-commit isolation and bounded retry; sandbox mailbox remains CROSS-010.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-008 | Cross-Cutting | Invoice email with PDF attachment | Quản trị / Hệ thống / Tác vụ nền | Customer invoice screen exposes send action | `InvoiceService.emailInvoice()` | Authorized finalized invoice endpoint generates PDF and calls `EmailService.sendInvoiceEmail()` | Authorized finalized invoice endpoint generates PDF and calls `EmailService.sendInvoiceEmail()` | JavaMail adapter plus immutable invoice | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Invoice owner/property access. Bằng chứng: Invoice access 6/6 and client tests passed; no sandbox inbox evidence. Cần làm: Add outbox/idempotency, recipient verification, attachment checksum, resend limits and sandbox mailbox evidence.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-009 | Cross-Cutting | Email outbox, retry, bounce and delivery audit | Quản trị / Hệ thống / Tác vụ nền | `/admin/email-outbox` failure queue with masked recipients, attempts and retry states | `EmailOutboxService` client | Transactional durable outbox with template/version metadata, idempotent request hash, bounded exponential retry, dead-letter, bounce and manual retry | Transactional durable outbox with template/version metadata, idempotent request hash, bounded exponential retry, dead-letter, bounce and manual retry | V55 `email_outbox` and append-only `email_delivery_attempts` | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: `AUDIT_LOG:VIEW` for queue/audit; `AUDIT_LOG:UPDATE` for retry/bounce; tenant filtering. Bằng chứng: Backend 10/10 and Angular 4/4; [T324 evidence](../../testing/evidence/007/remediation/T324-email-outbox.md). Cần làm: Preserve fail-closed delivery, append-only attempts, masked recipients and tenant filtering; sandbox mailbox evidence remains CROSS-010.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-010 | Cross-Cutting | SMTP sandbox delivery and configuration | Quản trị / Hệ thống / Tác vụ nền | No mailbox evidence | Environment-driven client only in part | JavaMail adapter exists, but no configured non-production inbox was used in this audit | JavaMail adapter exists, but no configured non-production inbox was used in this audit | External SMTP sandbox | BLOCKED | P2 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Secret-managed test credential. Bằng chứng: Internal template tests only. Cần làm: Configure an environment-driven test mailbox, send disposable registration/booking/invoice messages and record rendered content/provider responses without committing secrets.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-011 | Cross-Cutting | Customer in-app support chat and persisted history | Quản trị / Hệ thống / Tác vụ nền | Client chat widget on public layout | `ChatService` customer mode, REST history and STOMP send/receive | Resolves reservation/property ownership, persists tenant-scoped conversation/messages and returns latest history | Resolves reservation/property ownership, persists tenant-scoped conversation/messages and returns latest history | Conversations, messages and events | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Authenticated customer/resource context. Bằng chứng: Full HTTP suite passed customer latest-conversation history and unauthenticated denial; service/UI tests passed; P0-01A revalidated `ChatService` focused 3/3 and the same file passed in the full frontend suite after aligning the `AuthService.logout$` mock and relative `/api` history URL; credential-gated live Playwright skipped. Cần làm: Execute the real two-user browser journey, support multiple conversations/history selection and add message pagination/retention.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-012 | Cross-Cutting | Support queue, claim, reply and escalate | Quản trị / Hệ thống / Tác vụ nền | `/admin/chat` | Support chat client/dashboard | Permissioned tenant queue, locking, claim ownership, reply and escalation | Permissioned tenant queue, locking, claim ownership, reply and escalation | Conversations, messages and events | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: `AI_CHAT:VIEW/CREATE`. Bằng chứng: Full HTTP suite passed tenant list/history, denied audit, assignment and escalation; controller 2/2 and support UI 2/2 passed. Cần làm: Add real WebSocket browser evidence, queue filters/SLA, optimistic conflict recovery and explicit unassign/reopen behavior.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-013 | Cross-Cutting | Support tenant isolation and denied-action audit | Quản trị / Hệ thống / Tác vụ nền | Support dashboard | Same support client | Service restricts accessible hotel ids and records accepted/denied assignment/reply/escalation events | Service restricts accessible hotel ids and records accepted/denied assignment/reply/escalation events | Tenant-filtered conversations/events | PASS | P0 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Property access plus `AI_CHAT` masks. Bằng chứng: H2 isolation integration 2/2 passed with denied-event assertions. Cần làm: Preserve the isolation suite and repeat against SQL Server in the final cross-property regression.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-014 | Cross-Cutting | Chat WebSocket authentication and destination authorization | Quản trị / Hệ thống / Tác vụ nền | Customer/support chat clients | Bearer STOMP connection and mode-specific queues | Channel interceptor restricts send/subscribe destinations and checks `AI_CHAT` masks | Channel interceptor restricts send/subscribe destinations and checks `AI_CHAT` masks | STOMP/SockJS | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Authenticated user; support view/create masks. Bằng chứng: Interceptor 7/7 and embedded-server STOMP 4/4 passed for invalid token, wrong destination, cross-role support subscription and reauthenticated reconnect; evidence: `docs/testing/evidence/007/remediation/T212-chat-websocket-security.md`. P0-01A regression guard passed the focused `ChatService` suite 3/3, including offline publish protection and fresh correlation IDs, and passed again in the full frontend suite. Cần làm: Preserve the embedded-server matrix and rerun it when STOMP endpoints, JWT reload or destination rules change.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-015 | Cross-Cutting | Duplicate-safe chat send, acknowledgement and read state | Quản trị / Hệ thống / Tác vụ nền | Both chat UIs show a timeout then allow retry | Publish returns only local success; echo is treated as acknowledgement | Messages have no client id/idempotency key; retry after timeout can duplicate; no mark-read/read-receipt command found | Messages have no client id/idempotency key; retry after timeout can duplicate; no mark-read/read-receipt command found | Chat messages include `is_read` but no complete lifecycle | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Authenticated chat. Bằng chứng: UI offline/timeout tests pass; P0-01A fixture regression passed 3/3 for offline no-publish, principal-scoped history and fresh correlation IDs; no replay/concurrency test. Cần làm: Add client message ids, persisted idempotency, server acknowledgements, delivery/read states and retry/replay/concurrency tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-016 | Cross-Cutting | Conversation close, attachments, search and SLA | Quản trị / Hệ thống / Tác vụ nền | No close, attachment, search or SLA controls | None | `CLOSED` exists in the model but no close/reopen endpoint; no attachment/virus-scan or queue metrics contract | `CLOSED` exists in the model but no close/reopen endpoint; no attachment/virus-scan or queue metrics contract | Missing supporting records | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Missing/dedicated permissions. Bằng chứng: Source trace. Cần làm: Add reasoned close/reopen, safe attachments, search/filter, SLA timestamps and audit-backed support controls.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-017 | Cross-Cutting | Facebook/Zalo tenant support adapters | Quản trị / Hệ thống / Tác vụ nền | No tenant channel configuration UI | None | Schema permits `FACEBOOK`/`ZALO`, but no webhook adapter, token configuration, consent or delivery mapping exists | Schema permits `FACEBOOK`/`ZALO`, but no webhook adapter, token configuration, consent or delivery mapping exists | External Facebook/Zalo sandbox | BLOCKED | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Pending approved adapter/config permissions. Bằng chứng: No sandbox credentials or provider contract. Cần làm: Keep in-app chat authoritative, define approved webhook/OAuth contracts and use provider test accounts only after OQ-006 to OQ-009 are resolved.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-018 | Cross-Cutting | Admin KPI, revenue and occupancy dashboard | Quản trị / Hệ thống / Tác vụ nền | `/admin/dashboard` | Legacy analytics client | `AnalyticsService` returns fixed revenue/bookings/occupancy arrays and an unlabelled mock AI forecast | `AnalyticsService` returns fixed revenue/bookings/occupancy arrays and an unlabelled mock AI forecast | No authoritative dashboard query | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: `REPORT:VIEW` route. Bằng chứng: Incomplete audit proves cards stay zero despite non-zero fixture; source confirms hard-coded backend. Cần làm: Replace with context-correct authoritative report/operations data, truthful timestamps/basis, loading/error states and reconciliation tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-019 | Cross-Cutting | Admin onboarding progress and approval CTAs | Quản trị / Hệ thống / Tác vụ nền | First dashboard section | Hard-coded component state | No onboarding progress/approval command is loaded or submitted | No onboarding progress/approval command is loaded or submitted | No state mapping | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Visible without action authorization. Bằng chứng: Two browser audits failed because CTAs were absent/non-actionable. Cần làm: Load actual property setup/approval state, wire permission-aware actions and remove the dashboard section for system-admin contexts where it does not apply.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-020 | Cross-Cutting | Admin maintenance/work-order table and legacy exports | Quản trị / Hệ thống / Tác vụ nền | Dashboard data table | Timer-only empty state | No work-order API request; table export controls do not produce Excel/PDF downloads | No work-order API request; table export controls do not produce Excel/PDF downloads | Missing work-order model/API | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: Missing. Bằng chứng: Four browser audits confirm no API and no downloads. Cần làm: Implement maintenance work orders and use canonical export services, or remove misleading controls until the capability exists.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-021 | Cross-Cutting | Management operations dashboard | Quản trị / Hệ thống / Tác vụ nền | `/management/dashboard` | `ManagementApiService.context()` | Returns selected-property room/housekeeping counts plus subscription/usage context | Returns selected-property room/housekeeping counts plus subscription/usage context | Rooms/tasks and mixed subscription sources | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Broad management role. Bằng chứng: Component/layout tests passed. Cần làm: Reconcile counts to the selected property and authoritative entitlement, add freshness/error semantics and real property-switch IDOR tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-022 | Cross-Cutting | Management property screen | Quản trị / Hệ thống / Tác vụ nền | `/management/properties` | None beyond context | Route loads the exact dashboard component and no property-specific edit/list workflow | Route loads the exact dashboard component and no property-specific edit/list workflow | Existing properties only | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Broad management role. Bằng chứng: Incomplete browser audit failed on identical route content. Cần làm: Build a distinct property profile/list/edit surface with tenant-safe fields, route title and behavior tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-023 | Cross-Cutting | Property revenue dashboard | Quản trị / Hệ thống / Tác vụ nền | `/management/property-revenue` | Typed report client | Authoritative property report filters, rows, totals and reconciliation status | Authoritative property report filters, rows, totals and reconciliation status | Property ledger/invoice/refund queries | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: `REPORT:VIEW`. Bằng chứng: Component/client and report security tests passed; browser/API data was intercepted. Cần làm: Add real SQL Server reconciliation/browser evidence, large-result pagination and expose the already implemented property export action.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-024 | Cross-Cutting | Platform Billing revenue dashboard | Quản trị / Hệ thống / Tác vụ nền | `/admin/platform-revenue` | Typed report/export client | System-scoped report and export APIs with separate permission | System-scoped report and export APIs with separate permission | Platform ledger/refunds/contracts | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: `PLATFORM_REVENUE:VIEW/EXPORT`. Bằng chứng: Component, client, security and intercepted reporting browser tests passed. Cần làm: Add database-backed browser/export verification and final row/total/checksum reconciliation before promotion to complete.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-025 | Cross-Cutting | Shared CSV/Excel/PDF financial export renderer | Quản trị / Hệ thống / Tác vụ nền | Platform export UI; property API/client | `RevenueReportService` | One result model renders three formats with content type, row count and checksum | One result model renders three formats with content type, row count and checksum | Generated files | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Context export permission. Bằng chứng: Export unit/integration 3/3 passed; platform browser download wiring passed with intercepted API. Cần làm: Preserve same-model and checksum tests while final database/file reconciliation is executed.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-026 | Cross-Cutting | Property revenue export UI | Quản trị / Hệ thống / Tác vụ nền | Property revenue page has no export controls | Client method `exportPropertyRevenue()` exists | Property export endpoint supports CSV/Excel/PDF | Property export endpoint supports CSV/Excel/PDF | Generated files | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: `REPORT:EXPORT`. Bằng chứng: Source confirms API/client without UI. Cần làm: Add permission-aware export controls, loading/success/error messages, filename/checksum handling and browser download tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-027 | Cross-Cutting | Non-financial operational exports | Quản trị / Hệ thống / Tác vụ nền | Legacy dashboard suggests export; no reliable reservation/customer/room/housekeeping exports found | No canonical clients | No shared operational export result/contracts | No shared operational export result/contracts | Missing | FAIL | P2 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Missing. Bằng chứng: Admin Excel/PDF audit failed. Cần làm: Define approved reservation/customer/room/task export schemas with PII minimization, tenant permissions, filters and file-level tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-028 | Cross-Cutting | Financial append-only audit and redaction | Quản trị / Hệ thống / Tác vụ nền | No audit viewer | None | `FinancialAuditService` records context, actor, states, reason, identities, correlation and redacted metadata | `FinancialAuditService` records context, actor, states, reason, identities, correlation and redacted metadata | Financial audit events | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: Internal financial services. Bằng chứng: Audit service test passed. Cần làm: Add tenant/system read APIs, retention/export policy, pagination and an authorized audit viewer with IDOR/privacy tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-029 | Cross-Cutting | Support conversation audit | Quản trị / Hệ thống / Tác vụ nền | No audit/history panel | None | Assignment/reply/escalation and denied attempts write tenant-owned support events | Assignment/reply/escalation and denied attempts write tenant-owned support events | Support conversation events | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Internal support services. Bằng chứng: Isolation integration verifies denied and accepted events. Cần làm: Expose immutable event history to authorized supervisors and add close/reopen/read-state events plus retention controls.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-030 | Cross-Cutting | General operational/admin audit log | Quản trị / Hệ thống / Tác vụ nền | `/admin/audit-log`, `/management/audit-log` | `OperationalAuditService` | `OperationalAuditController` and `OperationalAuditService` capture staff, roles, property, room, maintenance, housekeeping and reservation lifecycle mutations | `OperationalAuditController` and `OperationalAuditService` capture staff, roles, property, room, maintenance, housekeeping and reservation lifecycle mutations | `operational_audit_events` + V47 append-only SQL Server trigger | PASS | P0 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: `AUDIT_LOG:VIEW/EXPORT`; tenant assignment boundary; system scope is `SUPER_ADMIN` only. Bằng chứng: `OperationalAuditServiceTest` 4/4, controller 1/1, regression 51/51, Angular 2/2; [T213 evidence](../../testing/evidence/007/remediation/T213-operational-audit-log.md). Cần làm: Preserve append-only and tenant isolation tests.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-031 | Cross-Cutting | Stable error response and retry semantics | Quản trị / Hệ thống / Tác vụ nền | Shared Angular API-error model preserves code, fields, correlation, retryability and current state | Correlation/idempotency and navigation interceptors preserve the envelope | Controller advice, auth handlers, Spring Security and permission interception emit one stable envelope; external provider callback ACK bodies remain protocol-specific | Controller advice, auth handlers, Spring Security and permission interception emit one stable envelope; external provider callback ACK bodies remain protocol-specific | Responses plus transaction rollback boundaries | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Per endpoint. Bằng chứng: Fresh backend/frontend runs passed 40/40; evidence: `docs/testing/evidence/007/remediation/T189-stable-error-envelope.md`. Cần làm: Preserve additive compatibility and the completed catalog; new endpoints must classify retry safety and never expose internal exception detail.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-032 | Cross-Cutting | Loading, error and empty-state presentation | Quản trị / Hệ thống / Tác vụ nền | Stronger on Home, chat, notifications, management and reports; inconsistent on legacy admin screens | Mixed component-local state and `FeedbackStateComponent` | APIs provide varied errors | APIs provide varied errors | None | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Không bắt buộc | Theo nhóm | Quyền: UI only. Bằng chứng: Focused components pass; several legacy screens only log errors to console. Cần làm: Inventory every route state, replace console-only failures, provide accessible retry/empty guidance and add component/browser assertions per route.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-033 | Cross-Cutting | Async cancellation, duplicate-submit and safe retry | Quản trị / Hệ thống / Tác vụ nền | Global mutation headers plus booking/cancellation coordinator | `AsyncActionCoordinatorService` provides join/replace/cancel/busy/retry-safe policies | Persisted replay for reservation create/cancel; financial idempotency remains context-owned | Persisted replay for reservation create/cancel; financial idempotency remains context-owned | Mixed | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Per action. Bằng chứng: 26 backend/frontend/browser assertions passed; evidence: `docs/testing/evidence/007/remediation/T191-async-mutation-retry.md`. Cần làm: Legacy admin CRUD endpoints still need endpoint-specific persisted replay; keep their work in generated property/operations tasks.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-034 | Cross-Cutting | Home/login responsive layout and visible keyboard entry | Quản trị / Hệ thống / Tác vụ nền | `/` and `/login` | Public layout/components | Public APIs are not required for layout assertion | Public APIs are not required for layout assertion | Browser layout | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Public. Bằng chứng: 8/8 viewport cases passed at 375, 768, 1024 and 1440 widths with no horizontal overflow and visible focus. Cần làm: Preserve the viewport/focus suite while extending the same matrix to authenticated routes.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-035 | Cross-Cutting | Admin/management/customer authenticated responsive coverage | Quản trị / Hệ thống / Tác vụ nền | Admin/management layouts include mobile navigation; financial pages have local media rules | Layout services/components | Authenticated APIs | Authenticated APIs | Browser layout | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Role-specific. Bằng chứng: Layout component tests pass; no complete 320/375/768/1024/1440 role-route matrix. Cần làm: Execute all critical routes for customer, owner, receptionist, housekeeping and admin at required widths; fix tables/dialogs/overlays and record screenshots.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-036 | Cross-Cutting | Keyboard, focus, labels and live-region semantics | Quản trị / Hệ thống / Tác vụ nền | Public date/search, login, chat, notifications and report forms include many ARIA attributes | Component templates | None | None | Browser DOM | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Public/authenticated. Bằng chứng: Responsive audit verifies first tab/focus; source has broad but uneven ARIA usage. Cần làm: Add skip-link/focus restoration, dialog trapping, error focus, table semantics and keyboard journeys across all critical forms and overlays.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-037 | Cross-Cutting | Automated contrast, screen-reader and accessibility regression | Quản trị / Hệ thống / Tác vụ nền | No consolidated accessibility suite with an engine such as axe | None | None | None | Missing test evidence | FAIL | P1 | Cao - có thể chặn luồng demo | Có | Theo nhóm | Quyền: Test-only. Bằng chứng: Existing audit checks overflow/focus, not contrast/name/role/value violations. Cần làm: Add automated accessibility scans, manual screen-reader checks, contrast evidence and severity gates for every critical role journey.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-038 | Cross-Cutting | Reduced motion and VI/EN Home layout stability | Quản trị / Hệ thống / Tác vụ nền | Home slideshow and locale control | Public locale/slideshow services | None | None | Browser preference/local storage | PASS | P2 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Public. Bằng chứng: Reduced-motion plus five cold desktop and mobile locale runs passed without layout shift. Cần làm: Preserve these checks and extend reduced-motion rules to admin charts, dialogs and page transitions.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-039 | Cross-Cutting | Vietnamese/English locale switching | Quản trị / Hệ thống / Tác vụ nền | Public header exposes toggle; Home/public strings use translation resources | Public i18n services | Backend values remain mostly Vietnamese/default | Backend values remain mostly Vietnamese/default | Local storage and translation JSON | PARTIAL | P1 | Trung bình - cần nêu giới hạn khi demo | Có | Theo nhóm | Quyền: Public. Bằng chứng: Layout/Home locale tests and browser stability pass. Cần làm: Cover every public route and define an authenticated-admin localization policy; persist locale to profile if approved and test fallback/missing keys.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-040 | Cross-Cutting | Text encoding and terminology consistency | Quản trị / Hệ thống / Tác vụ nền | Most public strings are valid; admin remains largely hard-coded Vietnamese | Mixed inline strings and translation resources | Backend messages are mostly Vietnamese with separate English financial errors | Backend messages are mostly Vietnamese with separate English financial errors | Source files/resources | FAIL | P1 | Cao - có thể chặn luồng demo | Không bắt buộc | Theo nhóm | Quyền: UI only. Bằng chứng: Source scan found a confirmed mojibake error message in reservation management; Angular reports terminology warnings elsewhere. Cần làm: Remove mojibake, centralize VI/EN status/error/financial terms, prevent encoding regressions and complete T164 terminology normalization.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-041 | Cross-Cutting | Logs, correlation, metrics and operational observability | Quản trị / Hệ thống / Tác vụ nền | Detail-free health probes and operator metrics route | HTTP/realtime clients preserve correlation and emit metadata-only failures | HTTP/STOMP/scheduler/mail instrumentation records structured logs and low-cardinality timers/counters | HTTP/STOMP/scheduler/mail instrumentation records structured logs and low-cardinality timers/counters | Actuator/Micrometer and application logs; durable outbox remains CROSS-009 | PASS | P1 | Thấp hoặc đã có bằng chứng | Có | Theo nhóm | Quyền: Health public; metrics `SUPER_ADMIN` only. Bằng chứng: Fresh backend/frontend suites passed 39/39; P0-02B chứng minh production `OperationalMetrics` hoạt động và WebMvc slice cần mock dependency của `CorrelationIdFilter`, focused controller PASS 6/6. Evidence: `docs/testing/evidence/007/remediation/T190-operational-observability.md`, `docs/hotel-report/fix-progress/P0-02B.md`. Cần làm: Preserve no-PII tags, operator-only metrics and the automatic scheduler wrapper; configure external alert routing separately.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
| CROSS-042 | Cross-Cutting | Cross-cutting HTTP/WebSocket integration harness | Quản trị / Hệ thống / Tác vụ nền | Not user-facing | Angular/Playwright suites execute separately | Three integration suites pin `BackendApplication`; controller/interceptor suites execute current request and destination contracts | Three integration suites pin `BackendApplication`; controller/interceptor suites execute current request and destination contracts | Full H2 MVC contexts plus local random-port SockJS/STOMP server | PASS | P1 | Thấp hoặc đã có bằng chứng | Không bắt buộc | Theo nhóm | Quyền: Test-only. Bằng chứng: P0-02A cô lập shared H2 lifecycle; P0-02B/P0-02D cập nhật đúng WebMvc slice dependencies. Sau P0-02D Hotel controller PASS 2/2, P0-02A/B/C guards lần lượt PASS 18/18, 24/24, 40/40 và full backend đạt 817 PASS/7 ERROR. Evidence: `docs/hotel-report/fix-progress/P0-02A.md`, `docs/hotel-report/fix-progress/P0-02B.md`, `docs/hotel-report/fix-progress/P0-02D.md`. Cần làm: Preserve explicit application selection, complete slice dependencies and unique database fixtures for contexts that are dirtied; product/browser gaps remain tracked by their capability rows.. Nguồn: [cross-cutting.md](inventory/cross-cutting.md) |
## P0-02K tenant-isolation update (2026-08-06)

- Function ID: `STAY-020` (final invoice compatibility access), security context `CROSS-013`.
- Deprecated `POST /api/invoices/reservation/{reservationId}` now verifies reservation ownership/property access before returning the legitimate pre-finalization `409` state.
- Cross-tenant and nonexistent reservation identifiers both return `404` with an empty legacy response, preventing resource/state enumeration; an owned reservation without a finalized invoice returns `409`.
- Evidence: `fix-progress/P0-02K.md` and `fix-progress/evidence/P0-02K-*`.
