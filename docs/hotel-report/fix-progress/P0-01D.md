# P0-01D - Subscription policy frontend test failure

Ngày xử lý: 2026-08-05

Trạng thái blocker: **PASS**

Deployment readiness tổng thể: **NOT_READY**

Frontend P0 readiness: **READY_WITH_PAYMENT_EXCEPTIONS**

## 1. Function ID và phạm vi

- Function ID chính: `PROP-SUB-018` - Downgrade and proration policy.
- Function ID context: `PROP-SUB-019` - billing current-plan/catalog context.
- Test suite: `SubscriptionBillingComponent`.
- Test case: `creates a backend-owned purchase order and renders truthful policy blockers`.
- Spec: `frontend/src/app/features/management/subscription-billing/subscription-billing.component.spec.ts`.
- Component: `SubscriptionBillingComponent`.
- Service: `PlatformBillingService.getPolicyAvailability()`.
- Policy: backend `SubscriptionPolicyService`, error code `POLICY_NOT_CONFIGURED`.
- Phạm vi sửa: chỉ hai expectation trong subscription billing spec; không sửa production code, backend hoặc payment.

## 2. Kết quả trước khi sửa

Lệnh:

```powershell
cd frontend
npm test -- --watch=false --include=src/app/features/management/subscription-billing/subscription-billing.component.spec.ts
```

Kết quả:

- 1/1 test file FAIL.
- 3/4 test PASS, 1/4 test FAIL.
- Không có unhandled error.
- Test case lỗi tại assertion policy text ở dòng 44.
- Full frontend baseline trước P0-01D: 102/105 file PASS, 314/317 test PASS, 3 FAIL, 0 unhandled error.

Evidence:

- `evidence/P0-01D-focused-before.log`
- `evidence/P0-01D-stack-trace-clean.log`
- Full baseline: `evidence/P0-01C-full-after.log`

## 3. Expected, actual và stack trace

Expected đầu tiên:

```text
Downgrade is blocked
```

Actual relevant text:

```text
Hạ gói
Chưa cấu hình chính sách hạ gói
Tính phí chênh lệch
Chưa cấu hình cách tính phí chênh lệch
```

Assertion kế tiếp cũng đã cũ:

```text
Expected: Order SUB-1
Actual:   Đơn SUB-1
```

Stack trace đã làm sạch:

```text
AssertionError: expected rendered text to contain "Downgrade is blocked"
at frontend/src/app/features/management/subscription-billing/subscription-billing.component.spec.ts:44:33
```

Raw output và full rendered text nằm trong `evidence/P0-01D-focused-before.log`; bản sạch nằm trong `evidence/P0-01D-stack-trace-clean.log`.

## 4. Đối chiếu production contract

Production contract hiện tại:

1. `GET /api/platform/subscription-policies` trả `downgradeConfigured`, `prorationConfigured`, `errorCode`, `downgradeMessage`, `prorationMessage`.
2. `SubscriptionPolicyService.availability()` trả cả hai configured flag là `false` và `POLICY_NOT_CONFIGURED`.
3. Khi chưa có policy được phê duyệt, backend không tạo downgrade order, entitlement change, credit hoặc financial mutation.
4. Billing UI đã được Việt hóa và render trạng thái chặn bằng `Chưa cấu hình chính sách hạ gói` và `Chưa cấu hình cách tính phí chênh lệch`.
5. Order snapshot heading đã được Việt hóa từ `Order` thành `Đơn`.

Các tài liệu `subscription-policy-blockers.md`, `platform-billing-ui-readiness.md` và inventory `PROP-SUB-018` xác nhận hành vi fail-closed là nghiệp vụ đúng. Việc capability vẫn `BLOCKED` nghĩa là policy thương mại chưa được phê duyệt, không có nghĩa UI phải dùng wording tiếng Anh cũ.

## 5. Phân loại nguyên nhân

| Khả năng | Kết luận | Lý do |
|---|---|---|
| Fixture/mock đã cũ | Một phần | Fixture vẫn mô phỏng policy bị chặn đúng, nhưng message tiếng Anh không còn là wording UI |
| Expectation không còn đúng | **Nguyên nhân gốc** | Hai assertion giữ wording tiếng Anh trước khi UI được Việt hóa |
| Enum/status/role thay đổi | Không | Flags và `POLICY_NOT_CONFIGURED` không đổi |
| API contract thay đổi | Không | Typed policy availability contract không đổi |
| Production policy sai | Không | Backend và UI vẫn fail-closed, hiển thị đúng trạng thái không cấu hình |

Production behavior: **ĐÚNG**.

## 6. Thay đổi tối thiểu

Chỉ sửa hai expectation:

```ts
expect(element.textContent).toContain('Chưa cấu hình chính sách hạ gói');
expect(element.textContent).toContain('Đơn SUB-1');
```

File sửa:

- `frontend/src/app/features/management/subscription-billing/subscription-billing.component.spec.ts`

Không thay đổi template, component, service, enum, API, backend hoặc payment file.

## 7. Git checkpoint trước sửa

- Branch: `codex/ui-functional-audit-polish`.
- HEAD: `88f7da5d52dbcc6b802a44bcf19e9aca97ae905f`.
- Checkpoint: `stash@{0}`.
- Object: `d9c829cd0d3b45e13231b1db24aff394ed04f110`.
- Message: `P0-01D pre-fix checkpoint`.
- `.git/index.lock`: không tồn tại khi tạo checkpoint.
- Checkpoint ghi snapshot tracked mà không stash/reset worktree bẩn hiện hữu.

Vị trí hiện tại: P0-01C `stash@{1}`, P0-01B `stash@{2}`, P0-01A `stash@{3}`.

## 8. Kết quả validation

| Kiểm tra | Kết quả | Evidence |
|---|---|---|
| Focused subscription | **PASS** - 1/1 file, 4/4 test, exit 0 | `evidence/P0-01D-focused-after.log` |
| Frontend production build | **PASS** - exit 0, Angular generation 40.806 giây | `evidence/P0-01D-build.log` |
| Regression guards | **PASS** - 3/3 file, 9/9 test | `evidence/P0-01D-regression-guards.log` |
| Full frontend regression | **FAIL với payment exceptions** - 103/105 file PASS, 315/317 test PASS, 2 FAIL, 0 unhandled error | `evidence/P0-01D-full-after.log` |
| Backend build/test | **N/A** - backend không thay đổi | Không áp dụng |

Regression guard mapping:

- `chat.service.spec.ts`: `CROSS-011`, `CROSS-014`, `CROSS-015` - PASS 3/3.
- `partner-overview.component.spec.ts`: `PROP-SUB-006`, `AUTH-019` - PASS 3/3.
- `account-settings.component.spec.ts`: `AUTH-018` - PASS 3/3.

Build vẫn có các warning CSS budget/CommonJS hiện hữu; P0-01D không thay đổi chúng.

## 9. Full regression trước và sau

| Chỉ số | Trước P0-01D | Sau P0-01D | Thay đổi |
|---|---:|---:|---:|
| Test file PASS | 102/105 | 103/105 | +1 |
| Test PASS | 314/317 | 315/317 | +1 |
| Test FAIL | 3 | 2 | -1 |
| Unhandled error | 0 | 0 | 0 |

Subscription billing spec PASS trong focused run và full suite.

## 10. Hai lỗi còn lại

1. `profile.component.spec.ts` - profile payment wording: **DEFERRED_PAYMENT_PHASE**.
2. `payment-result.spec.ts` - payment result/callback wording: **DEFERRED_PAYMENT_PHASE**.

Không file payment nào được sửa. Full frontend suite không được ghi PASS vì hai test này vẫn FAIL.

## 11. Đánh giá frontend sau P0-01D

```text
Frontend build: PASS
Frontend non-payment regression: PASS
Payment wording regression: DEFERRED_PAYMENT_PHASE
Frontend P0 readiness: READY_WITH_PAYMENT_EXCEPTIONS
```

Deployment tổng thể vẫn `NOT_READY` do backend regression, permission/runtime/config/topology và Payment Sandbox chưa hoàn tất.

## 12. Evidence và tài liệu

- `docs/hotel-report/fix-progress/evidence/P0-01D-focused-before.log`
- `docs/hotel-report/fix-progress/evidence/P0-01D-stack-trace-clean.log`
- `docs/hotel-report/fix-progress/evidence/P0-01D-focused-after.log`
- `docs/hotel-report/fix-progress/evidence/P0-01D-build.log`
- `docs/hotel-report/fix-progress/evidence/P0-01D-regression-guards.log`
- `docs/hotel-report/fix-progress/evidence/P0-01D-full-after.log`
- `docs/hotel-report/02-functional-inventory.md`
- `docs/hotel-report/04-deployment-readiness.md`

## 13. Hướng dẫn kiểm tra thủ công

### A. Kiểm tra tự động

1. Mở PowerShell tại root repository và chạy `cd frontend`.
2. Chạy `npm test -- --watch=false --include=src/app/features/management/subscription-billing/subscription-billing.component.spec.ts`.
3. Xác nhận 1/1 file và 4/4 test PASS, không có `Unhandled Errors`.
4. Chạy `npm test -- --watch=false --include=src/app/core/services/chat.service.spec.ts --include=src/app/features/admin/partner-overview/partner-overview.component.spec.ts --include=src/app/features/client/account-settings/account-settings.component.spec.ts`.
5. Xác nhận 3/3 file và 9/9 test PASS.
6. Chạy `npm run build` và xác nhận exit 0.
7. Chạy `npm test -- --watch=false`.
8. Xác nhận 315/317 test PASS, đúng hai FAIL là `profile.component.spec.ts` và `payment-result.spec.ts`, không có unhandled error.

### B. Kiểm tra UI subscription policy

1. Khởi động frontend/backend Hotel theo runbook local; không dùng backend khác đang chiếm cổng 8080.
2. Đăng nhập tài khoản owner/management có quyền xem Platform Billing và chọn một cơ sở hợp lệ.
3. Mở `/management/billing?propertyId=<ID_CƠ_SỞ_TEST>`.
4. Trong DevTools Network, xác nhận `GET /api/platform/subscription-policies` trả HTTP 200 với `downgradeConfigured=false`, `prorationConfigured=false`, `errorCode=POLICY_NOT_CONFIGURED`.
5. Xác nhận UI hiển thị `Chưa cấu hình chính sách hạ gói` và `Chưa cấu hình cách tính phí chênh lệch`.
6. Xác nhận UI không cung cấp thao tác hạ gói hoặc credit tự động khi policy chưa cấu hình.
7. Với order fixture test riêng, xác nhận snapshot dùng tiêu đề `Đơn <mã đơn>` và hiển thị giá/thời hạn do máy chủ trả về.
8. Không mở, sửa hoặc xác nhận bất kỳ payment flow/provider callback nào trong kiểm tra P0-01D.

## 14. Blocker tiếp theo đề xuất

Đề xuất tiếp theo: mở **Payment Sandbox phase** để xử lý hai payment wording test `DEFERRED_PAYMENT_PHASE` cùng contract sandbox tương ứng. P0-01D không khởi động phase này và dừng trước mọi thay đổi payment.

## 15. Kết luận

**PASS** cho blocker `P0-01D`: subscription policy production behavior đúng, hai expectation tiếng Anh đã cũ được đồng bộ tối thiểu, focused/build/guards đều PASS và full regression chỉ còn đúng hai payment wording failure được defer.

Dừng tại P0-01D; không xử lý payment, backend hoặc blocker tiếp theo.
