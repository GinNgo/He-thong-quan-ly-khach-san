# Property Onboarding And Subscription Inventory

Audit date: 2026-08-04
Scope: partner registration, property onboarding/submission/approval/suspension,
imported-property claims, owner assignment and the plan/limit/entitlement lifecycle
required by FR-043 and FR-044.

## Evidence Rules

- `COMPLETE_VERIFIED` requires current executable evidence for the reachable behavior.
- Source-only behavior is not promoted to complete.
- Severity is `P0` critical access-control/data-boundary risk, `P1` release-significant,
  `P2` product-quality and `P3` minor polish.
- Platform Billing evidence remains separate from legacy `account_subscriptions`; a successful
  platform payment is authoritative only if the management feature gates read its entitlement.

## Fresh Verification

| Command / evidence | Result | Evidence impact |
|---|---|---|
| Focused backend service suite for claim, catalog, legacy limits, platform purchase/application/renewal/upgrade/policy | 33/33 passed | Claim quota rollback, legacy limit aggregation and the isolated Platform Billing lifecycle are current. |
| T185 claim/subscription HTTP and lifecycle suite | 13/13 controller assertions and 3/3 focused service assertions passed | Claim/subscription permission, malformed-payload rejection, principal ownership, DTO serialization, quota rollback and subscription usage lifecycle are executable. |
| Focused Angular owner/admin billing suite | 8/8 passed | Partner overview table/permission/approve request, platform order UI, payment panel and policy blocker presentation are current. |
| T234 property approval backend and Angular suites | Backend 78/78, frontend 10/10 and development build passed | The canonical approval/rejection transition, reviewer metadata, owner activation/deactivation, durable notification, tenant audit, public/management gates and typed admin UI are executable. |
| T236 registration review history and notification suites | Backend production compile and 64/64 focused tests; Angular 38/38 and development build passed | Owner-safe bounded history, reviewer notes, transactional in-app/outbox persistence, retry/dead-letter evidence and admin/owner history UI are executable. |
| T237 imported-property claim request suites | Backend production compile, 27/27 focused tests and 11/11 clock/persistence regressions; Angular 14/14 and development build passed | Typed normalized requests, stable rate-limit errors, requester/admin DTO clients, authentication recovery and accessible claim-dialog behavior are executable. |
| T238 claim approval and owner activation suites | Backend production compile and 44/44 focused tests; Angular 8/8 and development build passed | Locked canonical activation, owner-role refresh, audit/in-app/outbox rollback, active-assignment isolation and property-targeted management navigation are executable. |
| `docs/audit/financial/PLATFORM_BILLING_AUDIT.md` and T093-T110 evidence | Existing purchase, renewal, upgrade, callback, exactly-once application and browser evidence | The isolated Platform Billing flow can remain complete; this does not prove the legacy management entitlement bridge. |

## Capability Inventory

| ID | Capability | Route / UI | Frontend service | Backend API / service | Database | Permission | Tests / evidence | Status | Severity | Required remediation |
|---|---|---|---|---|---|---|---|---|---|---|
| PROP-SUB-001 | Public partner registration | `/partner/register`; canonical province/ward selectors, address validation, field-level errors and draft-registration redirect | Typed flat payload with normalized identity/profile/property fields; no caller-supplied city/country or legacy `propertyAddress` | Anonymous-only `POST /api/partner/register` with validated `PartnerRegistrationRequest`; service verifies active province/ward ancestry and performs one transaction | Creates normalized `users`, a complete non-demo `hotels` DRAFT row and PENDING owner mapping; no legacy BASIC entitlement is granted; city/country are derived server-side | Public anonymous path; authenticated conversion is rejected and belongs to PROP-SUB-002 | Backend focused suite 7/7 and Angular component suite 4/4; evidence: `docs/testing/evidence/007/remediation/T230-public-partner-registration.md` | `COMPLETE_VERIFIED` | P1 | Preserve anonymous-only DTO validation, server-owned geography derivation, atomic rollback and the DRAFT boundary before owner submission. |
| PROP-SUB-002 | Existing-customer partner conversion | `/partner/register` switches to a current-account explanation and property-only form for authenticated users | `PartnerRegistrationService.convertAuthenticated()` sends only property name, canonical location ids and address; anonymous registration remains separate | Authenticated-only `POST /api/partner/convert` resolves the account strictly from `CustomUserDetails.userId`, locks it, verifies normalized email ownership and rejects credential/account fields | Creates a complete DRAFT property and PENDING owner mapping atomically without changing password, profile or roles | Authenticated current account; generic/non-authoritative principals and anonymous callers are denied | Backend focused suite 14/14 and Angular focused suite 7/7; evidence: `docs/testing/evidence/007/remediation/T231-existing-customer-partner-conversion.md` | `COMPLETE_VERIFIED` | P1 | Preserve the principal-id boundary, strict property-only request and separate anonymous account-creation contract. |
| PROP-SUB-003 | Pending/draft property access gate | `/management/**` | Management context keeps assigned setup/billing properties visible and hides operational links until the selected property is operational | `PropertyAccessService.assignedHotelIds()` defines draft-only ownership scope; `requireManagedHotel()` and `requireAccessibleOrNotFound()` require `approval_status=APPROVED` and `operation_status=ACTIVE`, returning `PROPERTY_NOT_OPERATIONAL` otherwise | Active mappings remain available for setup/billing; operational APIs use the approved+active gate | Owner/management roles plus server-side property state | `PropertyAccessServiceTest` 6/6, `ManagementPortalServiceTest` 1/1, `PropertyOperationalErrorContractTest` 1/1, Angular management/dashboard tests 6/6; evidence: `docs/testing/evidence/007/remediation/T194-property-operational-access.md` | `COMPLETE_VERIFIED` | P0 | Preserve the split assigned/operational scopes; pending-owner lifecycle and approval transition consistency remain PROP-SUB-005/006/008 scope. |
| PROP-SUB-004 | Partner registration status | `/partner/registration-status` renders one truthful card per property with mixed, empty, loading, retry and safe management states | Typed `PartnerRegistrationStatusService` consumes `overallStatus`, `propertyCount` and canonical per-property rows | Authenticated `GET /api/partner/registration-status` resolves `CustomUserDetails.userId`, classifies only DRAFT/PENDING/APPROVED/REJECTED/SUSPENDED/CANCELLED and returns `MIXED` when needed | Bulk-fetches current OWNER mappings and exact requester/property rejected claims; duplicate legacy mappings collapse to the newest property row | Authenticated current account only; generic principals are denied and rejection reasons are rechecked against user/property ownership | Backend focused suite 33/33 and Angular focused suite 6/6; evidence: `docs/testing/evidence/007/remediation/T232-partner-registration-status.md` | `COMPLETE_VERIFIED` | P1 | Preserve the per-property contract, exact management gate and reason isolation as later lifecycle tasks add durable review events. |
| PROP-SUB-005 | Owner submits a draft property for review | `/partner/registration-status` shows a per-property submit action only for the exact DRAFT/INACTIVE/PENDING owner state | Typed `POST /api/partner/properties/{id}/submit`; legacy `/api/v1/hotels/{id}/submit` delegates to the same workflow | `PropertyApprovalWorkflowService.submitDraft()` locks the property and exact pending owner mapping, validates the transition, updates all status fields and appends tenant audit evidence in one transaction | `hotels.status` and `approval_status` become `PENDING_APPROVAL`; `operation_status` remains `INACTIVE`; audit failure rolls the transition back | Authenticated authoritative principal id plus exact PENDING OWNER mapping; cross-account ids fail as not found | Backend focused suite 46/46 and Angular focused suite 9/9; evidence: `docs/testing/evidence/007/remediation/T233-owner-draft-submission.md` | `COMPLETE_VERIFIED` | P1 | Preserve the locked owner-submit boundary and actionable queue filter as approval/rejection extends the same state machine in PROP-SUB-006. |
| PROP-SUB-006 | Admin property approval/rejection | `/admin/property-approvals`; typed queue with inline rejection editor | `PropertyService` exposes typed queue/approve/reject calls and the admin component enforces permission-safe actions and busy states | `PropertyApprovalWorkflowService` owns submit/approve/reject; typed admin and legacy routes delegate to it | Approve writes `ACTIVE/APPROVED/ACTIVE`, reject writes `REJECTED/REJECTED/INACTIVE`; exact owner mapping, role, reviewer metadata, durable notification and tenant audit commit atomically | Admin queue requires `ADMIN` or `SUPER_ADMIN`; legacy decisions require `SUPER_ADMIN`; actor id is authoritative and public/management reads require all canonical active states | Backend 78/78, Angular 10/10 and development build passed; evidence: `docs/testing/evidence/007/remediation/T234-admin-property-approval.md` | `COMPLETE_VERIFIED` | P1 | Preserve the single locked state machine and exact-owner invariant as suspension/history and imported-claim lifecycles are added. |
| PROP-SUB-007 | Property suspension/reactivation/closure | `/admin/properties` renders separate state badges, server-allowed actions and one validated reason dialog; management dashboard keeps suspended/closed properties selectable with read-only guidance | Typed lifecycle list and suspend/reactivate/close calls with same-row busy protection and stable safe-retry identity | `PropertyLifecycleWorkflowService` owns locked reasoned transitions and exact replay behavior; admin controller enforces lifecycle view/approve permissions | V71 adds lifecycle action/reason/actor/time metadata and permission rows; mappings, roles, bookings and historical rows remain unchanged | `ADMIN`/`SUPER_ADMIN` plus `PROPERTY_LIFECYCLE` VIEW or APPROVE; actor comes only from authenticated principal | Backend 98/98 regression tests, Angular 15/15 and development build passed; evidence: `docs/testing/evidence/007/remediation/T235-property-lifecycle.md` | `COMPLETE_VERIFIED` | P1 | Preserve the terminal close state, canonical public/operational gate and transaction rollback boundary; add immutable history plus email/outbox retry in PROP-SUB-009. |
| PROP-SUB-008 | Owner role and mapping activation timing | Registration and management shell | Pending applicants do not receive `PROPERTY_OWNER`; the role is granted only when approval activates ownership | Registration and claim services create `PENDING` owner mappings; approval changes the mapping to `ACTIVE`, while reject/cancel changes it to `INACTIVE` with an end timestamp | `app_user_role`, `user_properties` | Operational owner authority starts at approval; pending applicants remain non-operational | `PropertyOwnershipLifecycleServiceTest` 4/4, `PropertyRegistrationServiceTest` 1/1, `PropertyClaimServiceTest` 5/5, `PropertyClaimControllerIntegrationTest` 8/8, `HotelControllerIntegrationTest` 2/2; evidence: `docs/testing/evidence/007/remediation/T195-owner-mapping-activation.md` | `COMPLETE_VERIFIED` | P0 | Preserve the activation boundary and add transfer/history policy in the later ownership tasks. |
| PROP-SUB-009 | Registration review history and notifications | Owner registration status and both admin approval/lifecycle views expose a lazy, retryable, nullable-safe transition timeline | Typed owner/admin history calls, canonical event/actor unions and optional trimmed approval note | `PropertyReviewHistoryService` returns at most 100 newest tenant audit transitions; approval/lifecycle workflows persist reviewer notes, in-app notifications and property-review email outbox rows in the transition transaction; dispatcher retries only when explicitly enabled | Canonical history remains append-only in `operational_audit_events`; V72 adds a locked email outbox plus append-only delivery attempts, invariant checks and stale-claim recovery | Owner history accepts any exact user/property OWNER relation, including inactive, rejected or duplicate legacy mappings, and returns not-found cross-owner; admin history requires `PROPERTY_LIFECYCLE/VIEW`; raw actor ids/JSON are never returned | Backend compile and 64/64 focused tests; Angular 38/38 and development build; evidence: `docs/testing/evidence/007/remediation/T236-registration-review-history.md` | `COMPLETE_VERIFIED` | P2 | Preserve the bounded redacted history contract and same-transaction durable effects; consolidate the domain outbox with the generic T324 outbox only through a coordinator-owned migration plan. |
| PROP-SUB-010 | Imported-property claim request | Hotel detail claim dialog with anonymous login return, accessible keyboard/focus handling and safe retry feedback; `/admin/property-claims` | Typed `PropertyClaimService` requester/admin DTO client with normalized payload and stable 400/409/429 handling | Authenticated `POST /api/properties/{id}/claim` accepts only `EMAIL`, `PHONE` or `BUSINESS_LICENSE`, trims bounded data/note and returns an explicit DTO; configurable persisted-request quota returns `PROPERTY_CLAIM_RATE_LIMITED` plus `Retry-After` | `property_claim_requests`; V73 adds the requester/created-time lookup index without rewriting claim data | Authenticated requester; admin list/approve/reject retain `PROPERTY_CLAIM_VIEW`/`PROPERTY_CLAIM_APPROVE` or `SUPER_ADMIN`; requester identity comes only from the principal | Backend compile, 27/27 focused tests and 11/11 clock/persistence regressions; Angular 14/14 and development build; evidence: `docs/testing/evidence/007/remediation/T237-imported-property-claim-request.md` | `COMPLETE_VERIFIED` | P1 | Preserve server-side normalization, strict expiration boundary and safe DTO contract. Evidence-file upload remains N/A until an approved storage, malware-scanning, retention and access policy exists; concurrency is tracked by PROP-SUB-014. |
| PROP-SUB-011 | Claim approval and owner activation | Admin claim queue uses inline confirmation, same-row busy protection, safe conflict feedback and typed canonical response; refreshed owners receive property-targeted management links | Typed claim client verifies `ACTIVE/APPROVED/ACTIVE`; client layout derives owner navigation from the refreshed `PROPERTY_OWNER` role and active assignments only | `PropertyClaimService.approveClaim()` pessimistically locks the claim and delegates imported approval to the T234 state machine, which locks the exact imported property and single matching pending owner, rejects active/ambiguous ownership, grants the role, writes reviewer metadata and commits audit/in-app/outbox evidence atomically | Claim becomes `APPROVED`; property becomes `ACTIVE/APPROVED/ACTIVE`; exact OWNER mapping becomes primary `ACTIVE`; role, audit, notification and outbox roll back together on any persistence failure | `PROPERTY_CLAIM_APPROVE` or `SUPER_ADMIN`; requester identity and refreshed role come from authoritative persistence; pending/inactive mappings are excluded from active management context | Backend compile and 44/44 focused tests; Angular 8/8 and development build; evidence: `docs/testing/evidence/007/remediation/T238-claim-approval-owner-activation.md` | `COMPLETE_VERIFIED` | P1 | Preserve fail-closed behavior for multiple claimants or an existing active owner. No automatic replacement/deactivation policy is invented; lock-order and request/approval concurrency invariants remain PROP-SUB-014/T240. |
| PROP-SUB-012 | Claim rejection/cancellation | Admin uses an inline 10-500 character rejection editor with row busy/error states; requester can cancel the exact pending claim from durable registration status after reload | Typed admin reject and requester cancel clients verify canonical `REJECTED`/`CANCELLED` responses and refresh persisted status | Reject and cancel pessimistically lock the exact claim, allow only `PENDING`, require the exact pending OWNER mapping to deactivate, clean the owner role, and fail closed before claim mutation when the mapping is stale; requester cancellation uses a requester-scoped locked lookup | Claim and OWNER mapping remain as history; property lifecycle triplet is unchanged; registration status exposes nullable `claimId`/`claimStatus` and preserves the rejection reason | `PROPERTY_CLAIM_REJECT` or `SUPER_ADMIN` for rejection; cancellation identity comes only from the authenticated requester and foreign/missing claim ids are indistinguishable | Backend production compile and 74/74 focused tests; frontend 24/24 focused tests; independent final diff review; evidence: `docs/testing/evidence/007/remediation/T239-claim-rejection-cancellation.md` | `COMPLETE_VERIFIED` | P1 | Preserve the requester-scoped lock and fail-closed mapping cleanup. Cross-transition concurrency and database uniqueness remain PROP-SUB-014/T240. |
| PROP-SUB-013 | Claim response privacy and serialization | Claim/admin tables consume explicit property/requester summaries | Typed `PropertyClaimResponse` contract | Controller returns `PropertyClaimResponseDTO`; service maps inside the transaction, `User.passwordHash` is write-only and claim JPA relationships are ignored defensively | Only approved property/user summary fields are serialized; password hashes, roles, subscriptions and tenant relationship graphs are excluded | Authenticated requester/admin claim permissions | Backend claim/service/privacy/controller tests 16/16 and Angular safe-contract test 1/1 passed; evidence: `docs/testing/evidence/007/remediation/T196-claim-response-privacy.md` | `COMPLETE_VERIFIED` | P0 | Preserve DTO-only responses and add fields only through explicit privacy review. |
| PROP-SUB-014 | Claim uniqueness and concurrency | Request/admin clients preserve synchronous busy guards and show stable, safe conflict feedback for duplicate or stale cross-tab actions | Typed claim client maps `PROPERTY_CLAIM_CONFLICT` and `PROPERTY_CLAIM_NOT_PENDING` without exposing persistence details | Request, approve, reject and cancel use one deterministic `property -> claim -> OWNER mapping` pessimistic lock order; conflicts flush inside the transaction and return a claim-specific 409 | V74 fail-closed migration adds pending claim uniqueness per property/requester, one OWNER mapping per property/account across states, and one primary active owner per property; it performs schema and duplicate preflight, exact metadata verification, and never rewrites existing rows | Authenticated requester or existing claim admin permissions; requester-scoped claim lookup preserves foreign/missing non-enumeration | Backend production compile and 41/41 focused tests including Spring/JPA races 3/3; disposable SQL Server clean/repeat/duplicate-fail-closed/checksum verification; frontend 21/21; evidence: `docs/testing/evidence/007/remediation/T240-claim-uniqueness-concurrency.md` | `COMPLETE_VERIFIED` | P1 | Rollout must stop if V74 duplicate preflight throws; never auto-deduplicate. The primary index permits future co-owners while keeping a single primary active owner; T241 policy remains a separate stop gate. |
| PROP-SUB-015 | Ownership transfer, co-owner, relinquish and removal | Property ownership management and invitation acceptance routes cover invite/cancel/accept, transfer initiate/cancel/accept, co-owner leave and primary removal with safe conflict feedback | Typed ownership client never treats UI state as authority, clears re-authentication passwords, enforces reasons/cooling and handles unavailable financial readiness truthfully | Hotel-scoped governance uses deterministic locks, hashed one-time invitation tokens, after-commit deliver-or-cancel email, password re-authentication, atomic primary swap, soft membership history, role/session refresh, tenant audit and notifications | V87 adds invitation/transfer lifecycle plus membership history fields and constraints; subscription order/entitlement/term/invoice snapshots are never mutated by ownership transitions | Primary-only owner governance; verified exact-email acceptance; authenticated recipient transfer acceptance; foreign resources are hidden; default production transfer fails closed until overdue-invoice/dispute sources are authoritative | Backend production compile and 16/16 focused tests; frontend 13/13; independent final review; evidence: `docs/testing/evidence/007/remediation/T241-ownership-lifecycle.md` | `COMPLETE_VERIFIED` | P1 | Necessary-only scope defers decline/resend, BILLING_ADMIN administration, hotel close/delete and Super Admin debt override. Financial readiness gateway supports READY/BLOCKED/UNAVAILABLE; production remains safely unavailable for unsupported invoice/dispute checks until T242/T243 wiring. |
| PROP-SUB-016 | Authoritative platform plan catalog and order snapshot | `/management/billing` | `PlatformBillingService.getCatalog()` | `/api/platform/subscription-plans`; `SubscriptionOrderService` snapshots plan version/price/duration/features | `subscription_plans`, platform orders and snapshots | Authenticated owner/representative with property access | Platform evidence plus fresh platform service/UI tests | `COMPLETE_VERIFIED` | P1 | Preserve server-owned snapshots and keep catalog output free of merchant secrets. |
| PROP-SUB-017 | Platform purchase, renewal and approved upgrade | `/management/billing` and payment panel | Platform billing typed client | Platform order/attempt/callback/application, renewal and upgrade services | Platform order, transaction, contract, entitlement and history tables | Property owner and context-specific permissions | Existing T093-T110 browser/evidence plus fresh platform tests passed | `COMPLETE_VERIFIED` | P1 | Keep regression coverage when the management entitlement bridge is replaced; completion here is bounded-context-only. |
| PROP-SUB-018 | Downgrade and proration policy | Billing UI displays truthful policy blocker | `getPolicyAvailability()` | `SubscriptionPolicyService` returns `POLICY_NOT_CONFIGURED` without mutation | No downgrade/credit mutation | Authenticated owner with property access | Policy service 4/4 and prior negative journey evidence | `BLOCKED_EXTERNAL` | P1 | Obtain approved versioned downgrade/proration/accounting rules before implementing; keep simulator/error contract and zero-mutation tests. |
| PROP-SUB-019 | Legacy current subscription/catalog/features/usage APIs | Admin catalog and management feature guards use selected-property context, authoritative source labels and safe unavailable/upgrade states | Typed `SubscriptionService` calls compatibility routes with required `targetHotelId`; no user-wide current/features/usage assumptions remain | Public catalog returns privacy DTOs; `/me`, `/me/features`, `/me/usage` enforce assigned-hotel non-enumerable access and delegate platform-first entitlement plus the same authoritative usage calculator used by upgrade validation | Platform entitlement/catalog/usage is authoritative; only unambiguous `LEGACY_PROJECTION` compatibility is exposed and internal legacy subscription IDs are redacted | Catalog public; current/features/usage require authenticated assigned property, including historical reads for suspended properties | Backend production compile and 18/18 focused tests; frontend 15/15; independent no-blocker review; evidence: `docs/testing/evidence/007/remediation/T242-legacy-subscription-compatibility.md` | `COMPLETE_VERIFIED` | P1 | Catalog mutation/versioning remains PROP-SUB-024/T244; expiry/revoke/history remains PROP-SUB-022/T243. Preserve shared usage ownership and never reintroduce user-wide aggregation. |
| PROP-SUB-020 | Entitlement source-of-truth bridge | Property billing card and entitlement endpoint | `PlatformBillingService.getEntitlement()` | `PropertySubscriptionEntitlementService` reads `platform_subscription_entitlements` first and creates an idempotent, audited legacy projection only for an unambiguous owner/property scope | Platform entitlement plus unique `legacy_subscription_entitlement_projections` compatibility read model | Owner/property access and platform billing view | `PropertySubscriptionEntitlementServiceTest` 4/4, `PlatformBillingControllerTest` 3/3, Angular billing 4/4; evidence: `docs/testing/evidence/007/remediation/T197-entitlement-source-bridge.md` | `COMPLETE_VERIFIED` | P0 | Preserve platform-first precedence; finish per-property mutation gates in PROP-SUB-021. |
| PROP-SUB-021 | Per-property plan limits and feature gates | Management dashboard, room and room-type mutations | `ManagementApiService` and selected-property context | `SubscriptionFeatureService` resolves the target property's locked platform entitlement; room, room-type, bulk-room and image mutations count only that property | Platform entitlement/projection plus property-owned room/image rows | Authenticated owner/property role | Backend property/gate suite 22/22 and Angular dashboard 2/2; evidence: `docs/testing/evidence/007/remediation/T198-property-entitlement-gates.md` | `COMPLETE_VERIFIED` | P0 | Extend the same property-scoped quota contract to staff mutations in PROP-OPS-005. |
| PROP-SUB-022 | Subscription expiry and explicit revoke | UI can display dates/status but has no lifecycle action | No revoke call | Legacy scheduler scans all rows and is not tested; no platform contract/entitlement expiry or revoke service/endpoint was found | Legacy and platform status fields can remain stale | Missing/admin policy | No scheduler/revoke evidence | `MISSING` | P1 | Add clock-driven expiration and explicit audited revoke transitions for authoritative platform contract/entitlement/history; retain historical read/export access and test boundary times. |
| PROP-SUB-023 | Subscription-refund entitlement effect | Platform refund UI/service exists | Platform refund client | Refund service blocks without an approved `PlatformRefundEntitlementPolicy` | Platform refund, contract, entitlement and history | Platform refund permissions | Existing refund tests prove safe policy blocking | `BLOCKED_EXTERNAL` | P1 | Approve full/partial refund entitlement policy, implement a versioned policy adapter and test exact contract/entitlement/history effects without touching Property Commerce. |
| PROP-SUB-024 | Plan and feature administration/versioning | `/admin/plans` is read-only and falls back to a `mailto:` upgrade link | Legacy catalog read only | No admin plan/feature create/update/activate/version API found; seed initializers mutate catalog | `subscription_plans`, `subscription_features`, `plan_features` | Missing plan-admin mutation permission | No CRUD/version test | `MISSING` | P1 | Add governed versioned plan administration, prevent edits from changing existing order snapshots/contracts, validate limits/prices and audit activation/deactivation. |
| PROP-SUB-025 | Claim/subscription HTTP test harness | Not user-facing | Angular focused suite executes | Both MVC slices pin `BackendApplication`; the JPA tenant interceptor is isolated while real security filters and controller advice execute | MVC slice plus focused Mockito service lifecycle tests | Test-only | Fresh Maven runs passed 13/13 HTTP and 3/3 service assertions; evidence: `docs/testing/evidence/007/remediation/T185-claim-subscription-http.md` | `COMPLETE_VERIFIED` | P1 | Preserve the pinned context and tenant-interceptor isolation; claim privacy/state-machine gaps remain tracked by PROP-SUB-010 through PROP-SUB-013, while entitlement ownership remains PROP-SUB-019 through PROP-SUB-021. |

## Required Lifecycle Order

The remediation dependency order for T149 must start with:

1. Remove entity/credential exposure in PROP-SUB-013 and repair the HTTP harness in PROP-SUB-025.
2. Establish one property approval state machine and pending-owner model for PROP-SUB-003,
   PROP-SUB-005, PROP-SUB-006 and PROP-SUB-008.
3. Make platform entitlement the single management source for PROP-SUB-020 and PROP-SUB-021.
4. Repair registration and claim lifecycle behavior, then add suspension/transfer/history.
5. Add expiry/revoke/plan administration only after authoritative state ownership is stable.

No production merchant, real payment, automatic downgrade/proration or refund-entitlement policy
may be enabled by these remediation tasks without the separate approvals already required by
Feature 007.

Inventory count: 21 `COMPLETE_VERIFIED`, 0 `PARTIAL`, 0 `PLACEHOLDER`, 0 `BROKEN`,
2 `MISSING`, 2 `BLOCKED_EXTERNAL`, 0 `NOT_APPLICABLE`.
