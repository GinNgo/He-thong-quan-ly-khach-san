# Stay Lifecycle Inventory

Audit date: 2026-08-03
Scope: reservation changes and cancellation, no-show, physical-room assignment,
check-in, in-stay charges, settlement, checkout, invoices, housekeeping, reviews,
favorites and vouchers required by FR-043 and FR-044.

## Evidence Rules

- `COMPLETE_VERIFIED` requires fresh executable evidence for the complete reachable behavior.
- Source inspection or a browser journey with intercepted APIs is not promoted to complete.
- Severity is `P0` for tenant, inventory or financial-integrity risk, `P1` for
  release-significant behavior, and `P2` for product quality.
- Operational reservation state and financial state remain separate; checkout totals,
  payment success and refunds must be derived from authoritative server records.

## Fresh Verification

| Command / evidence | Result | Evidence impact |
|---|---|---|
| Focused backend lifecycle, reservation, folio, checkout and invoice unit suite | 63/63 passed across 10 classes | State-transition policy, customer cancellation ownership, service/surcharge rules, folio calculation, settlement gates, invoice finalization and checkout operation behavior are current at service/unit level. |
| Focused checkout balance/rollback, invoice access/immutability and financial-permission suite | 17/17 passed across 5 classes; `InvoiceAccessIntegrationTest` started a focused Spring MVC context successfully | Invoice customer/property access, immutable evidence, dedicated financial permissions and fail-closed checkout ordering have fresh automated evidence. The rollback tests use mocked repositories rather than a real database transaction. |
| Focused Angular profile, reservation management, checkout workspace and invoice/property-checkout clients | 20/20 passed across 5 spec files | Customer cancellation/refund display, admin status display, checkout settlement controls and typed invoice/checkout request shapes are current at component/client level. |
| `npx playwright test e2e/stay-checkout-invoice.spec.ts --project=chromium` | 1/1 passed | Check-in, service, multiple-payment, checkout, invoice and dirty-room presentation are connected through the UI, but the test intercepts every API call and is not backend/database completion evidence. |
| Source/API/database trace | Reservation controllers/services, legacy and authoritative payment/charge paths, invoice/checkout packages, housekeeping entities/endpoints and Angular routes/components were inspected | Source-only rows remain partial, placeholder, broken or missing; T207 and the existing checkout lifecycle now have isolated database-backed evidence. |
| T207 assignment concurrency evidence | H2 concurrency and deterministic-lock tests passed 1/1 and 4/4; isolated SQL Server lifecycle assignment/check-in/checkout/replay/rollback test passed 2/2; V41 migration adds date snapshot/index/active-assignment uniqueness guard | Physical-room assignment now uses reservation, assignment and candidate-room locks in deterministic order; date snapshots make the database guard enforceable. UI assignment remains a separate STAY-008/T301 scope. |
| T208 lifecycle authorization evidence | Backend permission/delegation matrix 5/5, cross-property fail-closed matrix 4/4 and reservation regression 15/15 passed; Angular permission/client suites 3/3 passed | Assignment, check-in, operational cancellation and no-show now have separate permission functions and dedicated endpoints; the generic status endpoint rejects those sensitive targets. |
| T209 legacy service-charge reconciliation evidence | Backend focused suite 30/30; Angular authoritative-service/lifecycle suite 10/10; isolated SQL Server V43 fixture applied twice and verified one reconciled row totaling 150,000 VND | The legacy mutation is retired with `410 Gone`, the admin action opens the authoritative checkout workspace, active legacy rows are backfilled with a stable identity, and folio calculation skips only rows represented by a reconciled append-only charge line. |

## Capability Inventory

| ID | Capability | Route / UI | Frontend service | Backend API / service | Database | Permission | Tests / evidence | Status | Severity | Required remediation |
|---|---|---|---|---|---|---|---|---|---|---|
| STAY-001 | Reservation list, detail and operational status visibility | `/admin/reservations`, customer `/profile?tab=bookings`, reservation timeline component | `ReservationService`, `ClientApiService` | `GET /api/reservations`, `/{id}`, `/my-bookings`; DTO includes payment/refund summaries | Reservations, details, assignments, payments and refunds | Customer ownership or reservation view/broad operational roles | Admin/profile component tests passed; no successful role-based live API journey | `PARTIAL` | P1 | Add real seeded customer/receptionist/manager list-detail journeys, stable pagination/filtering, event history and explicit loading/error/empty evidence. |
| STAY-002 | Change dates, guests, room quantity or room type after booking | No reachable edit/reschedule UI | None | No modification, re-quote or reschedule command/API found | No change request, price-delta or policy snapshot | Missing | No source/test | `MISSING` | P1 | Define allowed states, availability re-locking, re-quote/price-delta/refund rules and immutable change history before adding customer/staff UI. |
| STAY-003 | Customer-owned booking cancellation | Customer booking card cancel action | `cancelMyReservation()` | `POST /api/reservations/{id}/cancel`; locks booking, checks owner/state, requests refunds, releases rooms and holds | Reservation, assignments, holds and refund requests | `CUSTOMER` plus resource ownership | Four reservation cancellation unit cases and profile UI tests passed | `PARTIAL` | P1 | Add reason/confirmation, HTTP/IDOR/concurrency tests, provider-failure recovery and a real browser cancellation-to-refund journey. |
| STAY-004 | Cancellation window, fee and snapshotted policy | Customer cancellation has no terms/fee preview | None | No versioned cancellation policy, deadline, fee or policy snapshot | Missing | Missing | Source trace | `MISSING` | P1 | Introduce versioned property cancellation policy and booking snapshot, calculate refundable/fee amounts server-side and display the decision before confirmation. |
| STAY-005 | No-show transition and consequences | No no-show action in the reachable admin screen | Generic status client could call `NO_SHOW` directly | Lifecycle allows `CONFIRMED -> NO_SHOW`; generic status endpoint has no arrival-time validation, reason, actor evidence or refund/room-release policy | Reservation status and hold release only | Broad operational roles | Policy unit test covers the state edge only | `BROKEN` | P1 | Add a reasoned no-show command available only after the approved arrival threshold, define financial/inventory effects, dedicated permission, audit and positive/early/replay tests. |
| STAY-006 | Available physical-room lookup for a booking | No current room-picker screen | No typed assignment client | `GET /api/reservations/{id}/available-rooms` filters type, date, property and non-bookable room states | Rooms, types, reservations and assignments | Broad operational roles | Source trace; no HTTP/UI result matrix | `PARTIAL` | P1 | Build the room-picker UI/client and test all room, housekeeping, maintenance and overlapping-assignment states with multi-room quantities. |
| STAY-007 | Concurrent-safe physical-room assignment | No reachable assignment flow | None | `assignRooms()` locks the reservation, active assignment rows and sorted candidate rooms before validating state and persisting assignments; replay with the same room set is idempotent | `reservation_rooms` stores snapshotted stay dates; V41 adds a room/date index and filtered unique active-assignment guard | Broad operational roles | H2 concurrent assignment 1/1, deterministic locking 4/4, SQL Server lifecycle 2/2; evidence: `T207-physical-room-assignment.md` | `COMPLETE_VERIFIED` | P0 | Preserve lock ordering and the active-room/date guard; assignment/reassignment UI remains STAY-008/T301 scope. |
| STAY-008 | Assignment, reassignment and release UI | Reservation table has no assign/reassign control | None | Duplicate PUT/POST assignment APIs exist; no explicit reassignment/release command | Assignment status supports `ASSIGNED`/`RELEASED` | Missing UI; broad backend role gate | No UI/test | `MISSING` | P1 | Add permission-aware assignment UI with availability refresh, conflict recovery and explicit release/reassign history after STAY-007 is fixed. |
| STAY-009 | Check-in readiness and execution | Admin reservation row exposes check-in directly | Generic `updateReservationStatus()` | `POST /check-in` and generic status update require all physical rooms assigned, then mark them occupied | Reservation and assigned rooms | Broad roles rather than a dedicated check-in action | Lifecycle and mocked Playwright path passed; normal UI cannot assign rooms first | `BROKEN` | P1 | Connect check-in to the assignment workflow, show missing-room/readiness errors, validate arrival policy and add dedicated permission plus real API/browser tests. |
| STAY-010 | Staff-created/manual reservation | `/admin/reservations/create` | `createReservation()` | UI sends `userId` and `details[].roomId`, while `ReservationRequest` requires `roomTypeId`, quantity and authenticated username ownership | Reservation creation uses room type and current principal | `RESERVATION:CREATE` | Source contract mismatch; no component/API test | `BROKEN` | P1 | Replace the legacy form with a staff-booking contract that selects customer, quote and room type, uses server price/availability and assigns rooms only through the locked assignment workflow. |
| STAY-011 | Action-level lifecycle authorization | Reservation actions are shown only when the stored mask grants the exact command | Dedicated reservation client commands for assign/readiness, check-in, operational cancellation and no-show | Controller uses `RESERVATION_ASSIGNMENT`, `CHECKIN`, `RESERVATION_CANCEL` and `RESERVATION_NO_SHOW`; generic status rejects sensitive transitions and legacy checkout is permission-aligned | Reservation service still applies property access before every lifecycle mutation; V42 seeds distinct permission functions/default role masks | Exact function/action masks with tenant not-found semantics; super admin bypass remains explicit | Backend action matrix 5/5, property IDOR 4/4, regression 15/15 and Angular 3/3; evidence: `T208-lifecycle-action-authorization.md` | `COMPLETE_VERIFIED` | P0 | Preserve dedicated endpoints/masks; STAY-008/T301 remains the assignment UI/history scope and STAY-005/T299 remains no-show timing/consequence policy. |
| STAY-012 | Legacy in-stay service charge | Admin reservation list opens the authoritative checkout workspace; no legacy add-service dialog | `PropertyCheckoutService.addServiceCharge()` | `POST /api/management/reservations/{id}/charges/services` creates the append-only line; legacy `POST /api/reservations/{id}/services` is retired with `410 Gone` | `reservation_charge_lines.legacy_service_item_id` links V43-backfilled rows to `reservation_services`; filtered unique guard prevents duplicate reconciliation | `RESERVATION_SERVICE:CREATE` plus property access | Backend 30/30, Angular 10/10, SQL Server V43 repeat/backfill 1 row and 150,000 VND; evidence: `T209-legacy-service-charge.md` | `COMPLETE_VERIFIED` | P0 | Preserve the append-only legacy link and no-double-count rule; STAY-013 continues tracking HTTP/idempotency and expanded minibar policy coverage. |
| STAY-013 | Authoritative service and minibar charges | Checkout workspace service form | `PropertyCheckoutService.addServiceCharge()` | `POST /api/management/reservations/{id}/charges/services`; server-priced snapshot, usage time and append-only correction support | `reservation_charge_lines`, service catalog snapshots | `RESERVATION_SERVICE:CREATE` plus property access | Service tests 5/5, Angular checkout/client tests and intercepted Playwright service action passed | `PARTIAL` | P1 | Add HTTP/tenant/idempotency/database tests, explicit minibar catalog/usage UI and migrate every consumer away from legacy service items. |
| STAY-014 | Surcharge and negative adjustment | Checkout workspace surcharge form | `addSurcharge()` / `addNegativeAdjustment()` | Typed surcharge and permissioned negative adjustment services | Append-only charge lines and financial audit events | `RESERVATION_SURCHARGE:CREATE`; negative adjustment mask checked in service | Surcharge tests 5/5 and checkout component tests passed | `PARTIAL` | P1 | Add controller/tenant/concurrent duplicate tests, localized reason/type policy and visible audit history for approved adjustments. |
| STAY-015 | Legacy direct payment mutation | Admin reservation management opens the authoritative folio workspace | Property payment attempts and manual confirmation; `PaymentService.processPayment()` removed | `POST /api/payments` returns `410 Gone`; authoritative attempts derive amount/method from server-owned folio and require provider callback or permissioned manual confirmation | Legacy `payments` remain read-only evidence; V44 preserves source reference/status and marks unverified rows for reconciliation | `FINANCE:CREATE` is retained only to receive the retirement response; settlement permissions live on property payment commands | Backend 44/44, Angular 11/11, SQL Server V44 fixture: provider-backed row accepted (`0`), direct CASH/refund rows quarantined (`1`), exceptions recorded; evidence: `T210-legacy-direct-payment.md` | `COMPLETE_VERIFIED` | P0 | Keep legacy rows read-only and resolve quarantined rows through an approved manual reconciliation workflow. |
| STAY-016 | Booking financial summary during the stay | Checkout/payment/customer booking summaries | Property payment and checkout clients | `BookingFinancialSummaryService` derives charges, payments, refunds, balance and financial state | Authoritative ledger/charges plus legacy compatibility reads | Resource owner or authorized property role | Folio/checkout units pass; no complete API reconciliation against database fixtures | `PARTIAL` | P1 | Add one-VND database reconciliation across deposits, multiple payments, refunds, services, surcharges and legacy compatibility rows. |
| STAY-017 | Authoritative checkout preview | Checkout workspace | `PropertyCheckoutService.preview()` | Recomputes server folio and returns settled/outstanding/overpaid gate | Charge lines, ledger, refunds and reservation snapshots | `CHECKOUT:VIEW` | Preview 5/5, balance 3/3 and Angular request/gate tests passed | `PARTIAL` | P1 | Add HTTP/tenant tests and SQL-backed reconciliation with concurrent payment/refund/charge changes between preview and checkout. |
| STAY-018 | Debt override and overpayment handling | Checkout workspace supports reasoned debt override; overpayment stays blocked | `authorizeDebtOverride()` | Locks/rechecks folio, requires separate permission and records immutable override/audit; no approved overpayment resolution | Checkout overrides and audit events | `RESERVATION_DEBT_OVERRIDE:APPROVE` | Override 7/7 and balance tests 3/3 passed | `PARTIAL` | P1 | Add approval policy/limits, HTTP/tenant/concurrency tests and implement an approved overpayment refund/credit policy before enabling that case. |
| STAY-019 | Atomic checkout and rollback | Checkout workspace | `checkout()` sends only server override id | Reservation service locks booking, finalizes invoice, releases assignments, marks rooms dirty and creates housekeeping tasks in one transaction | Reservation, invoice/lines/allocations, rooms, assignments and tasks | `CHECKOUT:CREATE` | Transaction units 5/5, fail-closed ordering 3/3 and SQL Server aggregate rollback/concurrent replay 2/2 passed; evidence: `docs/testing/evidence/007/remediation/T211-atomic-checkout-sqlserver.md` | `COMPLETE_VERIFIED` | P0 | Preserve the SQL Server constraint-failure and replay suite when checkout persistence or uniqueness contracts change. |
| STAY-020 | Final invoice view, PDF and email | `/my-invoices`; admin invoice screens | `InvoiceService` | Customer/property-authorized invoice detail, PDF and email endpoints render finalized snapshots | Immutable invoices, lines, allocations and credit notes | Customer owner or property access; adjustment is separately permissioned | Invoice HTTP access 6/6, immutability 3/3 and Angular client tests 4/4 passed | `PARTIAL` | P1 | Add seeded PDF/content checksum, sandbox email delivery/outbox evidence, localization and complete customer/admin browser journeys. |
| STAY-021 | Post-finalization credit notes | Customer invoice detail displays credit notes; management client can create them | Property checkout/invoice clients | Permissioned credit-note service appends corrections without rewriting invoice | Credit notes and lines | `INVOICE_ADJUST:APPROVE` | Model/service tests exist; no fresh HTTP/UI creation journey | `PARTIAL` | P1 | Add approval/reason limits, tenant/IDOR/concurrency tests and a management creation/history UI with refreshed invoice reconciliation. |
| STAY-022 | Checkout creates one dirty-room housekeeping effect | Checkout result and management dashboard counts | Checkout client | `CheckoutOperationsService` uses a tenant-scoped checkout effect key, releases assignments, marks rooms dirty and creates at most one task per room | Rooms, assignments, housekeeping tasks and unique effect index | Checkout permission | Fresh `CheckoutOperationsServiceTest` 6/6 and intercepted Playwright dirty-room assertion passed | `COMPLETE_VERIFIED` | P1 | Preserve the exactly-once and rollback assertions while completing the staff queue lifecycle. |
| STAY-023 | Housekeeping queue, assignment and start | Dashboard exposes pending count only | No housekeeping task client or route | Repository supports pending lookup; no list/assign/start endpoint found | Task has assignee and assigned timestamp fields | Missing; `HOUSEKEEPING` role is not admitted by management controller | Source trace | `MISSING` | P1 | Add dedicated property-scoped queue/list/claim/assign/start APIs, housekeeping-role UI, stale assignment handling and own/cross-property tests. |
| STAY-024 | Housekeeping completion and room release | No reachable task list; API usable only with a known task id | None | `POST /api/management/housekeeping/{taskId}/complete` checks tenant/state and marks room clean/available unless under maintenance | Task and room update transaction | Broad management roles; no dedicated completion permission | Source path plus existing tenant tests; not included in the fresh passing suite | `PARTIAL` | P1 | Add task UI, dedicated permission/assignee rules, optimistic locking/idempotent replay and fresh tenant HTTP/browser evidence. |
| STAY-025 | Verified post-stay reviews and rating aggregation | Search/detail show imported/static rating values only | None | No review entity, create/moderate/respond API or verified-stay check | Hotel average/count fields only | Missing | Source trace | `MISSING` | P1 | Add one-review-per-completed-stay rules, moderation/response, aggregate consistency, abuse controls and localized customer/property UI. |
| STAY-026 | Favorites/wishlist | No favorite action or saved-stays route | None | No favorite entity/API/service | Missing | Customer ownership missing | No source/test | `MISSING` | P1 | Add owner-bound idempotent favorite records, list/remove APIs, card/detail controls, empty/loading states and tenant/public eligibility tests. |
| STAY-027 | Voucher/promotion application and redemption | Home has promotional/member cards and a static `PromotionsComponent` that can copy supplied codes | No quote/booking voucher client | No promotion catalog, eligibility, stacking, validation, reservation snapshot or redemption service | Missing | Missing | Display/copy source only; no financial effect | `PLACEHOLDER` | P1 | Implement approved promotion/membership/sponsored-placement policies, backend quote consistency, stacking/redemption limits and admin/customer lifecycle UI after product rules are approved. |
| STAY-028 | End-to-end stay journey through the UI | Admin reservations/checkout plus customer invoices | Reservation, payment-attempt, checkout and invoice clients | All expected APIs can be represented by the browser fixture | Browser state only in this test | Mocked role/session | Playwright 1/1 passed with all `/api/**` calls intercepted | `PARTIAL` | P1 | Run the same journey against the real backend and deterministic SQL Server fixture, including assignment, negative permission, timeout, retry and rollback cases. |
| STAY-029 | Stay lifecycle concurrency and executable database evidence | Not user-facing | Unit/component suites execute | Assignment now locks the reservation, active assignment rows and candidate rooms in deterministic order; check-in locks assigned rows and rooms; checkout keeps locked replay-safe housekeeping effects | `reservation_rooms`, `rooms`, `housekeeping_tasks` with real SQL Server transaction boundaries | Test-only | Locking unit 3/3, H2 persistence rollback/replay 2/2 and isolated SQL Server assignment/check-in/checkout/replay/rollback 2/2 passed; evidence: `docs/testing/evidence/007/remediation/T192-stay-lifecycle-sqlserver.md` | `COMPLETE_VERIFIED` | P0 | Preserve the SQL Server lifecycle harness; full invoice-and-operations rollback is now covered separately by T211/STAY-019. |

## Required Remediation Order

T149 must order stay-lifecycle remediation as follows:

1. Disable the client-authoritative legacy payment and service mutations in STAY-015
   and STAY-012, then reconcile their data into the authoritative folio without
   duplicating money.
2. Preserve the closed P0 assignment, permission and checkout-aggregate boundaries in
   STAY-007, STAY-011 and STAY-019 together with the STAY-029 SQL Server evidence.
3. Implement assignment UI and repair manual booking/check-in before expanding
   cancellation/no-show or housekeeping workflow UI.
4. Add cancellation/change policies and immutable lifecycle audit, then complete
   housekeeping queue, reviews and favorites.
5. Keep promotion/voucher work blocked on approved commercial policy, and only
   promote end-to-end rows after real backend, SQL Server and role-based browser evidence.

No real-money provider action, production credential, destructive financial cleanup or
invented cancellation/promotion policy is authorized by these remediation tasks.

Inventory count: 1 `COMPLETE_VERIFIED`, 13 `PARTIAL`, 1 `PLACEHOLDER`, 8 `BROKEN`,
6 `MISSING`, 0 `BLOCKED_EXTERNAL`, 0 `NOT_APPLICABLE`.
