# Public Booking Inventory

Audit date: 2026-08-02
Scope: home search, public discovery, autocomplete, current administrative locations,
landmarks, filters, sort, pagination, property details, availability, capacity,
pricing, authenticated/public booking and same-type or mixed-type multi-room behavior
required by FR-043 and FR-044.

## Evidence Rules

- `COMPLETE_VERIFIED` requires fresh executable evidence for the complete reachable behavior.
- Source inspection without a successful API/browser journey is not promoted to complete.
- Severity is `P0` for booking/inventory integrity or externally sellable inactive inventory,
  `P1` for release-significant behavior and `P2` for product quality or performance.
- Search, detail and booking must use the same property, room-type, room-state, date,
  capacity, price and payment-readiness rules.

## Fresh Verification

| Command / evidence | Result | Evidence impact |
|---|---|---|
| T187 public HTTP suite for `PublicDiscoveryControllerIntegrationTest` and `PropertySearchControllerIntegrationTest` | 11/11 passed against the real `BackendApplication` with deterministic province, legacy-alias, landmark, property, room-type and room fixtures. | Grouped discovery, current-province projection, approved-property search, authoritative stay pricing, landmark distance ordering and invalid-landmark rejection are freshly HTTP verified. |
| T187 concurrency suite for `ReservationConcurrencyIntegrationTest` and `ReservationHoldIntegrationTest` | 3/3 passed against persisted H2 fixtures: one of two simultaneous last-room requests succeeds, while hold expiry and concurrent scans complete exactly once. | The runnable Spring harness, pessimistic room-type lock and persisted hold-expiry boundary are current; provider/expiry races, restart recovery and SQL Server behavior remain open. |
| Focused Angular home, date, discovery, search, filter, detail and checkout suite | 37/37 tests passed across 10 public-booking spec files. | Date validation, local-date serialization, landmark URL recovery, filter emission, detail error recovery, checkout submit suppression and payment-attempt retry behavior are current at component/unit level. |
| In-app browser at `http://localhost:4200/` | Home rendered; the date dialog opened and closed without hanging; Home to Search navigation worked; the HOTEL filter produced `propertyTypes=HOTEL`; price sort produced `sortBy=PRICE_ASC`; an unauthenticated booking route rendered login. Autocomplete, popular destinations, featured properties and search results rendered recoverable connection-error states because the API was unavailable. | Public shell and URL-state behavior is reachable. No successful live API-backed search-to-detail-to-booking journey is available as completion evidence. |
| Source/API/database trace | Angular routes/components/services, public controllers, search SQL, room availability, reservation locking/holds and booking DTOs were inspected. | Source-only rows remain partial, placeholder, broken or missing. |

## Capability Inventory

| ID | Capability | Route / UI | Frontend service | Backend API / service | Database | Permission | Tests / evidence | Status | Severity | Required remediation |
|---|---|---|---|---|---|---|---|---|---|---|
| PUB-001 | Home destination/date/guest/property-type search | `/`; hero and sticky search | `HomeSearchStateService`, `ClientApiService` | Search state serializes to `/api/public/properties/search` | None before submit | Public | Date dialog did not hang; local-date/state tests passed; live API unavailable | `PARTIAL` | P1 | Add a successful API-backed browser journey, retain entered state through login/detail/back navigation, and prove validation messages, keyboard focus and mobile behavior. |
| PUB-002 | Day-use search and booking | Home displays day use as coming soon, while state code can still serialize `stayType=DAY_USE` without checkout | `HomeSearchStateService` | `PropertySearchRequestDTO.stayType` is ignored; search rejects a check-in without checkout; reservation requires an overnight range | Reservation dates have no day-use contract | Public | State unit tests pass only preparatory serialization; no backend path | `PLACEHOLDER` | P1 | Either remove unreachable day-use request generation or define day-use hours, inventory overlap, capacity, price, checkout and booking contracts before enabling it. |
| PUB-003 | Grouped autocomplete for province, ward, property and landmark | Home location dialog | `getSearchSuggestions()` with debounce, cancellation, retry and keyboard navigation | `GET /api/public/search/suggestions`; `PublicSearchSuggestionService` | `locations`, `hotels`, `property_images` | Public | Six discovery HTTP tests passed for accents, property/address, minimum length and duplicate landmark names; browser success path remains open | `PARTIAL` | P1 | Add ordering/deduplication/limit tests, verify keyboard selection and execute successful accent/no-accent, property and landmark browser journeys. |
| PUB-004 | Current 34-province, ward and landmark compatibility | Autocomplete and landmark search URL | Home search state | `LocationController`, `ProvinceCompatibilityService`, landmark validation/radius | `locations`, province compatibility data and landmark imports | Public | Current-province projection and duplicate-landmark HTTP cases passed; nationwide imported-data integrity and browser coverage remain open | `PARTIAL` | P1 | Execute imported-data integrity and browser tests for all current provinces, legacy aliases, invalid coordinates and radius/province mismatch. |
| PUB-005 | Popular destinations and featured properties | Home discovery sections | `getPopularDestinations()` and featured-property search | Public discovery and property search APIs | Current locations and approved properties | Public | Angular empty/error states passed; live sections showed connection errors | `PARTIAL` | P2 | Add a successful seeded browser run, deterministic ranking, image provenance/fallback checks and cache invalidation or freshness rules. |
| PUB-006 | Primary public property search and eligibility | `/search`; `GET /api/public/properties/search` | `searchHotels()` | `PropertySearchServiceImpl` filters approved and operationally active properties and production demo data | `hotels`, `room_types`, `rooms`, reservations and locations | Public | Five search HTTP tests passed for approved results, current-province compatibility, stay pricing, landmark distance and invalid landmark rejection | `PARTIAL` | P1 | Add suspension, demo visibility, invalid-date and remaining accepted-query coverage, then execute a successful live browser journey. |
| PUB-007 | Legacy public hotel search compatibility | Reachable `GET /api/v1/hotels/public/search`; no current UI consumer | Legacy-compatible client surface | `HotelController.searchHotels()` checks only legacy `status=ACTIVE`, filters some fields in memory and returns `Hotel` entities | `hotels`, room types | Public | Source trace | `BROKEN` | P1 | Retire or delegate this endpoint to the canonical search contract; never return entities or expose unapproved/suspended inventory through divergent legacy status rules. |
| PUB-008 | Property type, star and review filters | Desktop sidebar and mobile drawer | Search URL state and `SearchFilterSidebarComponent` | Native search applies type, star and minimum review score | Hotel classification/rating fields | Public | Filter component tests passed; HOTEL URL chip verified in browser; HTTP not executed | `PARTIAL` | P1 | Add API result assertions, zero/null rating rules, mobile drawer focus handling and filter-count/chip/pagination regression coverage. |
| PUB-009 | Amenity, cancellation, pay-at-property and breakfast filters/badges | No reachable controls or meaningful badges | `amenityIds` is always empty in the sidebar | Request DTO exposes fields, but search ignores them and responses hard-code empty amenities/badges and all policy flags false | Amenity and versioned booking-policy models are absent | Public | Source trace | `PLACEHOLDER` | P1 | Implement canonical amenity/policy data and assignments, indexed query predicates, property management, localized display and filter-to-booking snapshot tests. |
| PUB-010 | Minimum/maximum price filtering | Price range slider | Search query params | Separate `EXISTS` clauses allow different room types to satisfy min and max, while the displayed cheapest room can fall outside the requested range | Active room-type base prices | Public | Slider emission unit passed; source confirms SQL defect | `BROKEN` | P1 | Apply both bounds to the same eligible room type used for availability and displayed pricing; add boundary, null-price, multi-room-type and sort consistency tests. |
| PUB-011 | Sort and one-based pagination | Sort select and paginator | URL-backed state | `POPULAR`, `PRICE_ASC`, `PRICE_DESC`, `RATING`, `NEAREST`; page is clamped to 1+, size to 1..100 | Native query/count query | Public | `sortBy=PRICE_ASC` verified in browser; landmark sort unit passed; no data-backed pagination execution | `PARTIAL` | P2 | Add stable tie-breakers to every order, out-of-range page behavior, count parity and browser coverage across filter/sort/page changes. |
| PUB-012 | Search/detail availability consistency | Search cards and detail room list | Search and room-type clients | Search SQL excludes only maintenance/out-of-service-style states, while `RoomAvailabilityService` also excludes dirty, cleaning and occupied rooms | Physical rooms, reservation details and assignments | Public | Source trace; availability HTTP/concurrency suites blocked | `BROKEN` | P1 | Use one authoritative bookable-room predicate/projection for search, detail and reservation creation, then test every room/housekeeping/maintenance state and overlapping reservation status. |
| PUB-013 | Adult, child and total capacity | Guest selector, search results and detail room choice | Search params and detail capacity guard | Search and booking validate separate adult/child/total capacity; public room-type detail receives only total guests | Room-type capacity fields | Public/authenticated booking | State/checkout units passed; no API capacity matrix | `PARTIAL` | P1 | Pass adults, children and quantity to detail availability, normalize null/zero legacy capacities and execute boundary plus mixed-age capacity tests end to end. |
| PUB-014 | Search and detail price quote | Search cards, room cards and booking summary | Client displays API pricing but also carries URL estimates | Search chooses the lowest available room type and applies a fixed 15% multiplier; detail returns room totals from the same helper | Room-type base price only; no versioned public tax/fee/discount policy | Public | Source trace and checkout units; no quote-consistency integration test | `PARTIAL` | P1 | Define versioned tax/fee/discount/occupancy pricing, return quote identity/expiry, and prove search-detail-checkout totals match or refresh truthfully after price/catalog changes. |
| PUB-015 | Public property detail eligibility | `/hotel/:id`; `GET /api/v1/hotels/public/{id}` | `getHotelById()` | `HotelManagementService.getHotelById()` returns any ID without approval/operation/demo eligibility checks | `hotels`, property images | Public | Invalid/not-found Angular tests passed; source confirms eligibility bypass | `BROKEN` | P1 | Resolve public detail through the canonical eligibility policy and return 404 for draft, rejected, suspended, closed or disallowed demo properties. |
| PUB-016 | Public room-type listing and sellable status | Detail room list; `GET /api/public/properties/{hotelId}/room-types` and legacy route | `getRoomTypesByHotel()` | `PublicInventoryEligibilityPolicy` requires an approved, operationally active public property, filters room types to `ACTIVE`, and the locked reservation path re-checks the same policy after `findByIdForUpdate` | `room_types`, rooms, reservations | Public read; authenticated booking | HTTP catalog/legacy stale-URL test 7/7, reservation/service/policy tests, deterministic deactivation race; [T204 evidence](../../../testing/evidence/007/remediation/T204-public-room-type-sellability.md) | `COMPLETE_VERIFIED` | P0 | Preserve the shared policy; PUB-015/T283 still owns public property detail eligibility and PUB-023/T205 owns booking-request idempotency. |
| PUB-017 | Complete public property content | Detail page shows hero, one image, description and rooms | Detail DTO/client | DTO has gallery URLs, but UI lacks gallery, amenities, policies, map, verified review detail and payment/cancellation facts | Several required models/assignments absent | Public | Source/UI trace | `MISSING` | P1 | Build a localized accessible gallery and sections for amenities, map/location, policies, verified reviews and booking/payment facts backed by canonical data. |
| PUB-018 | Detail loading, invalid route, not-found and recovery states | `/hotel/:id` | Detail component | Public detail and room-type endpoints | None | Public | 2/2 detail recovery tests passed; live successful detail unavailable | `PARTIAL` | P2 | Add cancellation-safe subscriptions, retry for room errors, stale-query refresh behavior and successful seeded desktop/mobile browser evidence. |
| PUB-019 | Authentication gate and return to checkout | `/booking/:roomTypeId` guarded by `clientAuthGuard` | Auth/router services | `POST /api/reservations/book` requires `CUSTOMER` | JWT and user records | Customer | Browser rendered login for an unauthenticated booking route; no completed return journey | `PARTIAL` | P1 | Verify encoded return URL, login/register recovery, expired session handling and preservation/revalidation of dates, quantity and room type after authentication. |
| PUB-020 | Checkout context and client-side tamper resistance | Booking form | `BookingCheckoutComponent` | Backend ignores URL price/name/hotel authority and reloads the locked room type, capacity and server price | Room type, hotel, reservation | Customer | Six checkout tests passed, including duplicate-submit suppression and route context | `PARTIAL` | P1 | Replace required display authority in URL with a backend quote/context fetch, reject stale/tampered context visibly and add reload/back-navigation browser tests. |
| PUB-021 | Property-configured payment methods and pay-at-property truth | Checkout hard-codes pay at hotel, MoMo, VNPay and ZaloPay | Property payment client after reservation creation | Reservation stores caller method; payment attempt validates later; search flags always say pay-at-property false; booking requires enabled property payment configuration even for pay-at-hotel | Property payment configuration, reservation and attempts | Customer | Payment-attempt component units pass; source contracts disagree | `BROKEN` | P1 | Fetch allowed/readiness methods before booking, hide disabled methods, define pay-at-property semantics and do not block an approved pay-at-property booking on unrelated online-provider readiness. |
| PUB-022 | Server-owned booking capacity, price and deposit snapshot | Checkout submit | `bookRoom()` | `ReservationService` locks room type, validates property/capacity/availability, calculates price and captures deposit policy | Reservation, detail, property configuration and hold | Customer | Reservation service 13/13 passed, including deposit policy; HTTP path not executed | `PARTIAL` | P1 | Add validated request DTOs, room-type status validation, API/security/database integration tests and quote/version evidence in the booking response. |
| PUB-023 | Booking-request idempotency and ambiguous client retry | Submit button suppresses concurrent clicks only within one component instance | No booking idempotency key | Reservation creation has no persisted request identity; a timeout/reload/retry can create a second reservation and hold before payment-attempt idempotency begins | Reservations, details and holds | Customer | Component pending-submit test passed; backend idempotency is absent | `BROKEN` | P0 | Add persisted booking idempotency plus normalized payload hash and return the original reservation for equivalent retries; test timeout, double-tab, replay and conflicting payloads. |
| PUB-024 | Overbooking protection and reservation-hold expiry | Booking and pending-payment inventory | Reservation and payment clients | Room-type pessimistic lock, availability recount and persisted expiring hold are present | Room types, reservation details and holds | Customer/system scheduler | H2 integration passed 1/1 simultaneous last-room booking and 2/2 persisted/concurrent hold expiry cases; service suite remains green | `PARTIAL` | P0 | Execute payment/expiry races, restart recovery and SQL Server locking tests before claiming the complete production concurrency boundary. |
| PUB-025 | Multiple rooms of one room type | Detail quantity selector and one reservation detail | Quantity in booking request | Capacity, availability, price and hold quantity support one room type with quantity N | One reservation detail and one hold quantity | Customer | Source trace; checkout quantity tests are component-level | `PARTIAL` | P1 | Add successful and sold-out quantity API/browser tests, assignment completeness checks and atomic rollback when fewer than N physical rooms are allocatable. |
| PUB-026 | Mixed room-type multi-room cart | No cart or multi-line booking UI | None | Request accepts one `roomTypeId`; service creates one detail and one hold | Model can store multiple details, but creation cannot | Customer | No implementation/test | `MISSING` | P1 | Define a multi-line quote/cart, atomically lock room types in deterministic order, validate combined guests/prices/holds and support partial-unavailable recovery without partial booking. |
| PUB-027 | Anonymous public booking identity and ownership | Reachable `POST /api/reservations/public/book`, but no current UI uses it | None | Creates a new `guest_<millisecond>` user with fake local email and `NOPASSWORD`; no lookup token, ownership credential or duplicate-guest policy | `users`, reservations and PII | Public | Source trace | `BROKEN` | P1 | Disable the endpoint or implement a verified guest-booking identity/retrieval model, collision-safe identity, consent/retention rules, rate limits and owner-bound confirmation/access. |
| PUB-028 | Booking success and payment-attempt retry | Checkout success panel | Booking component and property payment panel | Reservation is created once in component state; payment attempt uses idempotency and can retry without recreating that in-memory reservation | Reservation and payment attempts | Customer | Checkout/payment-panel units passed; no live provider/API journey | `PARTIAL` | P1 | Persist resumable checkout state, recover after reload or redirect, distinguish reservation success from payment success and execute pending/success/failure/expiry browser journeys. |
| PUB-029 | Public search query performance and result consistency | Search results | One request from UI | Each page row triggers additional room-type, per-room-type availability and image queries after a native query with multiple correlated subqueries | Hotels, room types, rooms, images and reservations | Public | Source trace; no performance measurement | `PARTIAL` | P2 | Replace N+1 enrichment with bounded projections/batches, index the canonical predicates and measure p95 for representative nationwide data and maximum page size. |
| PUB-030 | Public-booking HTTP, concurrency and runnable API evidence | Not user-facing; supports public discovery/search/booking verification | Angular public flows remain covered separately | Four integration suites pin `BackendApplication` and use deterministic public, booking and simulator-payment fixtures | H2 isolated contexts with persisted rooms, reservations, payment policy and holds | Test/runtime only | Fresh Maven run passed 14/14; evidence: `docs/testing/evidence/007/remediation/T187-public-booking-http-concurrency.md` | `COMPLETE_VERIFIED` | P1 | Preserve the pinned contexts and deterministic fixtures; live browser/API product journeys and SQL Server races remain tracked by their capability rows. |

## Required Remediation Order

T149 must order public-booking remediation as follows:

1. Close the P0 sellable-inactive-room-type, booking-idempotency and overbooking-evidence
   boundaries in PUB-016, PUB-023 and PUB-024.
2. Establish one public eligibility and availability policy and retire/delegate the legacy search
   endpoint in PUB-007, PUB-012 and PUB-015 while preserving the completed PUB-030 harness.
3. Make filter, price, capacity and quote behavior use the same eligible room type from search
   through locked reservation creation.
4. Align configured payment methods, build complete property details, and finish one-type then
   mixed-type multi-room journeys.
5. Resolve anonymous booking identity/privacy and optimize the nationwide discovery queries only
   after integrity and ownership are executable.

No production payment enablement, real-money journey, destructive guest cleanup or automatic
activation of unapproved inventory is authorized by these remediation tasks.

Inventory count: 2 `COMPLETE_VERIFIED`, 17 `PARTIAL`, 2 `PLACEHOLDER`, 7 `BROKEN`,
2 `MISSING`, 0 `BLOCKED_EXTERNAL`, 0 `NOT_APPLICABLE`.
