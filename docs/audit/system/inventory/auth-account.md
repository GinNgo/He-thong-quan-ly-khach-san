# Authentication And Account Inventory

Audit date: 2026-08-01
Scope: public registration and login, session lifecycle, password recovery/change,
profile/avatar, social login, registration email, authorization context and the
supporting persistence/test evidence required by FR-043 and FR-044.

## Evidence Rules

- `COMPLETE_VERIFIED` is used only when a fresh executed test proves the reachable behavior.
- Source inspection without a successful execution remains `PARTIAL`, `BROKEN`, `MISSING`,
  `PLACEHOLDER` or `BLOCKED_EXTERNAL`.
- Severity is `P0` critical access-control risk, `P1` release-significant, `P2` product-quality,
  and `P3` minor polish.
- The auth endpoints are public by design through `/api/auth/**`; account/profile/upload endpoints
  require an authenticated JWT. No production OAuth, SMTP or other real credentials were used.

## Fresh Verification

| Command | Result | Evidence impact |
|---|---|---|
| `backend/.\\mvnw.cmd -q "-Dtest=AuthControllerIntegrationTest,CustomUserDetailsServiceAccountStatusTest" test` | 10/10 passed against the full `BackendApplication` H2 context | T215 proves username/email normalization, success, wrong-password, unknown-user and disabled-user handling without account enumeration. |
| `backend/.\\mvnw.cmd -q clean "-Dtest=CredentialRegistrationIntegrationTest,AuthServiceTest" test` | 11/11 passed: normalized registration, stable username/email conflicts, validation and concurrent duplicate protection plus service regression | T214 proves server-owned credential normalization, versioned identity constraints and one-account-only concurrent registration. Evidence: `docs/testing/evidence/007/remediation/T214-credential-registration.md`. |
| T216 focused Angular 8/8, backend HTTP 16/16 and Chromium 2/2 | Access-token storage, migration, expiry timer, interceptor denial, stale-session redirect and backend malformed/expired token rejection passed | T216 documents and verifies the tab-scoped access-token lifecycle. Evidence: `docs/testing/evidence/007/remediation/T216-access-token-session.md`. |
| `frontend/npx ng test --no-watch --no-progress --reporters=json --output-file=.codex-t141-auth-tests.json --include ...login... --include ...register... --include ...profile...` | 8/8 tests passed across the three requested specs | Login form controls/session user id, disabled social providers, registration messaging, and existing profile payment/refund presentation assertions are current. Profile mutation, password and avatar behavior are not covered by these tests. |
| `frontend/$env:LUXESTAY_E2E_WEB_URL='http://127.0.0.1:4217'; npx playwright test e2e/credential-login-logout.spec.ts --project=chromium --workers=1 --reporter=line` | 1/1 Chromium journey passed | T215 proves a username can be submitted through the public form, the returned session reaches the account menu, and logout clears both browser auth artifacts. Evidence: `docs/testing/evidence/007/remediation/T215-credential-login.md`. |
| `backend/.\\mvnw.cmd '-Dtest=AuthRefreshTokenIntegrationTest,AuthControllerIntegrationTest,RefreshTokenServiceTest' '-DforkCount=0' test` | 17/17 passed after T218 | T218 proves logout revokes the refresh family, clears the cookie and rejects an already-issued access token with `SESSION_REVOKED`; evidence: `docs/testing/evidence/007/remediation/T218-logout-revocation.md`. |
| `backend/.\\mvnw.cmd '-Dtest=PasswordResetServiceTest,PasswordResetControllerIntegrationTest' '-DforkCount=0' test` | 7/7 passed; `frontend/npm run build -- --configuration production` and focused auth specs 5/5 passed | T220 proves enumeration-safe reset requests, hashed one-time tokens, rate limiting, expiry/replay rejection, session revocation after reset and the public recovery routes; evidence: `docs/testing/evidence/007/remediation/T220-password-reset.md`. |
| `frontend/npx ng test --no-watch --coverage=false --include=auth-session-lifecycle.spec.ts --include=auth-refresh.interceptor.spec.ts` | 5/5 passed; `cd frontend; npm run build` completed | T218 verifies tab-scoped cleanup and the production bundle after logout/realtime disconnect wiring; evidence: `docs/testing/evidence/007/remediation/T218-logout-revocation.md`. |
| Source/API/database trace | Controllers, services, guards/interceptors, entities, configuration and tests inspected | Source-only rows are not promoted to `COMPLETE_VERIFIED`. |

## Capability Inventory

| ID | Capability | Route / UI | Frontend service | Backend API / service | Database / storage | Permission | Tests / evidence | Status | Severity | Required remediation |
|---|---|---|---|---|---|---|---|---|---|---|
| AUTH-001 | Credential registration | `/register`; `RegisterComponent` | `AuthService.register()` normalizes identifiers and maps conflict field errors | `POST /api/auth/register`; `AuthService.register()` uses normalized pre-checks plus database-race fallback | `users.version`, normalized identity indexes, role-code and user-role uniqueness in V48 | Public | T214 backend 11/11 and Angular register 2/2; evidence: `docs/testing/evidence/007/remediation/T214-credential-registration.md` | `COMPLETE_VERIFIED` | P1 | Preserve normalized identity and stable 409 contract; email verification and throttling remain separate auth tasks. |
| AUTH-002 | Username/password login | `/login`, `/admin/login` | `AuthService.login()`, `setSession()`, client logout | `POST /api/auth/login`; `AuthenticationManager`, `CustomUserDetailsService`, `JwtTokenProvider` normalize and resolve username or email | Reads `users`, roles and permission mappings | Public | T215 backend 10/10 and Chromium login/logout 1/1 cover username/email success, wrong password, unknown user, disabled account, session creation and browser cleanup; evidence: `docs/testing/evidence/007/remediation/T215-credential-login.md` | `COMPLETE_VERIFIED` | P1 | Preserve the enumeration-safe 401 contract and canonical username token subject; expiry, refresh and server-side revocation remain AUTH-004 to AUTH-006. |
| AUTH-003 | Suspended/disabled account enforcement | All login and authenticated routes | Login/admin-login show the bilingual suspension reason; the global 401 interceptor clears token, user and permission state and redirects protected routes with `reason=ACCOUNT_DISABLED` | `AccountStatusPolicy` rejects every status except normalized `ACTIVE`; credential/social issuance, HTTP JWT reload and new chat/notification WebSocket authentication all pass through the gate; `JwtAuthenticationEntryPoint` and auth handlers preserve `ACCOUNT_DISABLED` through wrapped Spring exceptions | Reads `users.status` on authentication reload; no token/session table is required for the current stateless JWT design | Public login; every protected request and realtime reconnect requires an active account | `AccountStatusPolicyTest` 2/2, focused auth/security units 9/9, `AuthControllerIntegrationTest` 6/6 including suspended login and post-issuance disablement, Angular login/interceptor tests 13/13; evidence: `docs/testing/evidence/007/remediation/T193-account-status-enforcement.md` | `COMPLETE_VERIFIED` | P0 | Preserve the stable code and reload gate. Proactive termination of an already-open WebSocket and durable refresh/session revocation remain AUTH-006/CROSS-002 scope. |
| AUTH-004 | Access-token session and renewal handling | All authenticated routes | `AccessTokenSessionStore` keeps access tokens in tab-scoped `sessionStorage`, migrates valid legacy tokens, rejects malformed/expired JWTs, and drives interceptor plus `AuthService` expiry cleanup | Stateless signed JWT with configured expiry; malformed and expired bearer tokens fail with stable 401 responses | No server session record; access token is browser-tab scoped and user metadata is non-authoritative | Authenticated bearer token | T216 Angular 8/8, backend HTTP 16/16 and Chromium 2/2 cover migration, expiry, malformed tokens, header denial, stale protected-route recovery and logout; evidence: `docs/testing/evidence/007/remediation/T216-access-token-session.md` | `COMPLETE_VERIFIED` | P1 | Preserve the documented tab-scoped invariant; rotating HttpOnly refresh cookies are complete in AUTH-005/T217, while server revocation remains AUTH-006/T218. |
| AUTH-005 | Refresh token / silent renewal | Protected API retry path; no standalone UI | `AuthService.refreshAccessToken()` uses credentialed refresh and `authRefreshInterceptor` shares one in-flight request, stores the renewed access token and retries the failed request | `POST /api/auth/refresh`; `RefreshTokenService` issues hash-only rotating families, locks the candidate row, detects replay and expires/revokes sessions; successful login issues the cookie | `auth_refresh_tokens` created by additive V49; raw token only in HttpOnly cookie, SHA-256 hash and family/status lifecycle persisted | Authenticated session plus same-origin `X-Refresh-Request` marker; backend remains authoritative | T217 backend 23/23, Angular 10/10 and production build passed; evidence: `docs/testing/evidence/007/remediation/T217-refresh-token-rotation.md` | `COMPLETE_VERIFIED` | P1 | Preserve hash-only storage, family replay revocation and single-flight retry; explicit logout/session revocation remains AUTH-006/T218. |
| AUTH-006 | Logout and token revocation | Profile/layout logout actions | `AuthService.logout()` clears tab-scoped token/user/permission state, calls the credentialed logout boundary and broadcasts realtime disconnect | `POST /api/auth/logout`; `AuthSessionRevocationService` sets `users.auth_revoked_at`, while `RefreshTokenService` revokes the presented family; `JwtAuthFilter` rejects tokens issued before the revocation instant | `users.auth_revoked_at` added by V50; active `auth_refresh_tokens` rows are marked `REVOKED` with `LOGOUT` | Same-origin `X-Logout-Request` marker; bearer/cookie session ownership remains server-authoritative | T218 backend logout regression plus Angular 5/5 focused lifecycle/refresh tests and production build; evidence: `docs/testing/evidence/007/remediation/T218-logout-revocation.md` | `COMPLETE_VERIFIED` | P1 | Preserve family revocation, access-token invalidation and immediate chat/notification disconnect. |
| AUTH-007 | Remember-me | Login forms | The unsupported remember-me checkbox was removed; login submits only username and password under the fixed session policy | `LoginRequest` and `AuthService.login()` accept only username/password; no alternate session lifetime is exposed | None | Public | `remember-me-removal.spec.ts` verifies both public/admin forms omit the checkbox and the request field | `COMPLETE_VERIFIED` | P2 | Keep the session lifetime fixed until an explicitly approved short/long policy is designed and tested. |
| AUTH-008 | Forgot/reset password | `/forgot-password`, `/reset-password?token=...`; recovery forms link from public/admin login | `AuthService.requestPasswordReset()` and `resetPassword()` | `POST /api/auth/forgot-password` returns a generic 202 response; `POST /api/auth/reset-password` validates a locked, hashed, expiring one-time token, updates the password and revokes existing sessions | `password_reset_tokens` created by V51; only SHA-256 token hashes and email fingerprints are persisted | Public request; token completion is self-contained and server-authoritative | T220 service 6/6, HTTP 1/1, Angular recovery 3/3 plus build; evidence: `docs/testing/evidence/007/remediation/T220-password-reset.md` | `COMPLETE_VERIFIED` | P1 | Preserve enumeration-safe responses, rate limits, one-time expiry/replay semantics and secret-managed mail settings. |
| AUTH-009 | Authenticated password change | `/settings`; admin profile password dialog | `UserService.changePassword()` | `PUT /api/users/me/password`; `UserService.changePassword()` | Updates `users.password_hash` | `isAuthenticated()` | No focused backend/frontend test found | `PARTIAL` | P1 | Add `@Valid` request constraints, one password policy shared by all UIs/backend, stable current-password error, session revocation after change and positive/negative tests. |
| AUTH-010 | Email verification and email-change verification | Registration/profile forms | No verification state or flow | Registration immediately sets `ACTIVE`; profile update accepts a new email directly | No verification token, verified-at or pending-email fields | Missing | Google adapter checks provider email verification only; local accounts do not | `MISSING` | P1 | Add verification state/tokens, resend/expiry rules and verified email-change flow; decide whether unverified accounts may book or only browse. |
| AUTH-011 | Current profile read | `/profile`, `/settings`, `/admin/profile` | `UserService.getProfile()`, `ClientApiService.getProfile()` | `GET /api/users/me`; `UserService.getUserWithSaaSContext()` | `users`, roles, properties, subscription and booking/chat counts | `isAuthenticated()` and current principal id | Existing profile spec passed but only asserts payment/refund presentation, not profile loading | `PARTIAL` | P2 | Add controller/service tests for own-profile isolation and role/SaaS context, plus client loading/error/empty assertions. |
| AUTH-012 | Profile update | `/profile`, `/admin/profile` | `UserService.updateProfile()` and auth-state synchronization | `PUT /api/users/me`; `UserService.updateProfile()` | Updates name, email, phone and avatar URL in `users` | `isAuthenticated()` and current principal id | Source-only; no focused mutation or validation test | `PARTIAL` | P1 | Validate/normalize fields, prevent arbitrary URL schemes, cover duplicate email, and keep login identity consistent: registration uses email as username but changing email currently leaves the old username. |
| AUTH-013 | Avatar upload and display | Client/admin profile file controls and layout avatars | `UserService.uploadAvatar()` then profile update | `POST /api/uploads/image`; `FileUploadService`; public `GET /api/public/uploads/{filename}` | Filesystem `uploads/` plus `users.avatar_url`; no upload metadata/ownership record | Upload requires `isAuthenticated()`; read is public | Security architecture recognizes public serving; no upload service/controller/UI test | `PARTIAL` | P1 | Verify image signatures instead of trusting MIME only, bound dimensions, add safe error contracts, delete replaced/orphaned files, add storage/authorization tests and document production object storage. |
| AUTH-014 | Registration welcome-email template and safe failure response | Register success message distinguishes delivered/not delivered | Typed `RegistrationResponse.welcomeEmailSent` | `EmailService.sendRegistrationSuccess()` escapes template values and fails registration email safely | No durable outbox/retry record | Triggered after public registration | `EmailServiceTest` 2/2 and register messaging test passed | `COMPLETE_VERIFIED` | P2 | Keep the truthful response contract; durable retry/observability belongs to the SMTP delivery row. |
| AUTH-015 | Actual SMTP delivery, retry and observability | No mail administration UI | No status/history client | Gmail SMTP adapter configured by application properties; delivery exception is logged and returns false | No outbox, delivery attempt or bounce record | External mail credential | Internal adapter/template tests passed; no sandbox inbox evidence or credentials | `BLOCKED_EXTERNAL` | P2 | Add environment-driven host/port/security settings, a non-production mailbox contract, retry/outbox and delivery audit; configure only sandbox/test credentials via secrets. |
| AUTH-016 | Google social login | Google button is disabled unless frontend client id exists | `AuthService.googleLogin()` via `angularx-social-login` | `POST /api/auth/google`; verifies audience and verified email | Links/provisions by email only; no provider identity table | Public OAuth callback adapter | Fail-closed unit and disabled-UI test passed; no provider sandbox credential | `BLOCKED_EXTERNAL` | P1 | Configure the same sandbox client id in frontend and `GOOGLE_CLIENT_ID`, register localhost origins, use test accounts, add adapter contract tests and never commit credentials. |
| AUTH-017 | Facebook social login | Facebook button disabled unless app id exists | `AuthService.facebookLogin()` via `angularx-social-login` | `POST /api/auth/facebook`; debug-token app-id check and `/me` profile lookup | Links/provisions by email only; no provider identity table | Public OAuth callback adapter | Fail-closed unit and disabled-UI test passed; no Facebook test app/user credentials | `BLOCKED_EXTERNAL` | P1 | Use a Facebook test app/test users, set frontend app id plus backend `FACEBOOK_APP_ID/SECRET`, verify app domains and add mocked contract/error tests; keep the secret backend-only. |
| AUTH-018 | Social-account provisioning/linking | Social login creates a customer when email is unknown | Social completion stores returned session | `AuthService.createSocialUser()` and `createSocialAuthResponse()` | `users`, customer role; no provider subject/link record | Public provider token | No successful adapter/provisioning/concurrency test; social response contains no permission masks | `PARTIAL` | P1 | Persist provider plus immutable provider subject, define explicit linking/unlinking and collision policy, load equivalent permission context and add duplicate/concurrent provisioning tests. |
| AUTH-019 | Roles, permission masks and auth menu context | Route guards/layout menus consume roles/permissions | Session stores roles and permissions | Credential login returns role and permission masks; `GET /api/auth/my-menu` builds menu from masks | Roles, functions and role-permission mappings | Bearer token for meaningful results; route is under the broad public auth matcher | Credential unit does not assert masks; social login returns an empty permission list; no menu auth test | `PARTIAL` | P1 | Align credential/social responses, explicitly annotate menu authentication, test permission aggregation and ensure server authorization never depends on client-stored masks. |
| AUTH-020 | Login throttling, lockout and auth audit | No UI state | None | No rate limiter, failed-attempt lockout, login audit or suspicious-activity event found | No login-attempt/audit persistence | Missing | No source or test found | `MISSING` | P1 | Add IP/account-aware throttling, safe lockout/unlock policy, structured audit events without secrets, monitoring and deterministic time-based tests. |
| AUTH-021 | Auth legal/support destinations | Login/register footer and terms/support text | Static anchors/notices | No supporting route/API | None | Public | Terms, privacy, cookie, contact and support links use `href="#"` or unsupported text | `PLACEHOLDER` | P2 | Link real localized policy/support routes, prevent page-jump placeholders and include keyboard/screen-reader/browser checks. |
| AUTH-022 | Auth HTTP integration test harness | Not user-facing | Angular focused suite executes | `AuthControllerIntegrationTest` and `CredentialRegistrationIntegrationTest` pin `BackendApplication` and supply only test encryption keys | H2 `create-drop`; Flyway disabled | Test-only | Auth login suite 9/9 plus account-status unit 1/1 and T214 registration suite 5/5; duplicate/conflict, login normalization and concurrency are executable | `COMPLETE_VERIFIED` | P1 | Preserve the pinned contexts and deterministic fixtures. |

## External Sandbox Contract

No production provider or mail integration is approved by this inventory.

| Adapter | Required non-production configuration | Minimum verification before status can change |
|---|---|---|
| SMTP | Secret-managed test mailbox credentials; environment-driven host, port, username, password, TLS, sender and login URL | Register a disposable test address, verify the rendered message and link in the sandbox inbox, record provider response and prove registration still succeeds on delivery failure. |
| Google | Frontend test client id, matching backend `GOOGLE_CLIENT_ID`, registered `http://localhost:4200` origin and provider test account | Valid token, wrong audience, unverified email, expired token, existing account and new account cases. |
| Facebook | Frontend test app id; backend-only `FACEBOOK_APP_ID` and `FACEBOOK_APP_SECRET`; Facebook test app and users | Valid token, wrong app id, invalid/expired token, missing email, existing account and new account cases. |

## Required Follow-Up Set For T149

T149 must create dependency-ordered implementation tasks for every row marked `PARTIAL`,
`PLACEHOLDER`, `BROKEN` or `MISSING`. The first dependency chain must address AUTH-003,
AUTH-005/AUTH-006, AUTH-008/AUTH-010 and AUTH-020 before broader account UI polish.
`BLOCKED_EXTERNAL` rows still require the adapter/configuration work described above, but may not
be activated with production credentials or production messages during Feature 007.

Inventory count: 5 `COMPLETE_VERIFIED`, 8 `PARTIAL`, 2 `PLACEHOLDER`, 0 `BROKEN`,
4 `MISSING`, 3 `BLOCKED_EXTERNAL`, 0 `NOT_APPLICABLE`.
