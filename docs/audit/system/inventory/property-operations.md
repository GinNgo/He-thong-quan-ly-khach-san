# Property Operations Inventory

Audit date: 2026-08-01
Scope: staff accounts, roles, permissions, property data, images, amenities,
operational policies, room types, physical rooms, room states, maintenance,
housekeeping and property services required by FR-043 and FR-044.

## Evidence Rules

- `COMPLETE_VERIFIED` requires fresh executable evidence for the reachable behavior.
- Source inspection without a successful execution is not promoted to complete.
- Severity is `P0` critical tenant/access/inventory-integrity risk, `P1` release-significant,
  `P2` product-quality and `P3` minor polish.
- Property-scoped operations must resolve the property from authorized access and must not rely
  on a user-wide legacy subscription or a client-supplied property identifier as authority.

## Fresh Verification

| Command / evidence | Result | Evidence impact |
|---|---|---|
| Focused backend suite for staff, permission interception, rooms, room types, services and checkout housekeeping | 32/32 passed | Staff scope/quota unit behavior, room/room-type quota denial, current service CRUD implementation and exactly-once checkout housekeeping creation are current. |
| T186 operations HTTP, tenant, feature-gate and architecture suite | 17/17 passed against the pinned `BackendApplication` H2 context | Staff allow/deny, unauthenticated denial, cross-tenant room/room-type/housekeeping/invoice/payment IDOR, own/super-admin housekeeping and feature allow/deny behavior are executable. |
| Focused Angular property/room-type/room/service/management-dashboard suite | 6/6 passed | Draft property payload/validation and component creation/loading are current. Most room, service and lifecycle mutations remain untested. |
| Source/API/database trace | Controllers, services, entities, repositories, migrations, guards and management/admin UI inspected | Source-only rows remain partial, broken, placeholder or missing. |

## Capability Inventory

| ID | Capability | Route / UI | Frontend service | Backend API / service | Database | Permission | Tests / evidence | Status | Severity | Required remediation |
|---|---|---|---|---|---|---|---|---|---|---|
| PROP-OPS-001 | Staff list and tenant-scoped read | `/admin/users`; `UserManagement` | `UserService.getUsers()` | `GET /api/users`; `UserService.getAllUsers()` uses accessible property ids for non-system users | `users`, `user_properties`, roles | `USER:VIEW` | Staff service scope plus HTTP super-admin allow and ordinary-user forbid assertions passed; multi-property staff-field privacy remains untested | `PARTIAL` | P1 | Test multi-property and cross-property reads, return only staff fields required by the screen and stop loading property options from the public hotel search. |
| PROP-OPS-002 | Staff creation and property assignment | Staff create dialog | `UserService.createUser()` | `POST /api/users`; validates accessible property, blocks privileged roles and checks `MAX_STAFF` | Creates `users`, roles and active `user_properties` staff mapping | `USER:CREATE` | Four staff creation/quota/scope unit cases passed | `PARTIAL` | P1 | Add validated DTOs, explicit password policy/invitation flow, allowed staff-role allowlist, duplicate normalization, transactional HTTP tests and remove the hard-coded fallback password. |
| PROP-OPS-003 | Staff update, role change and property move | Staff edit dialog | `UserService.updateUser()` | `PUT /api/users/{id}`; blocks self/privileged/cross-property mutation and moves active staff mapping | Updates user and staff mappings | `USER:UPDATE` | Property-move unit case passed; no HTTP/concurrency test | `PARTIAL` | P1 | Validate state/roles/fields, lock concurrent moves, preserve assignment history, use property-scoped entitlement and add positive/IDOR/rollback tests. |
| PROP-OPS-004 | Staff deactivate/remove lifecycle | UI offers status change and destructive delete | `updateUser()` / `deleteUser()` | Status is persisted but account status is not enforced by authentication; DELETE physically removes the user and every assignment | Deletes `users` and `user_properties`; historical foreign keys may block or lose actor identity | `USER:UPDATE` / `USER:DELETE` | Delete scope units passed but suspended-login defect is confirmed in AUTH-003 | `BROKEN` | P0 | Replace hard delete with reasoned assignment/account deactivation, retain historical actor references, enforce status on login/JWT reload and test active-session revocation and rehire. |
| PROP-OPS-005 | Staff quota and entitlement scope | Limit shown in management context | Management API | Staff count aggregates all accessible properties and checks legacy user-wide `account_subscriptions` | Cross-property usage versus property-scoped platform entitlement | Owner/manager mutation | Unit test proves the aggregation call, not correct property scope | `BROKEN` | P0 | Resolve `MAX_STAFF` from the target property's authoritative platform entitlement and enforce count plus insert atomically with multi-property and concurrency tests. |
| PROP-OPS-006 | Custom role catalog CRUD | `/admin/roles` | `RoleService` | `/api/roles`; normalized unique codes and soft-deactivation for custom roles | Global `roles` plus user counts | `ROLE` action masks | Source inspection; no focused backend or Angular role test | `PARTIAL` | P1 | Add validated requests, HTTP/UI tests, assignment/deactivation invariants and an explicit decision whether roles are system-global or property-owned templates. |
| PROP-OPS-007 | System-role integrity | Role UI hides delete for `systemRole` but still permits edit | `RoleService.updateRole()` | Delete protects known system codes; update protects only code changes and can still alter name, description and status | Global system-role rows | `ROLE:UPDATE` / `ROLE:DELETE` | Source-only | `BROKEN` | P1 | Make immutable fields and active status explicit for every system role, enforce server-side regardless of stale flags and test seeded-role tampering. |
| PROP-OPS-008 | Permission tree and role permission mutation | `/admin/role-permissions` | `RoleService.getRolePermissionsTree()` / `updateRolePermissions()` | `RolePermissionService` replaces masks; only `SUPER_ADMIN` is protected and a null permission list deletes all entries | Global `role_permissions`, functions and modules | `ROLE_PERMISSION:VIEW/UPDATE` | Permission interceptor 7/7 passed; no mutation service/controller test | `BROKEN` | P0 | Protect all governed system roles, reject null/duplicate/unknown entries, validate masks against supported actions, add optimistic locking/audit and test privilege-escalation/lockout cases. |
| PROP-OPS-009 | Role and staff mutation audit/concurrency | No audit/history UI | None | No immutable role, permission or staff-assignment mutation audit/version contract found | Only generic created/updated metadata | Missing | No source/test | `MISSING` | P1 | Add actor/reason/before-after audit events, version checks and concurrent role/assignment mutation tests with a safe admin history view. |
| PROP-OPS-010 | Property administration CRUD | `/admin/properties`; owner management dashboard | `PropertyService`; `ManagementApiService` | Admin create accepts an entity directly; update copies only a subset; delete is physical; owner portal can create but has no property edit endpoint | `hotels` and cascading property images/assignments | Admin endpoints require `SUPER_ADMIN`; route lacks a frontend guard | Property draft payload tests 2/2 passed; no backend lifecycle CRUD test | `BROKEN` | P1 | Use validated DTOs and a state-aware service, add owner-scoped edit for allowed draft/active fields, replace hard delete with closure/retention and add audit/rollback tests. |
| PROP-OPS-011 | Property profile field completeness | Admin create form and `/management/properties` create | Property and management clients | Management create persists bilingual text, contact, coordinates and check-in/out; admin update drops many of these fields | `hotels` has mixed legacy/current fields | Property access or `SUPER_ADMIN` depending endpoint | Draft payload test passed; source-only persistence trace | `PARTIAL` | P1 | Define one canonical property profile DTO, validate contact/URL/time/coordinates, support edit/read round trips and remove inconsistent legacy field handling. |
| PROP-OPS-012 | Property gallery management | Admin form accepts one `mainImage` URL; no gallery screen | None | No property-image create/reorder/primary/delete API found | `property_images` exists with primary, bilingual alt text and order | Missing | Seed/search reads only | `MISSING` | P1 | Add tenant-safe gallery upload/link/reorder/primary/delete endpoints and UI, enforce image quota atomically and test orphan cleanup and cross-property IDOR. |
| PROP-OPS-013 | Image validation, ownership and storage lifecycle | Room-type URL textarea; room create DTO URLs; generic authenticated upload | Inventory clients and avatar upload client | Room/room-type services accept arbitrary URL strings; generic upload has no property asset ownership; replaced/deleted assets are not reclaimed | Filesystem plus property/room-type/room image rows | Mixed authenticated and inventory permissions | Quota denial units passed; no scheme/signature/ownership tests | `BROKEN` | P1 | Introduce property-owned media records, allow approved URL/storage schemes only, verify file signatures/dimensions, require alt text/order rules and garbage-collect replaced assets safely. |
| PROP-OPS-014 | Amenities catalog and property/room amenity assignment | Search card can render amenities; customer room-search uses hard-coded filters | Public model has `amenities?: string[]` | No amenity entity/controller/service found; search response always sets an empty list | Missing | Missing | Source trace | `MISSING` | P1 | Add localized amenity catalog, property and room-type assignments, admin/owner management, indexed search filtering and end-to-end display/filter tests. |
| PROP-OPS-015 | Operational policies and house rules | No management screen | None | Hotel stores only free-form check-in/check-out strings; no cancellation, child, pet, smoking, deposit-at-property or house-rule model/API | No versioned property-policy tables | Missing | Source trace | `MISSING` | P1 | Define versioned localized policy models with effective dates, validation and booking snapshots; add owner UI and customer-visible details before policy-dependent booking changes. |
| PROP-OPS-016 | Room-type CRUD and soft disable | `/admin/room-types`, `/management/room-types` | Admin inventory and management clients | Tenant checks, property-local code uniqueness, numeric validation, quota checks and `INACTIVE` soft delete | `room_types` | Direct API uses `ROOM_TYPE` actions; management API uses role-only gate | Two quota/image unit cases passed; Angular component creation only | `PARTIAL` | P1 | Add complete CRUD/UI behavior tests, database uniqueness, active-booking disable rules, per-property entitlement and permission parity for management routes. |
| PROP-OPS-017 | Room-type image replacement | Admin room-type editor accepts newline-separated URLs | Admin inventory client | Update replaces all room-type images and checks aggregate image quota | `room_type_images` | `ROOM_TYPE:CREATE/UPDATE` on direct API | Image-quota rejection unit passed | `PARTIAL` | P1 | Integrate owned uploads, validate URLs/alt text/order/primary image, make replacement concurrency-safe and prove failed replacement leaves the old gallery intact. |
| PROP-OPS-018 | Physical room CRUD and bulk creation | `/admin/rooms`, `/management/rooms` | Admin inventory and management clients | Property/type ownership checks, property-local room number check, bulk cap and soft out-of-service delete | `rooms` | Direct API uses `ROOM` actions; management API uses role-only gate | Three quota/scope units and shallow Angular creation test passed | `PARTIAL` | P1 | Add DTO validation, database unique `(hotel_id, room_number)`, transactional bulk semantics, booking-conflict rules and full CRUD/IDOR/concurrency tests. |
| PROP-OPS-019 | Physical-room image lifecycle | No room image editor | DTO may carry images only on create | Create writes images; update explicitly does not update them and no separate endpoint exists | `room_images` | `ROOM:CREATE` only in reachable flow | Source-only | `BROKEN` | P1 | Add tenant-safe room image add/reorder/primary/delete or remove unsupported DTO fields; enforce owned media/quota and add update/rollback tests. |
| PROP-OPS-020 | Authoritative room-state transitions | Admin room dialog exposes room, housekeeping and maintenance status selectors directly | Generic `updateRoom()` | Arbitrary strings are copied without a transition policy; an operator can manually set occupied/reserved/clean states independently of bookings/tasks | Three independent status columns on `rooms` | `ROOM:UPDATE` or broad management role | No transition tests; current UI exposes unsafe combinations | `BROKEN` | P0 | Introduce one locked room-state policy driven by assignment/check-in/checkout/housekeeping/maintenance commands, reject invalid combinations and add concurrency/availability regression tests. |
| PROP-OPS-021 | Maintenance workflow | Admin room table toggles maintenance on/off through generic room update | Admin inventory client | No maintenance work order, reason, assignee, schedule, completion evidence or conflict policy | Only `rooms.maintenance_status` and note | `ROOM:UPDATE` | No behavior test | `PLACEHOLDER` | P1 | Replace the toggle with reasoned maintenance work orders, booking-impact checks, assignment/history and explicit start/complete/reopen transitions. |
| PROP-OPS-022 | Checkout creates dirty room and one housekeeping task | Checkout flow | Property checkout client | `CheckoutOperationsService` applies dirty-room/task effects with a tenant-scoped idempotency key | `rooms`, `housekeeping_tasks`; unique checkout effect migration | Checkout permissions | Fresh `CheckoutOperationsServiceTest` 6/6 passed plus prior T080 evidence | `COMPLETE_VERIFIED` | P1 | Preserve exactly-once and rollback coverage while implementing the missing work-queue lifecycle. |
| PROP-OPS-023 | Housekeeping queue, assignment and start | Dashboard shows only pending count | No task client or screen | Repository supports pending lookup, but no list/create/assign/start endpoints were found | `housekeeping_tasks` has assignee and timestamps | Missing; `HOUSEKEEPING` role is not admitted by management controller | No source/test | `MISSING` | P1 | Add property-scoped queue/list/assign/start endpoints and responsive staff UI with dedicated permissions, assignment ownership and stale-task handling. |
| PROP-OPS-024 | Housekeeping completion and room release | No reachable task list; API exists by task id | None | `POST /api/management/housekeeping/{taskId}/complete` validates tenant and terminal state, then marks room clean/available when not under maintenance | Updates task and room transactionally | Broad management roles; no dedicated action permission | Fresh tenant integration passed own-property, cross-property denial and super-admin completion; replay, dedicated permission and UI remain untested | `PARTIAL` | P1 | Add a dedicated permission/housekeeping role, task ownership rules, optimistic locking/idempotency, UI access and replay tests. |
| PROP-OPS-025 | Property service catalog tenant isolation | `/admin/services` | `HotelServiceService` | `HotelServiceLogicImpl` uses global `findAll/findById`, never resolves authorized property and DTO cannot bind a property; null-hotel rows are visible through the tenant filter | `services.hotel_id` nullable; code globally unique | Backend uses `HOTEL` actions | Six unit tests pass only the global repository behavior | `BROKEN` | P0 | Make property id server-authorized, separate immutable system templates from tenant services, use tenant-safe queries/constraints and add cross-property read/write/delete tests. |
| PROP-OPS-026 | Service route/API authorization parity | `/admin/services` route requires `HOTEL_SERVICE` | Hotel service client | Every `/api/services` endpoint checks `FunctionCode.HOTEL` instead of `HOTEL_SERVICE` | Role permission mappings use the migrated `HOTEL_SERVICE` code | UI and API disagree | Source trace; no HTTP test | `BROKEN` | P1 | Align on `HOTEL_SERVICE`, add action-specific UI controls and endpoint tests proving matching allow/deny decisions. |
| PROP-OPS-027 | Service create/edit/deactivate UI | Service table displays add/edit/delete buttons | Client has CRUD methods | Buttons have no click handlers, dialog or form; only list/reload works | Service rows readable | No UI action checks | Angular test only creates the component | `PLACEHOLDER` | P1 | Build validated localized CRUD UI with permission-aware controls, loading/error states and behavior tests after the tenant-safe backend is fixed. |
| PROP-OPS-028 | Service validation, pricing status and history | Table renders price/status | CRUD client | Backend accepts unvalidated code/name/price/status and physically deletes rows used by charge snapshots | `services`; charge lines retain snapshots but catalog history is absent | `HOTEL` actions currently | Global CRUD unit tests pass | `BROKEN` | P1 | Validate positive integer VND and enums, use soft deactivation/versioning, block unsafe deletion and test historical folio/invoice stability. |
| PROP-OPS-029 | Management portal action-level authorization | `/management/properties`, `/management/room-types`, `/management/rooms` | `ManagementApiService` | Controller uses one broad role check and no `@Permission` per read/create/update/housekeeping action | Property access is checked inside services, but action masks are bypassed | Any owner/admin/manager role admitted to all routes | Security architecture, feature-gate and tenant/IDOR suites pass, but the tests confirm only broad role/feature protection rather than action-mask parity | `BROKEN` | P1 | Apply the same action permissions as direct APIs, add frontend guards/control visibility and execute a role/action/tenant matrix for every endpoint. |
| PROP-OPS-030 | Operations HTTP, tenant and feature-gate test harness | Not user-facing | Angular focused suite executes | Full integration suites pin `BackendApplication`; a non-secret property-payment key is test-scoped and the production refund orchestrator constructor is injectable | H2 `create-drop`; Flyway disabled | Test-only | Fresh Maven run passed 17/17 permission, IDOR, tenant-filter, feature-gate and architecture assertions; evidence: `docs/testing/evidence/007/remediation/T186-operations-http-tenant-feature-gate.md` | `COMPLETE_VERIFIED` | P1 | Preserve the pinned context and test-only key; capability defects found by the suite remain tracked in PROP-OPS-001 through PROP-OPS-029. |
| PROP-OPS-031 | Property operations dashboard | `/management/dashboard` | `ManagementApiService.context()` | Returns per-selected-property room/housekeeping counts but combines them with user-wide legacy plan/usage data | Room/task counts by property; subscription/usage across accessible properties | Broad management role | Dashboard render test passed | `PARTIAL` | P2 | Use authoritative per-property entitlement/usage, expose timestamp/error semantics and add count reconciliation plus property-switch isolation tests. |

## Required Remediation Order

T149 must order the operations remediation work as follows:

1. Close the P0 boundaries in PROP-OPS-004, PROP-OPS-005, PROP-OPS-008,
   PROP-OPS-020 and PROP-OPS-025 before adding broader operations UI.
2. Restore the HTTP/security harness in PROP-OPS-030 and enforce action parity in
   PROP-OPS-026 and PROP-OPS-029 so tenant and permission fixes are executable.
3. Establish canonical property, room-state, service and entitlement models before
   expanding image, maintenance or housekeeping workflows.
4. Add owned media, amenities and versioned property policies, then complete the
   housekeeping queue and maintenance work-order lifecycle.
5. Add immutable operational audit/concurrency evidence and update every affected
   inventory row only after fresh service, HTTP and browser verification.

No production media migration, destructive property/user cleanup or automatic policy
activation is authorized by these remediation tasks.

Inventory count: 2 `COMPLETE_VERIFIED`, 10 `PARTIAL`, 2 `PLACEHOLDER`, 12 `BROKEN`,
5 `MISSING`, 0 `BLOCKED_EXTERNAL`, 0 `NOT_APPLICABLE`.
