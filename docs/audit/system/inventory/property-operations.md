# Property Operations Inventory

Audit date: 2026-08-01
Scope: staff accounts, roles, permissions, property data, images, amenities,
operational policies, room types, physical rooms, room states, maintenance,
housekeeping and property services required by FR-043 and FR-044.

## Evidence Rules

- `COMPLETE_VERIFIED` requires fresh executable evidence for the reachable behavior.
- Source inspection without a successful execution is not promoted to complete.
- Severity is `P0` critical tenant/access/inventory-integrity risk, `P1` release-significant,
  `P2` product-quality and `P3` minor polish.
- Property-scoped operations must resolve the property from authorized access and must not rely
  on a user-wide legacy subscription or a client-supplied property identifier as authority.

## Fresh Verification

| Command / evidence | Result | Evidence impact |
|---|---|---|
| Focused backend suite for staff, permission interception, rooms, room types, services and checkout housekeeping | 32/32 passed | Staff scope/quota unit behavior, room/room-type quota denial, current service CRUD implementation and exactly-once checkout housekeeping creation are current. |
| T186 operations HTTP, tenant, feature-gate and architecture suite | 17/17 passed against the pinned `BackendApplication` H2 context | Staff allow/deny, unauthenticated denial, cross-tenant room/room-type/housekeeping/invoice/payment IDOR, own/super-admin housekeeping and feature allow/deny behavior are executable. |
| Focused Angular property/room-type/room/service/management-dashboard suite | 6/6 passed | Draft property payload/validation and component creation/loading are current. Most room, service and lifecycle mutations remain untested. |
| T252 canonical property-profile backend/frontend verification | Backend 20/20 and Angular 9/9 passed; Angular development build passed | Admin and owner create/read/update now share the full validated profile contract, including tenant-safe read/update denial and persistence round trips. |
| T253 property-gallery backend/frontend verification | Backend 17/17 and Angular 15/15 passed; Angular development build passed | Tenant-safe upload/link/reorder/primary/delete behavior, aggregate locked quota, managed-file rollback/commit cleanup and both management surfaces are executable. |
| Source/API/database trace | Controllers, services, entities, repositories, migrations, guards and management/admin UI inspected | Source-only rows remain partial, broken, placeholder or missing. |

## Capability Inventory

| ID | Capability | Route / UI | Frontend service | Backend API / service | Database | Permission | Tests / evidence | Status | Severity | Required remediation |
|---|---|---|---|---|---|---|---|---|---|---|
| PROP-OPS-001 | Staff list and tenant-scoped read | `/admin/users`; `UserManagement` | `UserService.getStaff()` and `getStaffProperties()` | Dedicated `GET /api/users/staff` and `/api/users/staff/properties` resolve authorized operational properties, scope assignment history and serialize screen-specific DTOs | `users`, `user_properties`, roles, hotels | `USER:VIEW` on both endpoints | Backend HTTP tenant/privacy 3/3, existing service regression 11/11 and Angular client/UI 5/5 passed; [T245 evidence](../../../testing/evidence/007/remediation/T245-staff-list-tenant-scope.md) | `COMPLETE_VERIFIED` | P1 | Preserve dedicated staff/property-option contracts and property-scoped serialization when T246-T250 extend staff mutations. |
| PROP-OPS-002 | Staff creation and property assignment | Staff create dialog with required password, role and property guidance | `UserService.getStaffRoles()` / `createStaff()` | Dedicated validated `GET /api/users/staff/roles` and `POST /api/users/staff`; normalizes identifiers, requires an explicit 8-256 character initial password, enforces the active staff-role allowlist, authorized property scope and `MAX_STAFF`, and persists account plus assignment transactionally | Creates normalized `users`, allowlisted roles and one active `user_properties` staff mapping; assignment failure rolls back the flushed user | `USER:VIEW` role catalog; `USER:CREATE` mutation; property authority resolved server-side | Backend service 13/13, staff-create HTTP 7/7, rollback 1/1 and staff-read regression 3/3; Angular client/UI 9/9; [T246 evidence](../../../testing/evidence/007/remediation/T246-staff-creation-validation.md) | `COMPLETE_VERIFIED` | P1 | Preserve the dedicated contract and explicit-password policy; an invitation workflow remains intentionally absent until a separately specified mail/token lifecycle exists. |
| PROP-OPS-003 | Staff update, role change and property move | Staff edit dialog validates profile, replacement password, roles, target property and move reason | `UserService.updateStaff()` | Dedicated validated `PUT /api/users/staff/{id}` locks the account and assignment history, enforces the active staff-role allowlist and tenant authority, applies target-property `MAX_STAFF`, preserves the closed source assignment and creates one new active assignment transactionally; the generic update endpoint rejects staff bypass | Updates normalized user profile/roles/default property and appends a new `user_properties` assignment period while retaining the inactive source period | `USER:UPDATE`; target property is resolved from authenticated management access and cross-property accounts are hidden/denied | Backend service/HTTP/concurrency/rollback plus T245-T246 regression 33/33; Angular client/UI 12/12; [T247 evidence](../../../testing/evidence/007/remediation/T247-staff-update-property-move.md) | `COMPLETE_VERIFIED` | P1 | Preserve the dedicated endpoint, user-before-assignment lock order, property-scoped quota and assignment-history invariant; mutation audit/version history remains tracked by PROP-OPS-009. |
| PROP-OPS-004 | Staff deactivate/remove lifecycle | Staff management table with deactivate/rehire reason dialog | `UserService.deactivateStaff()` / `UserService.reactivateStaff()` | Property-scoped assignment lifecycle records reason, actor and timestamps; account becomes inactive when no active assignment remains; JWT reload rejects it | `user_properties` rows remain historical; additive lifecycle metadata and actor reference | `USER:DELETE` / `USER:UPDATE` | Backend lifecycle/security suite passed; Angular lifecycle suite 2/2; evidence: `docs/testing/evidence/007/remediation/T199-staff-lifecycle.md` | `COMPLETE_VERIFIED` | P0 | Extend property-scoped `MAX_STAFF` count-plus-insert enforcement in PROP-OPS-005. |
| PROP-OPS-005 | Staff quota and entitlement scope | Limit shown in management context/dashboard | Management API | `UserService` locks the target property's entitlement, counts active `STAFF` assignments for that property, then applies `MAX_STAFF` before insert/rehire; management usage exposes the same selected-property count | Property-scoped platform entitlement and active `user_properties` rows | Owner/manager mutation | `UserServiceTest` quota isolation/order cases, `ManagementPortalServiceTest` selected-property usage assertion, frontend dashboard suite 2/2; evidence: `docs/testing/evidence/007/remediation/T200-staff-quota-scope.md` | `COMPLETE_VERIFIED` | P0 | Preserve entitlement lock ordering and add database-backed concurrent HTTP evidence when the production test harness is extended. |
| PROP-OPS-006 | Custom role catalog CRUD | `/admin/roles` supports create/edit/deactivate/reactivate with loading and conflict feedback | `RoleService` | Validated metadata-only `/api/roles` requests, case-insensitive normalized codes, pessimistic mutation locks, assigned-role deactivation guard and soft lifecycle; roles explicitly remain global templates because `app_role` has no property ownership while staff tenant scope remains in `user_properties` | Global `app_role` plus user assignment counts; no client-supplied property or lifecycle ownership | `ROLE:VIEW/CREATE/UPDATE/DELETE`, with DELETE for deactivation and UPDATE for reactivation | Backend service/HTTP 11/11 and Angular client/UI 7/7; [T248 evidence](../../../testing/evidence/007/remediation/T248-custom-role-catalog.md) | `COMPLETE_VERIFIED` | P1 | Preserve the global-template boundary and assignment guard; T249 separately hardens all seeded system-role metadata and status fields. |
| PROP-OPS-007 | System-role integrity | Catalog and permission screens classify by authoritative seeded code/flag and hide edit/lifecycle mutation controls | `Role` governed-system policy shared by `RoleService`, `RolePermissionService` and `DataInitializer` | Seeded codes or explicit system flags make code/name/description/classification immutable; entity callbacks repair stale flags and force `ACTIVE`; reserved codes cannot be recreated as custom roles; API tampering returns conflict | Global `app_role`; current seeded-code policy also preserves explicitly flagged legacy system roles | `ROLE:UPDATE` / `ROLE:DELETE` plus server-side system-role invariant | Backend entity/catalog/permission/HTTP 24/24 and Angular predicate/UI 9/9; [T249 evidence](../../../testing/evidence/007/remediation/T249-system-role-integrity.md) | `COMPLETE_VERIFIED` | P1 | Keep the entity/client governed-code lists synchronized through the shared API projection; schema-level metadata constraints would require a separate additive migration if future direct database writers are introduced. |
| PROP-OPS-008 | Permission tree and role permission mutation | `/admin/role-permissions` | `RolePermissionService` | Governed system roles are immutable; payloads reject null/duplicate/unknown entries and unsupported action bits; role version is checked optimistically and every accepted mutation writes an append-only audit snapshot | `app_role`, `app_role_permission`, `app_role_permission_audit`, functions and modules | `ROLE_PERMISSION:VIEW/UPDATE` | Service governance 5/5, Spring/H2 mapping 3/3, frontend build passed; evidence: `docs/testing/evidence/007/remediation/T201-role-permission-governance.md` | `COMPLETE_VERIFIED` | P0 | Preserve the expected-version contract and add HTTP 409/concurrent database evidence in the next security harness pass. |
| PROP-OPS-009 | Role and staff mutation audit/concurrency | Role/staff screens require mutation reasons and expose permission-gated targeted history links to `/admin/audit-log` or `/management/audit-log` | Versioned role/staff requests plus targeted operational-audit filters | Role create/update/lifecycle and staff update/assignment/lifecycle mutations write append-only actor/reason/before-after snapshots; stale request versions fail after tenant authority checks, while existing row locks serialize competing writers | `app_role.version`, `users.version`, `user_properties` history and append-only `operational_audit_events` | Existing `ROLE`/`USER` action permissions; history requires `AUDIT_LOG:VIEW`; tenant viewers remain property-filtered by `OperationalAuditService` | Backend role/staff/audit HTTP, service, integration and concurrency suites 50/50; Angular clients, mutation dialogs and targeted audit filters 28/28; [T250 evidence](../../../testing/evidence/007/remediation/T250-role-staff-audit-concurrency.md) | `COMPLETE_VERIFIED` | P1 | Preserve request-version checks after tenant authority, append-only redacted snapshots and permission-gated tenant-safe history filtering. |
| PROP-OPS-010 | Property administration CRUD | `/admin/properties`; owner management dashboard profile editor | `PropertyService`; `ManagementApiService` | Validated DTOs keep lifecycle server-owned; locked state-aware service supports admin drafts, active-owner scoped profile edit, draft/rejected submission and reasoned `CLOSED` retention instead of physical deletion; every accepted mutation appends before/after audit evidence and closed properties are excluded from public detail | Retained `hotels` aggregate, `user_properties`, images and dependent history; append-only `operational_audit_events` | Admin create/update/close require `SUPER_ADMIN`; owner edit additionally requires an active property-local `OWNER` assignment and hides cross-property ids as not-found | Backend lifecycle/HTTP/IDOR/public-visibility/rollback 14/14 and Angular clients/admin payload/owner editor 7/7; [T251 evidence](../../../testing/evidence/007/remediation/T251-property-administration-lifecycle.md) | `COMPLETE_VERIFIED` | P1 | Preserve server-owned lifecycle fields, row-lock-before-transition ordering and same-transaction audit rollback; T252 extends the shared profile field/format contract. |
| PROP-OPS-011 | Property profile field completeness | `/admin/properties` complete create/edit dialog; management dashboard complete owner profile editor | Shared `PropertyProfile` model through `PropertyService` and `ManagementApiService` | One `PropertyProfileDTO` drives admin/owner create, list, detail and update responses; update uses `{profile, reason}`; centralized mapping validates property type, province/ward, HTTP(S) URL, coordinate pairing and price order while DTO validation covers contact, time and coordinate bounds; server-owned identifiers/lifecycle fields remain read-only | Existing `hotels` columns are round-tripped completely; canonical writes synchronize legacy `name`/`description`, derive city/country from location and retain lifecycle/audit history | Admin routes require `SUPER_ADMIN`; owner detail/update requires an active local `OWNER` assignment and hides cross-property ids as not-found | Backend DTO/mapper/service/HTTP/IDOR 20/20, Angular service/admin/owner 9/9 and Angular development build passed; [T252 evidence](../../../testing/evidence/007/remediation/T252-property-profile-canonical.md) | `COMPLETE_VERIFIED` | P1 | Preserve the canonical full-profile contract and read-only lifecycle boundary; T253-T254 own gallery/media validation beyond the single legacy `mainImage` field. |
| PROP-OPS-012 | Property gallery management | Shared gallery editor in `/admin/properties` and the management dashboard supports link/upload, ordering, primary selection and delete with loading/error/empty states | `PropertyGalleryService` client uses one property-scoped contract for list/link/upload/reorder/primary/delete | `/api/v1/properties/{propertyId}/gallery` resolves super-admin or active assigned-property scope; mutations lock the property before aggregate `MAX_IMAGES` count-plus-insert, enforce exact property-local reorder membership, keep one primary image and synchronize `hotels.main_image`; upload rollback and delete-after-commit reclaim managed files | Existing `property_images` stores URL, bilingual alt text, primary and contiguous order; managed files use property-scoped random names in configured upload storage; no schema change | Existing management roles at the controller plus independent service-side tenant authority; cross-property property/image ids return not-found and closed/pending-review properties are immutable | Backend gallery/storage 17/17, Angular client/shared/admin/owner 15/15 and Angular development build passed; [T253 evidence](../../../testing/evidence/007/remediation/T253-property-gallery-management.md) | `COMPLETE_VERIFIED` | P1 | Preserve property-lock-before-entitlement ordering and post-commit file deletion; T254 owns deeper shared media ownership/validation rules across room-type and room galleries. |
| PROP-OPS-013 | Image validation, ownership and storage lifecycle | Shared property gallery; room-type URL editor; room-create image contract | `PropertyMediaService` and `PropertyMediaPolicy` shared by property, room-type and room inventory services | New links require credential-free absolute HTTPS and Vietnamese alt text; uploads verify signature/content type/dimensions/pixels, persist checksum/storage metadata and use property-scoped ownership; gallery rows keep contiguous order/single primary; replaced or deleted managed assets are reclaimed only when unreferenced and after commit | `property_media` owns tenant/source/storage/checksum/dimension/alt metadata; property/room-type/room image rows reference it; V80 backfills legacy rows and normalizes physical-room metadata | Existing property/gallery and inventory action permissions plus independent property access and tenant filter on owned media | Backend 39/39, Angular 16/16, development build and disposable SQL Server migration/backfill/idempotence validation pass; [T254 evidence](../../../testing/evidence/007/remediation/T254-property-media-lifecycle.md) | `COMPLETE_VERIFIED` | P1 | Preserve HTTPS/alt/signature policy, reference-counted post-commit deletion and tenant ownership; T258 and T260 retain dedicated room-type and physical-room gallery CRUD/concurrency UI scope. |
| PROP-OPS-014 | Amenities catalog and property/room amenity assignment | Reusable assignment editor in admin property/room-type dialogs and owner dashboard/inventory; public search filter and result badges | `AmenityService` clients use public/system catalog plus property/room-type assignment contracts; search URL round-trips `amenityIds` | Global localized catalog has validated code/category/soft status; tenant-safe property and room-type replacement resolves ownership server-side and rejects duplicate/inactive IDs before mutation; public search requires every selected active amenity from either property or active room types and returns localized badges | `amenities`, `property_amenities`, `room_type_amenities`; V81 seeds eight localized entries, enforces unique ownership and composite room-type/property integrity, and adds forward/reverse search indexes | Catalog lifecycle requires `SUPER_ADMIN`; assignment endpoints require management roles plus independent property access; public catalog/search are read-only | Backend 10/10, Angular 17/17, development build and disposable SQL Server catalog/ownership/index/idempotence validation pass; [T255 evidence](../../../testing/evidence/007/remediation/T255-amenity-catalog-assignments.md) | `COMPLETE_VERIFIED` | P1 | Preserve soft catalog lifecycle, exact tenant-owned replacement and all-selected-amenity semantics; T277 retains cancellation/pay-at-property/breakfast policy badges and wider public-booking snapshot scope. |
| PROP-OPS-015 | Operational policies and house rules | Reusable version editor in admin property dialog and owner dashboard; effective policy on public property detail and booking checkout | `OperationalPolicyService` management/public contracts; checkout acknowledges the displayed version and booking service snapshots the authoritative effective version | Tenant-owned drafts are validated and versioned under a property lock; published versions are immutable, effective windows do not overlap, stale client versions are rejected, and no cancellation/refund amount is inferred from free-form policy text | `property_policy_versions`; reservation stores policy id/version/effective time and immutable localized JSON snapshot; V82 adds uniqueness, status/range constraints, FK and effective-date indexes | Management roles plus independent property assignment check and Hibernate tenant filter; public read exposes only the current policy of an active approved property | Backend 8/8, Angular 22/22, development build and disposable SQL Server migration/idempotence/constraint validation pass; [T256 evidence](../../../testing/evidence/007/remediation/T256-operational-policy-snapshots.md) | `COMPLETE_VERIFIED` | P1 | Preserve published-version immutability and booking snapshot semantics; cancellation charges/refunds remain separately policy-gated and are not derived by this task. |
| PROP-OPS-016 | Room-type CRUD and soft disable | `/admin/room-types`, `/management/room-types` provide validated create/edit/deactivate flows and permission-aware actions | Admin inventory and management clients share create/update/delete contracts | Tenant-owned mutations validate normalized code/name, price/capacity/status, enforce property entitlement, lock updates/deactivation, preserve property ownership and reject deactivation while a nonterminal booking references the type | `room_types`; V83 enforces property-local normalized code uniqueness plus price/capacity/status checks | Direct and management routes use matching `ROOM_TYPE:VIEW/CREATE/UPDATE/DELETE` permissions plus independent property access | Backend 11/11, Angular 5/5, development build and disposable SQL Server migration/idempotence/constraint validation pass; [T257 evidence](../../../testing/evidence/007/remediation/T257-room-type-crud-soft-disable.md) | `COMPLETE_VERIFIED` | P1 | Preserve locked mutation ordering, active-booking guard and permission parity; T258 owns the dedicated gallery replacement lifecycle. |
| PROP-OPS-017 | Room-type image replacement | Reusable gallery editor in admin and owner room-type management supports owned upload, HTTPS link, reorder, primary and delete | Dedicated `RoomTypeGalleryService` client; ordinary room-type edits no longer replace the gallery | Property-scoped gallery API locks property then room type, enforces aggregate image quota, tenant ownership, validated Vietnamese alt/HTTPS metadata, collision-safe ordering, one primary and post-commit media cleanup; failed association leaves the prior gallery intact | `room_type_images` references property-owned media; V84 normalizes legacy order/primary state and enforces unique order plus one primary | `ROOM_TYPE:VIEW` for list and `ROOM_TYPE:UPDATE` for mutations, plus independent assigned-property access | Backend 14/14, Angular 13/13, development build and disposable SQL Server normalization/idempotence/constraint validation pass; [T258 evidence](../../../testing/evidence/007/remediation/T258-room-type-gallery-lifecycle.md) | `COMPLETE_VERIFIED` | P1 | Preserve property-before-room-type lock order, atomic association rollback and reference-counted media cleanup; T260 applies the same lifecycle to physical-room images. |
| PROP-OPS-018 | Physical room CRUD and bulk creation | `/admin/rooms` and `/management/rooms` support validated create/edit/bulk/deactivate with permission-aware controls | Admin inventory and management clients share direct/management CRUD and atomic bulk contracts | Property then room/type locks serialize quota and number allocation; normalized room numbers, property/type ownership and DTO bounds are enforced; bulk preflights the full range before any insert; active assigned bookings block room-type change and soft deactivation | `rooms`; V85 normalizes legacy numbers, enforces unique `(hotel_id, room_number)` and floor/trim bounds | Direct and management routes use matching `ROOM:VIEW/CREATE/UPDATE/DELETE` permissions plus independent tenant access | Backend 17/17, Angular 6/6, development build and disposable SQL Server migration/idempotence/constraint validation pass; [T259 evidence](../../../testing/evidence/007/remediation/T259-physical-room-crud-bulk.md) | `COMPLETE_VERIFIED` | P1 | Preserve property-first lock ordering, all-or-nothing preflight and active-booking guard; T260 owns physical-room gallery lifecycle. |
| PROP-OPS-019 | Physical-room image lifecycle | Reusable gallery editor in admin and owner room edit flows supports owned upload, HTTPS link, reorder, primary and delete | Dedicated `RoomGalleryService` client | Property-scoped room gallery locks property then room, enforces aggregate image quota, tenant ownership, validated Vietnamese alt/HTTPS metadata, collision-safe ordering, one primary and transaction-aware media cleanup | `room_images` references property-owned media; V86 normalizes legacy order/primary state and enforces unique order plus one primary | `ROOM:VIEW` for list and `ROOM:UPDATE` for mutations, plus independent assigned-property access | Backend 6/6, Angular 15/15, development build and disposable SQL Server migration/idempotence/constraint validation pass; [T260 evidence](../../../testing/evidence/007/remediation/T260-physical-room-gallery-lifecycle.md) | `COMPLETE_VERIFIED` | P1 | Preserve property-before-room lock order, atomic association rollback and reference-counted media cleanup. |
| PROP-OPS-020 | Authoritative room-state transitions | Admin room table and maintenance command controls | Admin inventory and management inventory clients | `RoomStatePolicy` owns initialize, reserve, check-in, release, checkout, maintenance, deactivation and housekeeping transitions; generic room update rejects state mutations; direct and management maintenance commands use locked service methods | `rooms` with optimistic `version` and V37 state-domain/ownership checks | `ROOM:UPDATE` for maintenance commands; property access remains server-enforced | Focused backend suite 35/35; frontend build and focused maintenance tests pass; SQL Server lifecycle 2/2; V37 migration/invalid-state check pass; [T202 evidence](../../../testing/evidence/007/remediation/T202-authoritative-room-state.md) | `COMPLETE_VERIFIED` | P0 | T021 still covers reasoned maintenance work orders; T207 still covers broader deterministic assignment uniqueness/concurrency. |
| PROP-OPS-021 | Maintenance workflow | Shared work-order panel in admin and owner room inventory exposes create, assignment, schedule, impact, history and explicit lifecycle actions | Dedicated `MaintenanceWorkOrderService` client | Tenant-scoped work orders validate property-local assignees, lock work order/room transitions, block start during active booking assignment and integrate with the authoritative room-state policy | V87 adds work orders/history, lifecycle/schedule checks and one-active-order-per-room uniqueness | `ROOM:VIEW/CREATE/UPDATE/DELETE` by action, plus independent assigned-property access | Backend 7/7, Angular 12/12, development build and disposable SQL Server V87 validation pass; [T261 evidence](../../../testing/evidence/007/remediation/T261-maintenance-work-orders.md) | `COMPLETE_VERIFIED` | P1 | Preserve tenant IDOR behavior, `NORMAL` priority contract, reasoned reopen/cancel transitions and room-lock integration. |
| PROP-OPS-022 | Checkout creates dirty room and one housekeeping task | Checkout flow | Property checkout client | `CheckoutOperationsService` applies dirty-room/task effects with a tenant-scoped idempotency key | `rooms`, `housekeeping_tasks`; unique checkout effect migration | Checkout permissions | Fresh `CheckoutOperationsServiceTest` 6/6 passed plus prior T080 evidence | `COMPLETE_VERIFIED` | P1 | Preserve exactly-once and rollback coverage while implementing the missing work-queue lifecycle. |
| PROP-OPS-023 | Housekeeping queue, assignment and start | Dashboard shows only pending count | No task client or screen | Repository supports pending lookup, but no list/create/assign/start endpoints were found | `housekeeping_tasks` has assignee and timestamps | Missing; `HOUSEKEEPING` role is not admitted by management controller | No source/test | `MISSING` | P1 | Add property-scoped queue/list/assign/start endpoints and responsive staff UI with dedicated permissions, assignment ownership and stale-task handling. |
| PROP-OPS-024 | Housekeeping completion and room release | No reachable task list; API exists by task id | None | `POST /api/management/housekeeping/{taskId}/complete` validates tenant and terminal state, then marks room clean/available when not under maintenance | Updates task and room transactionally | Broad management roles; no dedicated action permission | Fresh tenant integration passed own-property, cross-property denial and super-admin completion; replay, dedicated permission and UI remain untested | `PARTIAL` | P1 | Add a dedicated permission/housekeeping role, task ownership rules, optimistic locking/idempotency, UI access and replay tests. |
| PROP-OPS-025 | Property service catalog tenant isolation | `/admin/services` property selector and read-only system-template rows | `HotelServiceService` sends the selected property as an API parameter outside the mutable payload | `HotelServiceLogicImpl` resolves the property through `PropertyAccessService`, binds tenant ownership server-side, uses explicit scoped queries, hides cross-property ids and rejects system-template mutation | V39 enforces tenant/system ownership and filtered code uniqueness; legacy null-owner rows become system templates | Backend still uses `HOTEL` actions pending PROP-OPS-026 | Backend 6/6, frontend 3/3 and disposable SQL Server V39 scope validation pass; [T203 evidence](../../../testing/evidence/007/remediation/T203-property-service-tenant-isolation.md) | `COMPLETE_VERIFIED` | P0 | T264 owns permission parity, T265 owns full CRUD UI and T266 owns validation, soft deactivation and history. |
| PROP-OPS-026 | Service route/API authorization parity | `/admin/services` route and action controls use `HOTEL_SERVICE` | Hotel service client | Every `/api/services` endpoint enforces `HOTEL_SERVICE` with matching `VIEW/CREATE/UPDATE/DELETE` actions | Role permission mappings use the same migrated `HOTEL_SERVICE` code | Route, UI and API share one function/action contract | Authorization parity plus tenant service regression 10/10 passed; [T264 evidence](../../../testing/evidence/007/remediation/T264-service-authorization-parity.md) | `COMPLETE_VERIFIED` | P1 | Preserve action-specific checks; `HOTEL` permission alone must never authorize service catalog access. |
| PROP-OPS-027 | Service create/edit/deactivate UI | Service table displays add/edit/delete buttons | Client has CRUD methods | Buttons have no click handlers, dialog or form; only list/reload works | Service rows readable | No UI action checks | Angular test only creates the component | `PLACEHOLDER` | P1 | Build validated localized CRUD UI with permission-aware controls, loading/error states and behavior tests after the tenant-safe backend is fixed. |
| PROP-OPS-028 | Service validation, pricing status and history | Table renders price/status | CRUD client | Backend accepts unvalidated code/name/price/status and physically deletes rows used by charge snapshots | `services`; charge lines retain snapshots but catalog history is absent | `HOTEL` actions currently | Global CRUD unit tests pass | `BROKEN` | P1 | Validate positive integer VND and enums, use soft deactivation/versioning, block unsafe deletion and test historical folio/invoice stability. |
| PROP-OPS-029 | Management portal action-level authorization | `/management/properties`, `/management/room-types`, `/management/rooms` | `ManagementApiService` | Controller uses one broad role check and no `@Permission` per read/create/update/housekeeping action | Property access is checked inside services, but action masks are bypassed | Any owner/admin/manager role admitted to all routes | Security architecture, feature-gate and tenant/IDOR suites pass, but the tests confirm only broad role/feature protection rather than action-mask parity | `BROKEN` | P1 | Apply the same action permissions as direct APIs, add frontend guards/control visibility and execute a role/action/tenant matrix for every endpoint. |
| PROP-OPS-030 | Operations HTTP, tenant and feature-gate test harness | Not user-facing | Angular focused suite executes | Full integration suites pin `BackendApplication`; a non-secret property-payment key is test-scoped and the production refund orchestrator constructor is injectable | H2 `create-drop`; Flyway disabled | Test-only | Fresh Maven run passed 17/17 permission, IDOR, tenant-filter, feature-gate and architecture assertions; evidence: `docs/testing/evidence/007/remediation/T186-operations-http-tenant-feature-gate.md` | `COMPLETE_VERIFIED` | P1 | Preserve the pinned context and test-only key; capability defects found by the suite remain tracked in PROP-OPS-001 through PROP-OPS-029. |
| PROP-OPS-031 | Property operations dashboard | `/management/dashboard` | `ManagementApiService.context()` | Returns per-selected-property room/housekeeping counts but combines them with user-wide legacy plan/usage data | Room/task counts by property; subscription/usage across accessible properties | Broad management role | Dashboard render test passed | `PARTIAL` | P2 | Use authoritative per-property entitlement/usage, expose timestamp/error semantics and add count reconciliation plus property-switch isolation tests. |

## Required Remediation Order

T149 must order the operations remediation work as follows:

1. Close the P0 boundaries in PROP-OPS-004, PROP-OPS-005, PROP-OPS-008,
   PROP-OPS-020 and PROP-OPS-025 before adding broader operations UI.
2. Restore the HTTP/security harness in PROP-OPS-030 and enforce action parity in
   PROP-OPS-026 and PROP-OPS-029 so tenant and permission fixes are executable.
3. Establish canonical property, room-state, service and entitlement models before
   expanding image, maintenance or housekeeping workflows.
4. Add owned media, amenities and versioned property policies, then complete the
   housekeeping queue and maintenance work-order lifecycle.
5. Add immutable operational audit/concurrency evidence and update every affected
   inventory row only after fresh service, HTTP and browser verification.

No production media migration, destructive property/user cleanup or automatic policy
activation is authorized by these remediation tasks.

Inventory count: 13 `COMPLETE_VERIFIED`, 5 `PARTIAL`, 2 `PLACEHOLDER`, 6 `BROKEN`,
5 `MISSING`, 0 `BLOCKED_EXTERNAL`, 0 `NOT_APPLICABLE`.
