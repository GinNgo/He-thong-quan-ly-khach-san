import { TestBed } from '@angular/core/testing'; import { provideHttpClient } from '@angular/common/http'; import { HttpTestingController,provideHttpClientTesting } from '@angular/common/http/testing';
import { environment } from '../../../environments/environment'; import { RoomGalleryService } from './room-gallery.service';
describe('RoomGalleryService',()=>{let service:RoomGalleryService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(RoomGalleryService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());
it('uses room-owned gallery routes',()=>{service.addLink(9,{imageUrl:'https://cdn.example/101.jpg',altTextVi:'Phong 101',primary:true}).subscribe();const request=http.expectOne(`${environment.apiUrl}/v1/rooms/9/gallery/links`);expect(request.request.method).toBe('POST');request.flush({});});
it('sends exact order primary and delete commands',()=>{service.reorder(9,[2,1]).subscribe();http.expectOne({method:'PUT',url:`${environment.apiUrl}/v1/rooms/9/gallery/order`}).flush([]);service.setPrimary(9,2).subscribe();http.expectOne({method:'PUT',url:`${environment.apiUrl}/v1/rooms/9/gallery/images/2/primary`}).flush({});service.delete(9,1).subscribe();http.expectOne({method:'DELETE',url:`${environment.apiUrl}/v1/rooms/9/gallery/images/1`}).flush([]);});});
